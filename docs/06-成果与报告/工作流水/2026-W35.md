# 2026-W35

## 2026-08-24：V9.17 实现与自适应调度单因素消融启动

- 按 [V9.17 完整机制设计](../../01-主线版本/TraceAAD-V9.17-机制设计.md) 完成独立实现 `llm4ad/method/traceaad_v9_17/`：八个初始 hypothesis、统一三步 maturation/development block、按真实前沿质量竞争、正 block gain 续投、停滞后 Discovery、假设内冻结尺度父节点选择、轨迹条件 Refine/Explore、bounded repair、过程工件与精确 checkpoint 恢复。`experiments.runners.traceaad.run` 接入 `v9_17`。
- 建立 [Adaptive vs FixedCycle 单因素消融协议](../../03-机制验证/02-搜索调度与预算分配/2026-08-24-V9.17自适应调度消融/README.md)。两臂共用同一个 V9.17 实现，唯一在线差异为全体 active sweep 结束后的规则：Adaptive 让 `g>0` 的 hypotheses 进入下一 sweep；FixedCycle 无条件进入 Discovery。配置审计中只有 `development_continuation` 一个字段不同。
- 配对覆盖 5 tasks × 3 repeats。每对运行使用相同 seed，并共享第一次 Development 决策前的完整初始化状态：八个 roots、八个三步初始化 maturation、冻结的 `s_R`、初始 active set、树与父节点计数，以及已经消耗的 primary/repair 调用事实。初始化捕获入口为 `experiments.runners.traceaad.capture_v917_initializations`；直接边界截取与按初始化 evaluations/tree/events 重建的 15 份 checkpoint 均通过 V9.17 状态不变量和 FixedCycle 加载校验。
- Adaptive 正式批次为 `v9_17_20260823_adaptive_<task>_rep<1..3>`，FixedCycle 配对批次为 `v9_17_fixed_cycle_20260824_paired_<task>_rep<1..3>`；每路继续计数至 1000 primary slots，repair 的 LLM/evaluator 调用单独记录。
- 30 路搜索全部启动并占满可用生成容量：server3 9、server3b 9、local 3、server1 9；Adaptive 与 FixedCycle 各 15 路。守护会话 `v917_fixed_watch` 根据 checkpoint 自动恢复异常运行，启动核对为 15 对、0 配置或配对错误。
- 预注册分析入口 `experiments/analysis/analyze_v917_scheduler_ablation.py` 按 `task × repeat` 输出 100/250/500/750/1000 best-at-budget、Explore/Refine 份额、Discovery 间隔、连续 sweep、正增益再成功率、Adaptive 追加 block 的 hypothesis/global frontier 命中和预算集中度。最终 search best 与 held-out 在全部 1000-slot 搜索完成后统一评估；当前启动事实不构成机制效果结论。
- 实现、runner、初始化捕获、容量调度与分析的最终定向检查为 77 项通过，相关 Python 文件 Ruff 通过。`no-maturation` 保留为本轮核心单因素消融完成后的第二优先级，未启动。

## 2026-08-24：V9.17 消融首批 9 路 held-out

- 30 路中 9 路完成 1000 primary slots：OBP 两臂各 3 路、VRPTW Adaptive 3 路（rep1 在首批评估启动后数分钟收尾，重测为完整三重复）。统一用 `experiments/evaluate_best.py` 完成 held-out，工件 `traceaad_v9_17/eval_best_20260824_v917_adaptive_complete`（OBP、VRPTW 各自任务目录下）、`traceaad_v9_17_fixed_cycle/eval_best_20260824_v917_fixed_cycle_complete`（OBP），全部规模×重复有效。
- OBP 配对结果与 VRPTW Adaptive 单臂数字（19.084 ± 0.598）已写入[消融文档](../../03-机制验证/02-搜索调度与预算分配/2026-08-24-V9.17自适应调度消融/README.md)结果节：训练口径两臂接近不可分（1000 slots 差 ≤1.5 bins、方向不一致），held-out 按容量分化——1k_100 三重复同向 Adaptive 少 6.6–20.2 bins，5k_500 与跨规模 10k_500 三重复同向 FixedCycle 少约 1 bin。不构成机制结论，判定待全部 30 路与过程量齐备。

## 2026-08-24：V9.17 增加 best 历史

- `RunArtifacts.record_best` 在刷新 `best_program.py` 的同时向 `best_history.jsonl` 追加 `{eval_count, fitness, child_id, program}`，全局最优每次改进留一条，任意预算处的最优程序可事后取用。此前中间最优程序只存在于 checkpoints，批次清理后不可恢复（数值 best-so-far 曲线仍由 evaluations.csv 承担）。
- append-only、随写随 flush，与 evaluations.csv 同级属证据级工件；V9.7 及更早版本不改（各自独立实现）。在跑的 30 路不受影响（进程内代码已加载），从下一次启动或恢复起生效。新增集成测试断言三次全局最优刷新各留一条且与 `best_program.py` 终态一致，v9_17 全部 20 项测试与 Ruff 通过；规范 §12 补记。
- 回填脚本 `experiments/analysis/backfill_best_history.py` 从留存工件重建历史 run 的 `best_history.jsonl`：V9.14–V9.17 完成批走 evaluations.csv 行序 running max + checkpoint 按 `child_id` 取程序文本（42+9 路）；V9.7 正式批与 qwen38 的 TSP/OBP 走 `candidates.jsonl`（预算轴 = evaluator_called 累计，16 路）。共 67 路、0 失败；每路校验条目严格递增、末条与 summary `best_score` 及 `best_program.py` 头一致（6 位有效数字口径）。ppab 24 路与 qwen38 CVRP/OP 8 路的程序文本已随 checkpoint 清理不可恢复，在跑 21 路待收尾后补。

## 2026-08-24：删除 V9.7 之前的版本（代码、测试、死分析脚本与回收站工件）

- 应用户要求清除 V9.7 之前的一切实验产物，仅保留文档中的分析结论与结果统计（git 历史另兜底代码）。删除：方法包 `llm4ad/method/traceaad_{v4,v5,v8,v9}`、对应 11 个测试文件、`analyze_process` 等共 12 个数据源已不存在的分析脚本及其输出目录；`experiments.runners.traceaad.run` 收缩为 v9_7/v9_14–v9_17 六个版本（移除 v4 checkpoint 兼容分支与 v5 专用 `--action-max-tokens` 参数），`_common.py` 移除 v4 进程匹配兜底；`test_traceaad_experiments` 与错误边界测试同步收缩到现存版本。
- 回收站中 V4/V5/V7/V8 冒烟/V9.2 共 48 项旧工件连同 trashinfo 永久删除。全仓 374 项测试通过、触及文件 Ruff 通过；在跑 21 路 V9.17 会话不受影响（命令行参数全部仍存在，11:10 重启的两路按当前代码正常运行）。
- 保留：三个排名脚本中旧版本条目（按 08-23 设计跳过缺失工件，文档行承载统计数字）、`docs/01-主线版本` 与 `docs/01-主线版本` 全部文档、V9.7 及之后的全部工件与分析脚本。

## 2026-08-24：删除非正式分支实验工件

- 删除 Qwen3.8 生成模型对照批次（`v9_7_qwen38_20260815_010107_*` 12 路 run 与全部 `eval_best_*qwen38*`）、ppab 来时路消融（`traceaad_v9_7/ppab_*` 12 路、`traceaad_v9_7_co/` 四任务目录与 `eval_best_ppab_*`；方法包与 launcher 此前已删）、被取代的旧评估（四任务 `eval_best_20260814_1020`、TSP/OP 的 `eval_best_20260821_v914_partial`）。`traceaad_v9_7` 现只含正式批 `20260814_150927` 与 `eval_best_20260815_parentpath`。
- 数字仍由文档承载：`生成模型对比.md` 两节、`父代来时路完整搜索消融.md`、`实验结果.md` CVRP 对照行；v4flash 工件此前已删，该节本就只有数字。374 项测试通过。

## 2026-08-24：五基线四任务重跑批次启动

- 目的：恢复 2026-08-21 删除的五个外部基线在四个非 VRPTW 任务上的逐样本搜索过程（VRPTW 批 20260822 的 logs 完好）。用户确认五方法全跑：EoH/ReEvo/MCTS-AHD/PathWise/CALM × 4 任务 × 3 重复 = 60 路，批次 `20260824_rerun`，写新目录不动原工件；原 `eval_best_*` 仍为正式结果。
- 批次启动：按 repeat → method → task 轮转调度，优先填满 EoH、ReEvo 各四路 rep1，随空位自动接续推进。全仓 374 项测试通过。重跑结果只用于恢复过程曲线与中途预算分析，不更新结果页。

## 2026-08-24：V9.17 第二批 6 路 held-out 与 best_history 回填

- 30 路中 16 路完成：新增 Adaptive TSP rep2/rep3、Adaptive OP rep1/rep3、FixedCycle TSP rep1/rep3。统一 held-out 无失败样本，工件 `eval_best_20260824_v917_adaptive_rep23`（TSP）、`eval_best_20260824_v917_adaptive_rep13`（OP）、`eval_best_20260824_v917_fixed_cycle_rep13`（TSP）；结果已写入[消融文档](../../03-机制验证/02-搜索调度与预算分配/2026-08-24-V9.17自适应调度消融/README.md)结果节。TSP rep3 同 seed 配对跨规模方向不一致（FixedCycle 在 train/TSP50/100 更低，Adaptive 在 TSP200 更低）；OP 两重复 held-out 为 15.134/15.187、30.482/30.770、54.761/56.070。
- 新完成 6 路 `best_history.jsonl` 已回填（累计 69 路）。尚无新的完整 task×repeat 配对；剩余 14 路集中在 FixedCycle 的 CVRP/OP/VRPTW 与两臂零星 rep。

## 2026-08-24：组会研究报告移入 docs/06-成果与报告

- [组会研究报告](../汇报/2026-W33-科研周报.md)（2026-08-12、V9.5 阶段快照）自 `docs/04-研究认识与构想/` 移入 `docs/06-成果与报告/` 并按周次命名，图片与绘图脚本随迁至 `docs/06-成果与报告/assets/traceaad-group-report/`；文内指向 `研究认识` 的链接改为跨目录相对路径。AGENTS.md 文档表补 `docs/06-成果与报告/` 一行。

## 2026-08-24：基线重跑批推进

- 基线重跑批次正常推进，三服务源与本地槽位满载运行；reevo OBP rep1 顺利完成，逐样本过程数据完整落盘。


## 2026-08-24：V9.17 Adaptive 臂五任务闭合、TSP 成对齐

- Adaptive 15/15 全部完成并出齐三重复 held-out：CVRP 8.896/9.232/9.145、15.138/15.641/15.364、27.517/28.543/27.742；OP 15.134/14.724/15.187、30.482/29.014/30.770、54.761/50.017/56.070（rep2 三档一致偏弱）；TSP 5.831/5.835/6.231、8.055/8.126/8.559、11.787/11.536/11.902。Adaptive rep1 的 TSP200 首评 1000 秒超时，3000 秒复测得 11.787 计入 n=3（同 V9.11 先例）。工件 `eval_best_20260824_v917_adaptive_complete`（TSP/CVRP/OP 任务目录下）。
- FixedCycle TSP rep2 收尾后三重复齐并完成 held-out（timeout 3000，工件 `eval_best_20260824_v917_fixed_cycle_complete`）；TSP 成为 OBP 之后第二个完整配对任务。配对形状：训练 250 slots 三重复同向 FixedCycle 更低，1000 slots 方向不一致；held-out TSP200 三重复同向 Adaptive 更低（少 0.139–1.084），TSP50/100 方向不一致。结果已写入[消融文档](../../03-机制验证/02-搜索调度与预算分配/2026-08-24-V9.17自适应调度消融/README.md)；被取代的两处 partial 工件已删。
- best_history 新增回填 7 路（adaptive cvrp×3、op rep2、tsp rep1，fixed tsp rep2、vrptw rep3）。剩余在跑 8 路全部为 FixedCycle（CVRP 611–692、OP 756–830、VRPTW 789/790）。

## 2026-08-25：VRPTW 升三规模协议并成为第三个完整配对

- VRPTW held-out 协议升为 50/100/200 三规模（16 实例 × seed 2025，`evaluate_best.py` 未提交修改 + 新增 `tests/test_vrptw_heldout_protocol.py` 3 项通过）；新协议的 vrptw50 与原单规模 test 数字逐位一致。
- FixedCycle VRPTW 三重复收尾并完成 held-out；Adaptive 侧用新协议重测。VRPTW 成为 OBP、TSP 之后第三个完整配对：vrptw100/200 三重复同向 Adaptive 更低（100 少 0.454–1.383，200 少 0.475–3.987），vrptw50 方向不一致，train best 方向不一致。跨规模泛化 Adaptive 占优与 TSP200 同向。结果写入[消融文档](../../03-机制验证/02-搜索调度与预算分配/2026-08-24-V9.17自适应调度消融/README.md)，工件为两臂各自 `eval_best_20260824_v917_*_complete`（VRPTW 任务目录下）。
- best_history 回填 4 路（fixed op rep1/rep3、vrptw rep1/rep2）。V9.17 现 26/30：剩 OP rep2（约 955/1000，守护自动接测）与 FixedCycle CVRP 三路（764–870）。

## 2026-08-25：V9.17 消融闭合（30/30）

- 最后 4 路收尾（fixed OP rep2、fixed CVRP ×3）并完成 held-out：OP 与 CVRP 配对齐，15 对全部闭合。CVRP 为最强任务级结果——held-out 9 格全部同向 Adaptive 更低，训练 train best 与 500 slots best-at-budget 亦三重复同向；OP 三规模方向不一致，由 Adaptive rep2 单路偏弱驱动（train 14.534 为六路最低）。
- 过程量（`docs/01-主线版本/V9.17/V9.16-V9.17对照分析/v917_scheduler_ablation/summary.json`）：每周期 sweep 数 1.59–3.25 任务分化（CVRP 最高）；追加 blocks 25–103/路，推进 hypothesis frontier 2–47（CVRP 最高）、global frontier 0–8；正增益再成功率全部 ≤0.47（CVRP 0.42–0.47 最高，OP/TSP 低档 0.06–0.24）。预注册判读写入[消融文档](../../03-机制验证/02-搜索调度与预算分配/2026-08-24-V9.17自适应调度消融/README.md)：连续发展长度任务分化成立；追加 blocks 兑现结果只在 CVRP 完整成立并在 TSP200/vrptw100/200 跨规模侧三重复同向，正 block gain 的预测力有限、兑现依赖任务的连续可改进结构。
- 全批 best_history 回填至 30/30。五个任务的 held-out 工件为两臂各自 `eval_best_20260824_v917_*_complete`（VRPTW 为三规模新协议）。

## 2026-08-25：实验结果文档体系统一大表

- [实验结果](../../02-实验结果/00-主实验结果.md) 重构为每任务一张统一大表：5 个外部对照 + TraceAAD V9/V9.14/V9.16/V9.17，首列为搜索集最终 best（held-out 工件 `run_records` 的 `train_artifact_score`），其余列为全部 held-out 规模；单版本排名接入 V9.17（2.367，1/6），VRPTW 段改 8 方法。删除独立维护的同规模测试结果与搜索结果两页。
- [归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md) 收纳全部历史版本（V4–V9.12、V9.15）与 V9.17 FixedCycle 消融臂的五行任务数据，同表记录训练 best 与 held-out；V6–V9.12 训练数字未留存，单元格留空。FixedCycle 五任务三重复数据从 `eval_best_20260824_v917_fixed_cycle_complete` 提取。
- `plot_search_curves.py` 曲线集改为 V9.14/V9.16/V9.17（清理 V9.7/V9.15 残留路径）并重绘 5 张图；`recompute_rankings.py` 已接入 V9.17 全部工件。生成两页废弃文档的 `recompute_search_rankings.py`、`recompute_same_scale_rankings.py` 一并删除。
- [配置](../../02-实验结果/00-主实验配置与冻结契约.md) 主表版本行与结果层级描述同步；V9.16 机制诊断的死链修复。两页 549 个数值格经工件重算值与 git 历史旧文档双源逐格核验。

## 2026-08-25：V9.17 两臂入结果页与搜索曲线

- [实验结果](../../02-实验结果/00-主实验结果.md)主表新增 V9.17 五行（TSP/CVRP/OP/OBV/VRPTW 三规模，VRPTW 旧单规模节已重做为全方法三规模），V9.17 held-out 单版本排名 2.367（1/6）、VRPTW 三规模平均名次 1.667；V9.17 FixedCycle 消融臂五任务五行入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。
- `plot_search_curves.py` 方法集扩为 V9.14 / V9.16 / V9.17 / V9.17 FixedCycle（后者橙色，读 `evaluations.csv`），五张搜索曲线图全部重绘入 `figures/`；FixedCycle 在 CVRP 面板全程低于 Adaptive，与其余面板的配对结果一致。

## 2026-08-25：基线重跑首批 7 批 held-out 与原批次对照

- 重跑批已完成三重复的 7 个 方法×任务 组合统一 held-out（工件各任务方法目录下 `eval_best_20260825_rerun/results.json`，TSP timeout 3000）：eoh 的 TSP/OP/OBP、reevo 的 TSP/OBP、pathwise OBP、calm TSP，全部规模×重复有效。其余 13 组合（CVRP 全部、mcts_ahd 多数、pathwise/calm 部分）仍在跑或差 1 路。
- 与主表原批次数字逐格对照（30 个规模格）：17 格更好、13 格更差；幅度基本在原批次 1 个重复标准差内（如 eoh-obp 容量 100 三档 −3.9/−33.3/−66.4 bins，对应原行 std 3.6/30.9/63.7），属采样波动而非系统性偏移。方向明显的：reevo-tsp 三档更好（−0.20~−0.34）、eoh-tsp 三档更差（+0.17~+0.22）、reevo-obp 六档全更差、eoh-obp 容量 100 明显更好。
- 口径不变：主表基线行保持原批次数字（原评估工件已删、文档行为正式记录）；重跑批用于过程曲线与中途预算分析，不替换官方行。

## 2026-08-25：重跑批评价改为每路完成即测

- 口径修正（用户明确）：held-out 测试与工件记录按"每路完成即测"执行，工件 `run_records` 随完成路数递增覆盖；"全部重复与测试完成"只是结果页更新门槛，不是测试门槛。守护 `rerun_eval_watch` 改为增量模式（每 5 分钟比对各组合已完成路数与工件已记录路数，有新完成路即重评该组合全部已完成路），首扫已补 mcts_ahd/pathwise 的 TSP 各 2 路、eoh-cvrp 1 路等此前等待凑齐的部分。

## 2026-08-25：TraceAAD V9.17 FixedCycle 移入正式结果

- 根据指示，TraceAAD V9.17 FixedCycle 实验结果从[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)移入[实验结果](../../02-实验结果/00-主实验结果.md)正式主表（五任务五行，OBP 5k_500 / 10k_500 为列最佳并加粗）。
- `recompute_rankings.py` 纳入 V9.17 FixedCycle，单版本上场平均名次 3.367（3/6）、VRPTW 9 方法三规模平均名次 4.667；主表排名与[配置](../../02-实验结果/00-主实验配置与冻结契约.md)同步更新。

## 2026-08-25：对比方法重跑结果文档

- 60 路中 59 路完成并全部增量测试完毕（mcts-cvrp rep3 仍在跑，该行暂 n=2）。新增[五个对比方法结果汇总](../../05-文献与基线/对比方法基准/00-对比方法基准汇总.md)：75 个规模格重跑更好 40 / 更差 35，方向混合、幅度多为批间波动；记录 reevo-op rep1 单路塌陷（train 13.664、OP200 37.09）、calm-obp 六档全更好等成组方向。主表基线行保持原批次数字不变。

## 2026-08-26：V9.18-R0 实现、初始化匹配与 A 阶段启动

- 根据 V9.18 battle 结果收缩设计：删除 `F` 历史信用、回撤幅度奖励和完整参考程序；R0 只保留质量主项与新 Explore 入口的衰减 opportunity prior，并把 Global-Facts-Lite 限定为最多三条真实数值事实。轨迹 `V_show` 与在线再选择代理分开识别，规范见[TraceAAD V9.18-R0 完整机制设计](../../01-主线版本/TraceAAD-V9.18-机制设计.md)和[实验协议](../../03-机制验证/02-搜索调度与预算分配/2026-08-25-V9.18机会评分消融/README.md)。
- 建立独立 `llm4ad/method/traceaad_v9_18/`、版本化 `v9_18_r0` checkpoint、R0 artifact 审计字段与统一 runner 三个实验臂：`v9_18_q_atomic`、`v9_18_q_opportunity`、`v9_18_facts`。增加评分、Diagnosis 审计、Global-Facts-Lite prompt、初始化 fork 和恢复边界测试；全仓 `401 passed`，Ruff 与 `git diff --check` 通过。
- 真实 server1 短跑完成 q-atomic 10 slots、q+O 9 slots、Global-Facts-Lite 9 slots；三条均 `status=finished`，checkpoint 和选择前分数快照可读。第一次 fork 冒烟发现并修复 checkpoint 同级工件定位错误，修复后 q+O 从同一八根 bootstrap checkpoint 完成 10-slot fork。
- 完成 5 任务 × 3 重复共 15 条八根 bootstrap，全部 `status=finished`、`budget_slots=8`、八个有效根；bounded repair 作为额外 evaluator call 单独记录。随后按 30 路容量启动 A 阶段：15 条 `q-atomic` 与 15 条 `q+O-atomic`，每对任务×重复共享 bootstrap 根池；当前只报告启动与过程健康，不报告质量结论。
- A 阶段过程审计保持 30/30 路工件可读；当前快照为 7/30 路完整、23/30 路运行中，完整路均达到 `budget_slots=1000` 且无 pending。新增 V9.18 过程审计脚本 `analyze_v918_process.py`，只把完整运行纳入同任务聚合，保留不完整运行的过程记录；机会项以实际 `selected_score-parent_q` 判定，`opportunity>0` 本身只表示入口状态。
- 修复未来批次的审计契约：repair 每次记录真实请求 prompt hash，并清除不在 repair prompt 中的事实板 hash；选择事件保存候选 `q/O/n_after/S` 快照。TraceAAD V9.18 新运行配置记录逻辑模型、backend、seed 与 LLM 元数据，旧版本配置形状保持不变；全仓回归 `404 passed`，V9.18 专属测试 `9 passed`。
- 新增 V9.18 实验入口与[V9.18 机制与证据分析](../../02-实验结果/版本机制诊断/V9.18-R0-机制与证据分析.md)，接管 30 路批次运行并建立过程审计基准。

## 2026-08-26：基线重跑批闭合与对照文档定稿

- 60/60 搜索完成、20/20 组合三重复 held-out 齐备（守护按每路完成即测收尾退出，`ALL_RECORDED`）。[五个对比方法结果汇总](../../05-文献与基线/对比方法基准/00-对比方法基准汇总.md)以完整数据定稿：75 规模格重跑更好 39 / 更差 36、23 格超原批次 1σ，方向无系统性；成组方向全好 6 组（ReEvo/CALM 的 TSP、ReEvo/MCTS-AHD 的 CVRP、MCTS-AHD 的 OP、CALM 的 OBP）、全差 9 组；ReEvo-OP 由 rep1 单路塌陷驱动。主表基线行保持原批次数字。

- [五个对比方法结果汇总](../../05-文献与基线/对比方法基准/00-对比方法基准汇总.md)新增"最差三路口径下的版本排名"节（基线 6 路取每列最差 3 路 vs TraceAAD 正式 3 路，V9.7/V9/V9.16/V9.17 四版本）：最差三路口径下 V9 均名次 1.40 反超 V9.16（1.60），V9.7 第一数最多（11），V9.17 9 项第一且 TSP/CVRP 六列全一；官方口径 V9.16 仍为 1.87 最强。口径切换的排序机制（小分差负于基线强跑的版本受益最大）一并记录。

## 2026-08-26：实验文档三分

- `docs/01-主线版本/` 收成三类：[主实验](../../02-实验结果/00-主实验结果.md)（五对照 + V9 / V9.16 / V9.17，训练曲线与测试表）、[机制实验](../../03-机制验证/00-机制验证总览.md)（`日期-名称`）、[其他实验](../../01-主线版本/00-机制设计演进.md)（历史版本、换模型、基线重跑）。V9.14 与 V9.17 FixedCycle 进入历史版本；主表只保留各 TraceAAD 对五个对照的单版本平均名次。
- 搜索曲线改为一张五任务总图：TraceAAD V9.16 + 五个对照（TSP/CVRP/OP/OBP 用 `20260824_rerun`，VRPTW 用原批）。脚本 `experiments/plotting/plot_search_curves.py`；删 `plot_report_curves.py` 与分任务旧图。

## 2026-08-26：experiments 目录三分归档

- 与文档三分对齐（[索引](../../../experiments/README.md)）：`<task>/<method>/` 规范根保留主实验（对照原批次 + `traceaad_v9_16`/`v9_17` + 在跑 `traceaad_v9_18_*`）；`traceaad_v9_7`/`v9_14`/`v9_15`/`v9_17_fixed_cycle` 迁入 `其他实验/历史版本/`，五对照 `20260824_rerun` 批迁入 `其他实验/基线重跑-20260824/`；机制实验目录待 V9.18 战役结束后迁入。V9.18 在跑 9 路与调度器、bootstrap 根池未动。
- 引用核对后删除无引用工件：VRPTW 被 0824 multiscale 取代的 0823 首批 eval ×7、七月 pathwise 孤儿运行目录 ×11、`eval_best_20260824_v917_fixed_cycle_rep13` 残留、`cvrp_aco/pathwise/eval_20260712_all3`。删除失效分析脚本 11 个：v9_8/v9_9/v9_10/v9_12/v9_13 数据批已清或 import 断链、目标文档不存在；v9_7 的 search_geometry/mechanism_value/region_revisit/audit_v913_proxy 依赖的 checkpoints/logs 已在 08-23 清理中删除（allocation 仍活，读 artifacts/decisions.jsonl）。analysis 剩 6 个。
- 路径更新：`recompute_rankings.py` 增加 `artifact_dir` 解析历史版本位置、`plot_search_curves.py` 对照批次读 `其他实验/基线重跑-20260824/`、`analyze_v917_scheduler_ablation.py` fixed 臂、`analyze_v97_allocation.py` 的 v9_7 根、`backfill_best_history.py` 跳过分类目录；AGENTS.md 实验口径同步。

## 2026-08-26：删除 V9.15 版本代码

- 按保留清单（V9.7 / V9.14 / V9.16 / V9.17 / V9.18）删除 V9.15 实现：`llm4ad/method/traceaad_v9_15/`、`launch_v915.py`、`run.py` 的版本接线（imports、VERSIONS、n_init/校验、build_method 分支、method_params、模型名合集）与 3 个 v915 专属测试及 `test_traceaad_experiments.py` 的 v9_15 参数分支；测试 409 → 380 全过，Ruff 通过。
- V9.15 的历史版本表证据不受影响：eval 工件在 `其他实验/历史版本/`，`recompute_rankings.py` 的 V9.15 行（平均名次 2.400）复算一致。

## 2026-08-27：BehaveSim 语义景观分析（P0+P1）

- 写定[分析计划](../../03-机制验证/03-算法行为几何/2026-08-27-BehaveSim语义景观分析/研究设计.md)后实现离线景观分析器：同一批 V9.16 抽样候选上构造压缩谱系图、BehaveSim kNN 图与距离阈值语义区域，计算结构分离、局部/全局平滑性、funnel、局部最优、高质量区域、时间事件与算子步幅。入口 `experiments/analysis/analyze_behavesim_landscape.py`，测试 8 项。
- 15/15 run 完成且与 v3 校正复算互证。主认识：谱系边与行为近邻几乎不相交（边 Jaccard 0.016–0.037）；VRPTW/OBP 上谱系汇聚到少数语义区域而 OP 一一对应；TSP/OBP/VRPTW 行为近邻 fitness 更近且三重复同向，两个 ACO 任务方向不一致；停滞以小步重访为主（0.45–0.90），大步无改善 ≤0.10；突破发生在更远行为距离。见[结果](../../03-机制验证/03-算法行为几何/2026-08-27-BehaveSim语义景观分析/README.md)。

## 2026-08-27：BehaveSim 相似度与 fitness 关系文档

- 从语义景观实验抽出单一关系的聚焦分析：行为距离与 fitness 差在 15/15 run 全局同向（Spearman 0.27–0.72），分箱梯度最近箱 |z 差| <= 0.03、最远箱 1.5–4.7；到抽样最优的 funnel 15/15 为正。任务级稳定声称限 TSP/OBP/VRPTW。见[分析](../../03-机制验证/03-算法行为几何/2026-08-27-BehaveSim语义景观分析/BehaveSim相似度与fitness关系.md)。

## 2026-08-27：V9.19 完整机制实现

- 按[设计文档](../../01-主线版本/TraceAAD-V9.19-机制设计.md)实现 V9.19：`llm4ad/method/traceaad_v9_19/`（BehaveSim-v3 combined-panel 搜索内 profiler、增量距离矩阵与 kNN 景观、Q/L/U/R_traj/S 节点评分与 target-ESS Boltzmann、G/p_E 自适应四算子、Pareto crossover 参考、轨迹条件提示、有界修复、latest.json+behave.npz 双文件 checkpoint）；runner 接入 `run.py` 的 `v9_19` 版本，新增 `launch_v919.py`（5 任务 × 3 重复、budget 1000、可恢复调度）。
- 测试：机制单测 28 项、launcher 6 项、5 任务集成冒烟（脚本 LLM、真实 evaluator 与 profiling）全过；`test_traceaad_experiments.py` 版本映射补 v9_19。调试中发现并修复 one-vs-many 距离内核的点数计数越界（两侧 lengths 长度不等时 nopython 越界读偶发段错误；真实任务轨迹恒达保留上限不受影响）。

## 2026-08-27：V9.19 训练过程可视化

- 新增 `experiments/plotting/plot_v919_run.py`（只针对 V9.19 的在线工件，不做历史版本）：单路 dashboard 六面板（搜索进度按算子着色、结果滚动占比、算子混合、被选父节点 S 的 Q/L/U/R_traj 加权分解、G 与 p_E、β/ESS/M 分配温度）；单路 landscape 三视图（BehaveSim 距离矩阵经典 MDS：质量视角叠加行为 kNN 边与谱系边、创建时间视角看前沿扩张、c_t–Q 分配散点）；`--task` 模式出三重复搜索曲线 + 汇总结果混合。读取容忍在跑工件（半行 JSONL、behave.npz 原子写）。测试 8 项。
- 实时监控页（`experiments/plotting/monitor.py`，:8765）接入 V9.19：修复 `slot` 列读取（进度原显示为 0），新增每 primary slot 最终分类（修复取最后一次结算）的机制流与两张实时图（四算子滚动混合、每重复 G/p_E 带 Crossover 标记），表格加 Improve% 与算子计数列。页面 JS 以真实 state 全量渲染验证。

## 2026-08-28：V9.19 profiling 失败致命中断修复

- 15 路正式实验中 3 路提前死亡（obp rep1 槽 329、obp rep2 槽 88、vrptw rep2 槽 160）：候选在训练评价上有效但违反 probe 协议（OBP priority 非有限值、VRPTW 于 depot 又选 depot），实现把 profiling 失败处理成 raise。根因是训练评价器与探针的结构性容忍差：OBP 训练的 argmax 静默吸收 inf/nan，VRPTW 训练与 probe 用不同实例分布。
- 修正 `traceaad.py`：profiling 失败按候选失败结算（outcome `profiling_failed`，消耗槽、不建节点、审计记录、checkpoint），run 继续；设计文档补写该容忍规则并同步三处失败类别枚举。新增回归测试，V9.19 套件 37 项全过。
- 三路以 `--resume-from` 在 server3 恢复，跨过死亡点继续出结果。

## 2026-08-28：V9.19 实现精简（按设计文档收缩工程内容）

- 去除文档未要求的工程：preflight 语法预检（与评价器报错重复）、repair 提示的 traceback 帧策展（改为尾部截断）、checkpoint 的决策恢复机器（SimpleNamespace 还原 hack）、无效参数（weights/error_retries/maximize/max_history/profile_timeout/error_handling，均为冻结常量或恒真）、invalid 零重试时的不可达 raise、死状态（_last_best_slot/ancestor_ids/depth/region_members/Landscape.has）。
- `traceaad.py` 重写为三方法主干（`_attempt` 生成→评价→有界修复、`_settle_pending` 全部分类、`_close_slot` 唯一结算落盘），9 个 per-attempt 实例字段并成一个槽内 `_SlotContext`。核心模块 2475 → 2166 行（traceaad 766→631、checkpoint 194→122、prompt 339→250、operators 155→133）。
- 兼容性保持：CSV 列、mechanism_events 格式、summary 字段、checkpoint 键不变；在跑旧 checkpoint（tsp rep1，463 槽）新代码只读加载验证通过。452 项全仓库测试过。已知代价：中断发生在 repair 窗口内再恢复时，该 repair 会被记为一个新 primary slot（旧代码恢复 attempt_kind），窗口极小，接受。
- obp_rep3 完成（1000 槽，best -730.5），held-out 六规模即测完成，无失败样本。

## 2026-08-28：V9.19 轨迹协议改 v4——训练实例轨迹并入评价（用户裁决）

- 用户判定 BehaveSim-v3 panel（4 固定实例 ×2）是离线分析工具的遗产，在线机制的行为轨迹应取自训练实例。实现 `tracked_eval.py`：五个任务的 V9.19 专属评价类，逐位复刻基准求解循环（同实例、同 rng 消耗顺序、同聚合），一次执行同时返回基准 fitness 与训练实例轨迹（构造类记选择序列保留 12 状态，ACO 逐迭代 incumbent 保留 5 状态）。run.py 的 v9_19 分支改用 tracked 评价。
- `behave.py` 删除 panel 探针与子进程 harness（800→290 行），保留 DTW 内核与打包，距离协议改单组训练实例均值，协议标识 `behavesim_v4_train_trajectory`；landscape/checkpoint 改单组轨迹；`profiling_failed` 结果类与 profiling 计数器随独立执行一起消失。设计文档 §3 重写为 v4。
- 位一致测试（5 任务，含 ACO 串行与并行池，tracked vs 基准逐位相等）8 项、机制 28 项、集成 7 项、接线/绘图 69 项全过。v3 checkpoint 被新代码按协议不匹配拒绝；可恢复 v3 的代码备份在 `/tmp/traceaad_v9_19_v3_panel_backup`。
- 在跑的 15 路 v3 协议实验未动，待用户决定停掉重开（v4）或跑完留作对照。

## 2026-08-28：V9.19 输出不含 docstring/注释并重开 15 路

- 生成输出约定：程序只含 import、签名和可执行语句；设计理由留在 `Idea`。目标函数展示去掉模板 docstring；入库用 AST 去掉 docstring 与注释。半程规范根 15 路已停并删除，按 local 3、server1 6、server3 6 重新开搜。

## 2026-08-28：V9.19 现版本 15 路开跑

- 旧机制中断批次迁到 `experiments/其他实验/V9.19-旧机制中断-20260828/`，规范根重新开搜。15 路 fresh：local 3、server1 6、server3 6；`v919_*` tmux + `v919_scheduler --watch`。预算 1000、8 根。

## 2026-08-28：V9.19 实现审查与精简

- 对照[完整机制](../../01-主线版本/TraceAAD-V9.19-机制设计.md)审查后：P/U/T、ν、两动作、BehaveSim-v4 与 D_t 已对齐。修正修复槽对 outcome 的双计；T 在插入后由形成路径计算；精确重复不再二次评价，仍消耗 primary slot。
- 删 `operators.py`，去掉 `_SlotContext`、prompt/candidate 哈希、未用邻域接口与权重参数化。方法目录 2661 → 2387 行。机制、绘图、runner、5 任务集成测试全过。可开正式搜索。

## 2026-08-28：V9.19 实现与完整机制设计对齐

- 按[设计文档](../../01-主线版本/TraceAAD-V9.19-机制设计.md)把在线控制器收成 $P,U,T$：区域前景 $P=(2Q+\mathrm{median}(Q_{\mathrm{neighbor}}))/3$，节点分数 $S=0.75P+0.10U+0.15T$；形成边在插入前计算档案相对新颖度 $\nu$，$v_i=y_i+\frac12(1-y_i)\nu_i$，$T$ 读最近 4 条有效边。生成端只保留 Develop / Explore，$p_E=\mathrm{clip}(0.60-0.60T,0.10,0.60)$。删除四算子、局部响应 $G$、Behavior Crossover。
- 形成来时路提示增加 Behavior 标签；普通槽另存 `decisions.jsonl`（$D_t$）。绘图与 monitor 改为 P/U/T 与两动作。
- 机制、集成、绘图、runner 相关测试全过。

## 2026-08-28：V9.19 搜索观察页

- `:8765` 改为 V9.19 专用观察页：五任务总览、best 改进历程、形成树、BehaveSim MDS 景观、形成轨迹、逐 slot 的 P/U/T 竞争与 Idea/代码。工件增加 `best_history.jsonl`（每次全局最优的程序）和 `checkpoints/view.json`（无代码节点快照）；已在跑的 15 路由现有 csv/checkpoint/npz 重建。
- 入口仍是 `uv run python -m experiments.plotting.monitor`。

## 2026-08-28：V9.19 旧版停止分析

- 旧版 v3 批次停止于 411–1,000 slots，仅 2/15 路完整；归档中每路当前 best 的五任务 held-out 均完成三个重复。训练同期与测试均显示 TSP 正向、其余任务总体退化；预算分配、四算子、行为距离、路径与 profiling 成本见[V9.19 旧版机制诊断](../../02-实验结果/版本机制诊断/V9.19-旧版机制诊断.md)。主实验结果页保持正式 1,000-slot 口径。

## 2026-08-30：500 预算主对照 held-out 重评

- 主表方法集（五对照 + TraceAAD V9.16/V9.17/V9.19/V9.20 全五任务、V9.21 OBP）取每路前 500 次评价内训练集最优样本，held-out 重评完成 46 组 × 3 重复，无失败行；入口 `experiments/analysis/run_budget500_eval.py`，汇总与结果页已移除，仅保留当时的“500预算对照”记录。TraceAAD V9 中途 best 工件未留存，无法重评。
