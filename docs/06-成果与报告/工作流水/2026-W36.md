# 2026-W36

## 2026-08-31：V10 机制实现、评审与生成模型口径统一

- 按 [V10 完整机制设计](../../01-主线版本/生成协议与上下文演进.md) 完成独立实现 `llm4ad/method/traceaad_v10/` 与 runner，通过 32 项机制测试及端到端冒烟。
- 正式实验口径统一切换至 **Qwen3.8-27B** 与官方 thinking 采样配置（temp 1.0 / top_p 0.95 / top_k 20），修复历史采样配置不一致问题，废弃旧批次并重新规范。

## 2026-09-01：server1 接入与预算分配经验沉淀

- server1 部署 Qwen3.8-27B 并联调通过，纳入正式实验支持矩阵；批次扩至 25 路（5 任务 × 5 重复）。
- 梳理总结预算分配价值对象演进历程，沉淀入[预算分配经验](../../04-研究认识与构想/研究主线与问题.md)；确立 V10.1 机制基座（真实质量选父 + 在选中节点上分别生成三个子代并逐个评价，轨迹仅作为生成证据）。

## 2026-09-02：V10.1 机制落地与正式批次运行

- 按 [V10.1 完整机制设计](../../01-主线版本/生成协议与上下文演进.md) 完成 `llm4ad/method/traceaad_v10_1/` 与 `experiments/traceaad_v10_1/` 独立实现；通过三算子流程、断点恢复测试与审查。
- 正式批次 `20260902`（5 任务 × 3 重复、1000 预算）启动并推进，支持集群后端异常断点无损续跑。
- server3 GPU1 服务实例一夜两崩，定位为 vLLM V1 的 MTP 投机解码 × FlashInfer 路径 CUDA 非法访存（已知 bug 类，正常负载下偶发）；全部生成端点（server3 双端口、server1、本地 llama.cpp）统一去除 MTP，采样口径不受影响。无 MTP 并发实测：8 并发聚合 307 tok/s 近线性扩展，单流约为 MTP 配置的 1/3.5。
- 已完成的 8 路（obp/tsp/vrptw/op 各 2 路）按增量口径完成 held-out 测试，结果在 `experiments/traceaad_v10_1/results/<task>/results.json`；结果页等三重复齐后更新。
- 完成[预算流向—算子产出—后续兑现联合分析](../../02-实验结果/各版本实验记录/历史版本-实验结果.md#v10-1)（10 路完成 + 5 路进行中口径）：父节点分配与 ESS 工作正常；瓶颈定位为 Pivot 对 top 父节点的无差别使用（改进率 0–2%）与提案端结构跳变多样性（三组对照赢家起点全部更差、靠早期换构翻盘）。
- 完成[方向族预算流与 Fuse 角色分解](../../02-实验结果/各版本实验记录/历史版本-实验结果.md#v10-1)：node ESS 60–90 的同时 12/15 run 族 ESS ≤2（单族 ≥75% 预算）；4054 个 Pivot 族仅 3.6% 出生后获得过预算；Fuse 高改进率主要是向 elite donor 的传播（2–10% 差距档 58.8% 改进率中 53.9 点是 transfer），128 个 Fuse 新全局最优全部是 synth。方向层的存活与再定向是 V10.2 的对象。
- CVRP rep2 于 815/1000 停死：Fuse 携带的父节点与 donor 两段完整代码均不参与裁剪，该路精英代码膨胀至 29–31k 字符后双双超限（57460 > 57344 chars）；checkpoint 恢复随机数状态使重启确定性复现。不续跑，CVRP 按 n=2 收口。815 截断 best（嵌入 Clarke-Wright 与多轮局部搜索的 30k 程序）held-out 全实例无超时，三规模均优于 rep1 轻量最优且差距随规模扩大（test_50/100/200 领先 1.0/2.2/5.0），结果在 `experiments/traceaad_v10_1/results/cvrp_aco/rep2_stopped_815/results.json`。
- 决定把生成原则改为选择性修改：新增机制须可替换/删除既有机制（Fuse 改选择性融合、supersede 即替换；Refine/Pivot 明确允许减法），Task Contract 声明启发式组件定位；不加行数/字符硬限与复杂度打分。膨胀根因是"生成许可 + fitness 奖励"双引擎（CVRP 三路中仅 rep2 膨胀且训练/held-out 双赢），改动属新版本，不并入 20260902 批。
