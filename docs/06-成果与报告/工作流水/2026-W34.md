# 2026-W34

## 2026-08-17：V9.8 四任务完整 held-out

- 正式批次 `20260815_225000` 的四任务三重复均已完成 `1000 eval`，统一使用 `experiments/evaluate_best.py` 完成 held-out；测试 worker 为 16，OBP 按 6 个尺度、每尺度 5 个实例执行。
- 完整工件写入各任务 `traceaad_v9_8/eval_best_20260817_v98_complete/results.json`。均值（按任务原始指标方向）为：TSP50/100/200 `6.197989/8.526220/11.949251`；CVRP50/100/200 `9.220167/15.748477/28.461341`；OP50/100/200 `15.070311/30.308873/53.801198`；OBP 六档 `413.800/2022.467/4032.533/81.000/402.333/804.067`。
- V9.8 当前批次满足四任务三重复证据完整性，但仍作为独立批次事实，不自动更新既有跨版本综合排名。

## 2026-08-17：V9.9 OBP 三重复 held-out

- 批次 `20260816_154200` 的 Online Bin Packing 三次 1000-eval 搜索完成后统一 held-out（口径与 V9.8 相同：默认 6 档、timeout 1000、无 `max_sample_order`）。测试均值 417.800/2032.267/4052.133/81.400/403.000/804.600，全部高于 V9.8 同期测试（+4.0/+9.8/+19.6/+0.4/+0.7/+0.5 箱）；rep3 为离群（train −730.5），剔除后 10k_100 与 V9.8 持平。单任务不构成结论，V9.9 不进入跨任务排名。
- 工件在 `experiments/online_bin_packing/traceaad_v9_9/eval_best_20260816_v99/results.json`，结果记入 [OBP 主实验汇总](../../02-实验结果/00-主实验结果.md#online-bin-packing)。当时 TSP / OP / CVRP 仍在搜索。

## 2026-08-17：V9.9 TSP / OP 三重复 held-out

- TSP 与 OP 的三次 1000-eval 搜索均已完成，并使用 `experiments/evaluate_best.py` 完成三规模 held-out；每个规模 3/3 成功。TSP 均值为 `5.976288/8.424565/11.954665`（TSP50/100/200），OP 均值为 `15.022635/30.324871/54.696771`（OP50/100/200）。
- TSP 工件为 `experiments/tsp_construct/traceaad_v9_9/eval_best_20260817_v99_complete/results.json`；OP 工件为 `experiments/op_aco/traceaad_v9_9/eval_best_20260817_v99_complete/results.json`。OP 使用 4 个 evaluation worker，结果全部为有限值；运行时的 `exp` overflow 仅为生成程序警告。
- V9.9 当前已有 TSP、OP、OBP 三个任务的完整 held-out，CVRP 三路搜索仍在运行；结果已写入各任务主表，但不更新跨任务排名。

## 2026-08-17：本地 qwen3.8 CVRP 补跑守护

- 批次 `v9_7_qwen38_20260815_010107` 的 CVRP 三路此前因与远程 V9.8/V9.9 抢本机评价核搁置（见 `run_v97_local_qwen38.sh` 注释）。重新评估：32 核机器 16 路搜索已超卖（评价进程约 30 核、load 41），仍不可直接启动。
- 新增守护 `experiments/runners/traceaad/run_v97_local_qwen38_cvrp.sh`（tmux 会话 `v99_cvrp_waiter`）：连续 3 轮满足「评价总 CPU < 23 核 且（op_aco 三路 done 或 评价总 CPU < 15 核）」后，以 `--eval-workers 2` 启动 CVRP 三路（沿用同一批号、seed=rep、可 resume），全部 done 后退出。触发预估在 op_aco 收口前后（约 5–6 小时后）；对应本地守护日志已按历史清理口径移除。

## 2026-08-17：V9.9 过程诊断与探索—利用机制反思

- 对批次 `20260816_154200` 的 12 次运行做了全量过程诊断（分配几何、算子份额、$C_R$ 激活、Explore 子代生存、胜选血统构成），并与 V9.7 批次 `20260814_150927` 做同口径对照。核心发现：三个自适应机制（$\mu$ 形状、$\pi$ 动态范围、$C_R$）在实际运行中全部退化为常数；任务分化在搜索侧而非泛化侧（CVRP/OBP train best 本身更差，TSP/OP 跨规模泛化最强）。
- 结合 A2DEPT（退火—重升温、Boltzmann 捞回、stepping stone）、BaSE（asymmetric proposal mass、按最近改善路由）与轨迹分析（breakthrough rate、novelty×局部化交互）三篇工作，把理解误差定位为三条：探索单位错位（意图份额 vs 族锚定）、利用对象错位（锚点 vs 区域局部化）、体制错位（静态混合 vs 随证据演化）。分析见 过程诊断与机制反思。
- 诊断脚本入库：`experiments/analysis/analyze_v99_process.py`（A–F 六节输出，支持未完成运行的 CVRP 按已有事件统计）。
- 复核修订（同日晚）：C_R 标记的回撤状态上 Refine 父子改善率 48.5% vs 非回撤 5.4%（$n$=3138/5666，improve 口径含向祖先恢复），证明锚点级路径状态可承载时间尺度匹配信号；后半程全局刷新 Explore 4 次 vs Refine 40 次，节律应为停滞重开而非单调收缩；深度改用有效深度（胜选路径 improve 边数：V9.7 CVRP 65–73 vs V9.9 27–37）。P2 由"投资单位升为区域"改写为"单位保持锚点、粗粒度对象降为派生视图"，避免重复 V9.8 把视图升格为信用容器的复杂化。
- V9.10 设计稿评审修订（同日）：初始化 8 次评价显式豁免 $\rho$ 更新；$C_R$ 生态位改为 $Q+D$ 联合条件化的设计论证（前沿路径退步态保持可选、停滞路径退步态下沉）；invalid 计算子失败正当化为期望成功的组成部分；算子信用链由"每锚点 Beta 后验 + 路径平滑"简化为单层路径加权统计 $G_{o,t}$；补轨迹分析（breakthrough 语义）、Stage P（$H_C=5$）、A2DEPT（算子偏好继承）三处正文溯源；删除防御性章节（不引入对象清单、不证明主张清单），全文收紧。

## 2026-08-17：V9.10 机制实现

- 按 V9.10 机制诊断 完成独立实现 `llm4ad/method/traceaad_v9_10/`（schema / forest / selection / prompt / history / artifacts / checkpoint / traceaad），并把 `experiments.runners.traceaad.run` 扩展到 `--version v9_10`；批量启动器 `experiments.runners.traceaad.launch_v910`。
- 唯一在线机制为折扣 Thompson 联合分配：锚点—意图臂先验 $\Beta(2p_o, 2(1-p_o))$；动作在 $H_C{=}3$ 代短窗口内单向结算（成功/失败/pending）；已结算动作按父链窗口 $H_P{=}4$、距离半衰期 $h_P{=}2$、时间半衰期 $h_R{=}20$ 加权进同一 Beta 伪后验；每响应前对全臂采样 $\theta$ 归一化为 $\omega$，无 Top-K。生成侧与 V9.9 完全一致（共享 parent path 8 事件、`Idea+Code` 契约）。
- 日志按设计 §14.1 记录：每次响应所选臂的 $\theta/A/B/\omega$、后验熵与有效锚点数、`action_settled` 事件保留原动作编号与 pending 时长；summary 新增动作结算计数。$B_{\mathrm{resp}}{=}5000$ 触发时标记可恢复协议失败。
- 测试 `tests/test_traceaad_v910_integration.py`（17 项：先验、短窗口结算五分支、父链/时间折扣解析值、链窗截断、pending 不入后验、新臂先验、采样确定性与归一化、端到端冒烟、协议失败、checkpoint 恢复、runner 配置、launcher resume）；全仓 472 项测试通过。尚未启动正式搜索。

## 2026-08-18：Qwen3.8 CVRP 两路 held-out 与重启恢复

- 机器 10:16 重启中断本地 llama-server 与批次 `v9_7_qwen38_20260815_010107` CVRP rep1（961/1000）；rep2/rep3 在重启前已完成（07:56/08:15，train best −8.9197/−8.6717）。按 local 后端标准配置（127.0.0.1:8001、每 slot 32k × 3 slots）重启 llama-server，rep1 自 checkpoint 恢复续跑（tmux `v97q38_cvr_r1`）。
- 对已完成两路统一 held-out（同协议 16 workers）：CVRP50/100/200 均值 9.121405/15.612048/28.340585（n=2），相对 V9.7（Qwen3.6）三档高约 1.1%/3.0%/3.9%。工件 `traceaad_v9_7/eval_best_20260818_qwen38_partial/results.json`，结果记入 [CVRP 主实验汇总](../../02-实验结果/00-主实验结果.md#cvrp-aco) 诊断注；rep1 完成后补测并出正式行。

## 2026-08-18：V9.10 六路重启后恢复

- 批次 `v9_10_20260817_222517`：TSP 与 OBP 各三路已在 02:13–04:39 完成 1000-eval；CVRP（328/329/334）与 OP（661/642/652）六路在 09:47:06 因机器重启前的关机流程同时以 `model tokenizer failed after 3 attempts` 协议错误退出。
- 经 `launch_v910 --batch 20260817_222517 --watch`（tmux `v910_launcher`）统一 resume：CVRP 三路走 server3、OP 三路走 server3b，全部从中断 checkpoint 续跑无回退；完成后的 TSP/OBP 六路被 launcher 正确跳过。运行恢复记录不形成性能结论。

## 2026-08-18：V9.10 TSP / OBP 三重复 held-out 与监控启动

- 对批次 `v9_10_20260817_222517` 已完成的 TSP 与 OBP 各三路统一 held-out（`experiments/evaluate_best.py`，timeout 1000、workers 8）：TSP50/100/200 = 6.159360/8.582870/12.353827；OBP 六档 = 418.600/2046.333/4082.067/80.933/402.867/805.067。TSP 相对 V9.9 三档同向高 1.9%–3.3%；OBP 相对 V9.9/V9.8 在 capacity=100 三档更高、capacity=500 方向不一致。已写入两任务主实验汇总主表；CVRP/OP 完成前 V9.10 不进跨任务排名。
- 工件：`traceaad_v9_10/eval_best_20260818_v910/results.json`（TSP 与 OBP 各自任务目录下）。启动只读运行监控 `experiments.plotting.monitor`（tmux `monitor`，端口 8765）。

## 2026-08-18：Qwen3.8 CVRP 三路完成与正式 held-out

- 批次 `v9_7_qwen38_20260815_010107` rep1 自 checkpoint 恢复后于 15:13 完成 1000-eval（train best −8.1876，自 eval 150 后未再刷新），三路齐。统一 held-out（同协议 16 workers，工件 `traceaad_v9_7/eval_best_20260818_qwen38_complete/results.json`）：CVRP50/100/200 均值 8.872333 ± 0.464111 / 14.922395 ± 1.210404 / 26.722866 ± 2.802006，相对 V9.7（Qwen3.6）三档低约 1.6%/1.6%/2.0%。
- 优势全部来自 rep1：train best 三路最好（−8.1876，最大化口径最高分，eval 150 后未刷新），测试三档也最强（CVRP200 23.49，低出 Qwen3.6 均值约 13.9%）；rep2 的 train（−8.9197）与测试均为三路最差，高于 Qwen3.6 均值三档；rep3 仅 CVRP50 略低。三路 train–test 排序一致，路间方差为 Qwen3.6 批次 3–6 倍。两重复诊断注已替换为正式注，主表新增生成模型对照行（不参与加粗与跨任务排名），见 [CVRP 主实验汇总](../../02-实验结果/00-主实验结果.md#cvrp-aco)。

## 2026-08-18：docs/01-主线版本 扁平化与运行工件精简

- 四任务结果页合并为 `docs/02-实验结果/00-主实验结果.md`，搜索曲线图集中到 `figures/`（以 task 命名），任务子目录删除；`plot_search_curves.py` 输出路径同步修改并冒烟验证，全部绘图脚本改为只输出 png。
- 按旧批次标准精简已完成搜索与 held-out 的运行工件（删除 `logs/events.jsonl`，保留 evaluations.csv、best_program/best_curve、checkpoints、llm_calls、summary）：V9.8 全部四任务、V9.9 的 TSP/OP/OBP、V9.10 的 TSP/OBP，共 27 个文件、释放约 3.7 GB。
- 确认 V9.9 CVRP 三路已于 08-17 15:08–17:01 完成 1000-eval（此前文档记"仍在搜索"已过时），V9.9 四任务搜索全齐、待统一 held-out；V9.10 OP 三路当日 15:48–15:52 完成搜索、同样待 held-out（CVRP 三路仍在搜索，约 586–593）。两者的 events.jsonl 暂保留，held-out 完成后再精简。

## 2026-08-18：V9.9 CVRP 与 V9.10 OP held-out、V9.9 闭批

- V9.9 CVRP 三路统一 held-out（同协议 16 workers，工件 `traceaad_v9_9/eval_best_20260818_v99_complete/results.json`）：9.362723 ± 0.328244 / 16.143917 ± 0.710780 / 29.033141 ± 1.076603，相对 V9.8 三档高 1.5%/2.5%/2.0%，rep2 端到端最弱。V9.9 四任务齐、闭批：CVRP 行写入实验结果，`recompute_rankings.py` 补 V9.9 工件后重算——单独排名 3.400（第 4/6，介于 V9.6 与 V6 之间），代表性同场维持 V8/V9/V9.7/V9.8 不变。
- V9.10 OP 三路 held-out（工件 `traceaad_v9_10/eval_best_20260818_v910/results.json`）：OP50/100 = 14.680024 ± 0.262076 / 28.950195 ± 1.549586，相对 V9.9 低 2.3%/4.5%；**rep3 的 best 程序在 OP200 产生非有限启发式矩阵（exp 溢出，训练集 OP50 不触发）**，按无效处理，OP200 以 n=2 记 47.611953 ± 0.937469（比 V9.9 低约 13%）。`evaluate_best.py` 增加单程序失败容错（记 `eval_failed`、不进统计、整批继续）。
- 实验总汇新增 §2.3 V9.10 状态；V9.10 CVRP 三路仍在搜索。精简 V9.9 CVRP 与 V9.10 OP 的 events.jsonl（6 个文件、1.25 GB）。

## 2026-08-18：V9.10 结果与机制失效诊断

- 基于 V9.10 批次 `v9_10_20260817_222517` 的 held-out 与终局 checkpoint 完成学术机制诊断，记录于 V9.10 机制诊断。TSP、OP、OBP 没有显示相对 V9/V9.7 的正向净收益；过程上约 36%–55% 动作仍为 pending，约半数锚点从未重访，top-20 联合臂质量约 2%，分配接近均匀。
- 新增 V9.10 过程诊断脚本 `analyze_v910_failure.py`，从 checkpoint 重放 pending 年龄、结算深度、锚点熵和联合臂质量；没有修改已完成运行工件。
- 原始 V9.10 CVRP 恢复中 rep2 已完成 1000 eval；rep1/rep3 继续恢复。
- 原始 V9.10 CVRP 三路搜索于当日晚完成 1000 eval；统一 held-out 写入 `experiments/cvrp_aco/traceaad_v9_10/eval_best_20260818_v910_cvrp_complete/results.json`：CVRP50/100/200 = 9.737690 ± 0.145253 / 16.855841 ± 0.553005 / 30.482182 ± 1.493201。V9.10 四任务结果闭合并同步至实验结果页；OP200 仍按 n=2 报告。

## 2026-08-19：V9.11 批次、OBP held-out 与生成服务迁移

- 正式批次 `v9_11_20260819_022200`（V9.11 停滞触发 explore + 一次性 landing）四任务三重复于 02:28 启动。OBP 三路当日 06:55–09:53 完成 1000 eval。
- OBP 三路统一 held-out（工件 `experiments/online_bin_packing/traceaad_v9_11/eval_best_20260819_1129/results.json`）：六档 417.600 ± 0.917 / 2029.800 ± 3.341 / 4043.800 ± 9.007 / 80.867 ± 0.115 / 402.867 ± 0.924 / 805.200 ± 1.929。OBP 行写入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。
- 生成容量调整：server3 上剩余预算最大的三路（op r1、tsp r1/r2）自 checkpoint 迁至本地 llama.cpp 服务源续跑（eval 编号连续），server3 释放 3 slot。`_common.py` 的 local backend 模型名由 Qwen3.8-27B 修正为 Qwen3.6-27B（本地 8001 已换回该模型；resume 校验按此名比对）。

## 2026-08-19：结果页收编、归档与合并

- [实验结果](../../02-实验结果/00-主实验结果.md)四张主表收编为 6 个 TraceAAD 版本（V4、V5、V8、V9、正统 V9.7、V9.9）加 5 个外部对照；行序为先 5 个外部对照再按版本顺序放 TraceAAD，加粗按保留行重算。
- V6、V7、V8.2、V8.3、V9.1×2、V9.2–V9.4、V9.6、V9.7-batch、V9.8、V9.10、V9.11(OBP) 移入新建的[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)，数值原样保留。
- v4flash 对比页改名重组为“生成模型对比”（该页已移除）：v4flash 批次为第 1 节，Qwen3.8 批次（V9.7 协议 × 4 任务 × 3 次）为第 2 节，与正统 V9.7 批次做同协议两模型对照。
- 实验总汇与实验覆盖两页删除：实验总汇并入实验结果（保留四张主表与搜索曲线，跨任务排名、批次状态段与结论随合并移除，数字由 git 历史与本日志承担）；全库对两页的引用重定向至[实验结果](../../02-实验结果/00-主实验结果.md)或[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)，[实验配置](../../02-实验结果/00-主实验配置与冻结契约.md)尾句与 AGENTS.md 文档表同步。
- 搜索曲线（`plot_search_curves.py`）按新口径重绘：方法集从 8（V9、V9.6、V9.7 + 5 外部对照）改为 7（V9、V9.7 + 5 外部对照），V9.6 条目从脚本删除，四图重新生成写入 `figures/`。
- V1–V9.6 机制诊断从 experiments 移入 analysis；V9.8 机制识别实验页删除，锚点来源批次、$2\times2$ 配对设计与探针/批次入口并入 V9.8 机制诊断 §2.1 与 §9，全库 8 处链接重定向。experiments 目录收敛为实验结果、归档实验结果、生成模型对比、配置四份文件加 figures。

## 2026-08-19：监控界面重设计与版本归档分组

- `experiments/plotting/monitor` 前端整体重做：sticky 顶栏（live 指示灯、全局统计）、活跃版本标签与"已归档版本"折叠分组——全部完成或全部停滞的版本自动归档（V9.1–V9.11、qwen38 与 V9.5 旧批次入档），默认视图只留在跑批次；每版本统计条（完成运行/平均进度/活跃速率合计/整批预计），任务卡片化（中文任务名、rep 徽标、进度条、best-so-far 曲线加整洁刻度与悬停提示）。随后按反馈放大曲线（任务卡单列全宽、图高 320）并在曲线上加突破点标记：每次刷新全局最好处画空心圆，悬停显示 eval 与数值，图例统计各 rep 突破次数（V9.7 TSP rep1 的 11 次突破与机制诊断记录的 final best 位置一致）。
- V9.7 机制诊断从 git 恢复并重放 batch 清理（此前从工作区消失，有 7 个文档链接指向它）；若确需整文件删除另行处理其链接与内容归属。

## 2026-08-19：V9.7-batch 下架与 V9.7 定名

- 删除 V9.7-batch 批次 `20260813_184519`（V9.6 历史上下文的中间配置，12 个 run 目录约 0.4 GB）；正式版统一称为 V9.7（批次 `20260814_150927`），不再使用"正统"前缀。
- 全库清除 V9.7-batch 的结果与统计：[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)四行、[研究认识](../../04-研究认识与构想/2026-09-18-核心研究认识.md) §1.2/1.3 的对照数字、V9.7 机制诊断整节路线诊断与各处对照、L3 的对照句、组会报告 §5.3、[实验配置](../../02-实验结果/00-主实验配置与冻结契约.md)与历史“生成模型对比”页的"正统"字样（git 历史保留原文）。OBP capacity=500 大档的列最佳归属改为 V9.6。
- 监控面板标签同步：V9.7 目录内唯一日期批次不加后缀（正式版即 `V9.7`），qwen38 与多日期批次（V9.5）保留后缀。

- 监控面板按反馈收敛显示集：V9.1–V9.6（含 V9.5 两个日期批次）从面板移除（服务端 `HIDDEN_VERSIONS` 过滤，轮询 run 数 155→84），这些版本的正式数字只在[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)；面板保留在跑的 V9.12 与近期批次 V9.7/V9.7·qwen38/V9.8–V9.11。

## 2026-08-19：V9.11 终局分析与认识回写

- 用完整 held-out 数据重算单版本上场排名：V9.11 = 3.100（6 方法第 2），一代梯度 V9.8 2.667 > V9.11 3.100 > V9.9 3.400 > V9.10 4.033（口径与旧表一致，旧表数字逐一校验吻合）。头对头对 V9.10 好 12/15、V9.9 好 10/15、V9.8 平手（7:8）、正统 V9.7 好 5/15；TSP50 均值 5.918 为全部 24 行独占最佳。
- V9.11 机制诊断新增第 7 节终局修正：CVRP 中期排序完全反转（终局三档低于 V9.9 2.7%–3.8%、V9.10 6.5%–8.4%），OP 搜索值与 held-out 倒挂（终局三档高于正统 V9.7 0.6%–1.9%）；整体定位改为“V9.9/V9.10 退化后的有效恢复，与 V9.8 平手”。
- [研究认识](../../04-研究认识与构想/2026-09-18-核心研究认识.md)新增 §1.7（V9.11 止跌但未统一升级）与 §3 快照纪律（中期过程排序不预测终局 held-out 排序，附两个反转实例）。

## 2026-08-19：V9.11 TSP、OP 完成与四任务闭合

- TSP 三路完成搜索并统一 held-out（timeout 1000、16 workers，工件 `traceaad_v9_11/eval_best_20260819_v911_tsp_complete/results.json`）：TSP50/100/200 = 5.918362 ± 0.025098 / 8.462339 ± 0.145793 / 12.377767 ± 0.488868。rep1 的 best 程序在 tsp200 首评 1000 秒超时，3000 秒单独复测得 12.903243（754.7 秒，sanity 与训练分一致）计入 n=3。相对 V9.10：50/100 档低 3.9%/1.4%、200 档高 0.2%；相对正统 V9.7：50 档低 1.2%、100/200 档高 0.9%/2.9%。
- OP 三路统一 held-out（16 workers，工件 `traceaad_v9_11/eval_best_20260819_v911_op_complete/results.json`）：OP50/100/200 = 15.101646 ± 0.078705 / 30.081826 ± 0.240843 / 53.419896 ± 1.111003。相对 V9.10 三档高 2.9%/3.9%/12.2%（方向一致），相对正统 V9.7 三档高 0.7%/0.6%/1.9%（方向一致，越高越好）；相对 V9.9 的 OP100/200 低 0.8%/2.3%。
- V9.11 四任务搜索与 held-out 全部完成，四行已写入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。

## 2026-08-19：V9.11 CVRP 三路完成与 held-out

- V9.11 CVRP 三路完成搜索，统一 held-out（`experiments/evaluate_best.py`，16 workers，工件 `traceaad_v9_11/eval_best_20260819_v911_cvrp_complete/results.json`）：CVRP50/100/200 = 9.110544 ± 0.318409 / 15.488990 ± 0.616214 / 27.920116 ± 0.898755。相对 V9.10 三档低 6.5%/8.1%/8.4%，相对 V9.9 低 2.7%/4.1%/3.8%，相对 V9.8 低 1.2%–1.9%（方向一致）；相对正统 V9.7 三档高 1.0%/2.2%/2.4%（方向一致），与 V9 互有高低（50 档 +0.3%、100/200 档 −0.06%/−0.7%）。CVRP 行写入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。

## 2026-08-19：V9.12 批次启动与运行监控

- 正式批次 `v9_12_20260819_130358`（V9.12 进展概率 Refine/Explore）四任务三重复于 13:04 启动（调度器会话 `v912_scheduler`）。
- 运行监控面板（`experiments.plotting.monitor`）在 tmux 会话 `traceaad_monitor` 启动（端口 8765）：68 个 run 被识别（V9.7/9.8/9.9/9.10/9.11/9.12），V9.11 剩 5 路在收尾，V9.12 十二路全部推进、无停滞。
- 监控增加老工件回退：无 `evaluations.csv` 的旧批次改读 `artifacts/candidates.jsonl`（预算轴 = `evaluator_called` 行数，与 summary 的 `evaluator_call_count` 对齐）；同版本目录多批次时标签页加批次后缀（`V9.7·0813/0814/qwen38`、`V9.5·0811/0812`）。V9.1–V9.6 与正统 V9.7 全部上面板。
- 新增TraceAAD V9.12 机制诊断，用 checkpoint 重放区分局部进展概率的机制运行、实际选择改写、Explore 后续恢复和 matched-eval 搜索表现；分析脚本可随其余运行完成刷新。

## 2026-08-19：V9.7 机制区域重访门槛分析

- 兑现 V9.8 诊断 §9.5 承诺的离线重访报告：对 V9.7 正式批次 `20260814_150927` 四任务 12 run 按程序创建顺序重放静态机制代理区域。脚本 `experiments/analysis/analyze_v97_region_revisit.py`（复用 search geometry 的标签/宏簇定义），输出 `docs/01-主线版本/V9.7/机制诊断/traceaad_v97_region_revisit/summary.json`，分析见V9.7 机制诊断。
- 核心数字：每 run 宏簇 4–6、标签组合 10–48，新宏簇进入全部发生在前 125 次评价（中位 4–73）；Explore 换簇 97.9%–99.8% 落回已访问宏簇、首位目的地占 42%–68%；Explore child 出生即达家族前沿仅 5.2%–7.4%（Refine 8.7%–24.2%）、低于家族前沿 $1s$ 以上占 33.6%–81.9%；无新标签的 Explore child 占总预算 23.4%–27.8%；off-route 独有宏簇 12 run 仅 3 例。
- 门槛结论：全局生成信息的杠杆在"已访问区域前沿认知"，跨路线代码迁移的静态杠杆近零；L3 跨路线知识节已同步收窄。

## 2026-08-19：区域前沿固定锚点探针（第二步）启动

- 设计并实现单步识别实验 `experiments/runners/traceaad/v97_frontier_probe.py`（协议 `traceaad-v9.7-region-frontier-probe-v1`）：锚点取自 V9.7 正式批次、限创建时点 ≥ 200（宏簇词表饱和之后），冻结锚点创建时刻的全局状态（已访问宏簇、各簇前沿 fitness、跨簇最优程序）；五条件 `pp_refine/pp_explore/fp_refine/fp_explore/fc_explore`，fp = 来时路 + 前沿表，fc = 再加跨区域最优程序，块内共享采样 seed，V9.7 原生 prompt。主对比：`fp−pp`（按意图）与 `fc−fp`；机制指标为亚前沿重入率、落点分布、有效率与修改幅度。
- 全量设计 72 锚点（四任务 × 质量三分层 × 6）× 5 条件 × 3 重复 = 1080 trials，目录 `experiments/generation_probe/20260819_v97_region_frontier`；冒烟（CVRP 3 锚点 × 5 条件）15/15 完成、有效率 12/15、pp≈3.3k / fp≈3.5k tokens。三个 shard 已在 tmux（`v97fp_s0/s1/s2`，local 后端，可恢复）启动；对应本地 shard 日志已按历史清理口径移除。
- 配对分析脚本入库：`experiments/analysis/analyze_v97_frontier_probe.py`（锚点级聚合 + bootstrap 95% CI + 落点/亚前沿分类），已在冒烟数据上机械验证；探针完成后出正式分析。

## 2026-08-19：V9.13 机制实现（方法包 + Stage P + Stage A）

- 按 V9.13 机制诊断 完成独立实现 `llm4ad/method/traceaad_v9_13/`（schema / forest / regions / prompt / checkpoint / artifacts / traceaad），`experiments.runners.traceaad.run` 扩展 `--version v9_13 --treatment pp|fp|fc`。V9.7 主干逐位保留：意图日程复用 V9.7 的哈希映射（treatment pp 与 V9.7 行为等价、分叉分支共享同一意图随机数），Refine 提示在不加全局事实区时与 V9.7 字节级一致。唯一机制改动：`n_eval ≥ 200` 后 Explore 按冻结处理追加 `[Searched Proxy Regions]` 前沿表（fp）或再加异区域前沿程序（fc，无替代区域时记 `inactive_no_alternative` 回退 pp）。区域视图不序列化，恢复时从 forest 按程序 id（=真实评价次序）重建；`mechanism_tags`/`macro_family` 从 search geometry 分析冻结复制（`v97-search-geometry-20260814-frozen`）。
- Stage P runner `experiments/runners/traceaad/v913_stage_p.py`（协议 `traceaad-v9.13-stage-p-decision-snapshot-v1`）：从 V9.7 批次 `20260814_150927` 的 candidates+decisions 工件重放真实 Explore 决策（evaluator 调用数按 `evaluator_called` 前缀和重建，b_t=决策前完成评价数）；快照含决策前森林、实际展示的 parent path、区域前沿（e(p)≤b_t）与 V9.7 选择分数。抽样：3 来源运行 × 3 评价区间（[200,466]/[467,733]/[734,999]）× 2（锚点质量半区）= 18/任务、四任务 72 快照 × 5 条件 × 3 重复 = 1080 trials；条件顺序按任务×运行×区间分组平衡、组内分片；`prepare-probe` 内置审计（pp 等于无全局事实区构建、五条件仅全局事实区不同、fc 参考必为异区域、被选快照的森林重放选择与真实决策逐个比对；pp 与 V9.7 的字节级一致由测试套件断言）。结果变量含 next_selection_rate（按 V9.7 规则写回快照后新 child 是否成为下一次选择）、archive_duplicate、reference_copy、sub_frontier、frontier_gap 等全套 §7.3 指标。分析脚本 `experiments/analysis/analyze_v913_stage_p.py`：快照级配对 + 来源运行→快照层级 bootstrap + §8 冻结候选选择规则评估，已在合成结果上机械验证。V9.13 的全部实现（方法包、Stage P/A 入口、分析）不 import 任何其它版本的方法代码；V9.7 仅作为数据来源（快照抽取）与测试中的对照基线出现。
- Stage A 启动器 `experiments/runners/traceaad/launch_v913.py`：每任务×重复先跑 `--treatment pp --budget 200` 共同前缀（V9.7 行为、单一 checkpoint 装载器），完成后 `fork` 复制为 control(pp)/treatment(fp|fc) 分支并改写 checkpoint config（budget 1000 与分支 treatment，森林与计数不动），`audit-fork` 校验两分支恢复态与前缀逐字段一致；`launch --watch` 持续填前缀、分叉与分支。
- 真实数据验证：决策配对与森林裁剪重放 231/231 复现真实路线+锚点选择；四任务 prepare-probe 全绿（每单元合格快照 70+，远超所需 2）；调度 1080 trials 均衡、块内五条件共享 seed。测试 `tests/test_traceaad_v913_integration.py` 20 项（意图日程等价、prompt 字节级、区域视图与 tie-break、冻结规则与分析定义逐语料一致、激活边界与 fc 回退、checkpoint 重建、runner 配置、trim/调度/next-selection/fork），全仓 519 项测试通过。尚未启动任何正式生成或搜索。

## 2026-08-19：V9.13 代理标签盲评审计与 Stage P 启动

- 完成 §4.2 前置审计：`audit_v913_proxy.py` (历史脚本)) 按来源运行 × 区域分层每任务盲抽 24 个程序（剜除 fitness/意图/终局），结论见V9.13 机制诊断。主要误差：TSP 字面 `two_opt`/`2-opt` 拼写不触发 `two_opt` 标签，全量 25.1%（599/2382）程序属此类（completion_rollout 低估、local_score 高估）；TSP `lookahead` 通用命名漏标；OBP `np.where` 弱触发偶发。未发现语义相反的误标。规则按设计保持冻结，误差作为解释边界记录。
- 正式 Stage P 启动：prepare 生成 72 快照 / 1080 trials（目录 `experiments/generation_probe/20260819_232935_v913_stage_p`，seed 913101），shard 0 冒烟 3/3 valid、结果字段齐全后，11 个 shard 铺满 server3×6 + server3b×5 全部空位（tmux `v913sp_s0..s10`）。预计约 2 小时出全量；已设 20 分钟巡检自动接续：全齐后跑分析、按 §8 冻结处理并启动 Stage A（或记录停止）。

## 2026-08-20：V9.13 Stage P 完成并按 §8 停止

- Stage P 全量完成:1080/1080 trials(11 shard,server3×6 + server3b×5,约 1 小时 40 分),valid 976(90.4%),探针目录 `experiments/generation_probe/20260819_232935_v913_stage_p`,分析 `analysis/summary.json`。
- §8 冻结规则判定 **selected_treatment = none**:FP 六项门槛过五项,唯一失败是 CVRP 的 valid_rate −7.4pp(>5pp);FC 另有 OP 的 archive_duplicate/reference_copy +5.6pp(>5pp),且 FC−FP 质量增量仅 2/4 任务为正。FP 的正向证据:conditional delta_q/s 四任务均值全部提高(+0.65 至 +1.47s)、亚前沿重入四任务全部下降、重复率零上升、next_selection 3/4 改善;fp_refine−pp_refine 四任务全负(信息作用依赖 operator)。
- 按 §8 规则 4,V9.13 停止在机制识别阶段,Stage A 不启动。现象与结论见 V9.13 机制诊断;巡检自动化已撤。

## 2026-08-20：区域前沿固定锚点探针完成（独立第二估计）

- `20260819_v97_region_frontier` 探针 1080/1080 完成（3 local shard，valid 1007/93.2%），分析见V9.13 机制诊断。主结果：fc−fp 四任务配对 Δq 全正（CVRP +3.941 [+1.671, +6.323]、OP +0.607 [+0.187, +1.018]），亚前沿重入同向下降 19–29pp；fp 表单独对 Explore 混合、对 Refine 无稳定作用；所有臂新宏簇进入率 0。
- 事后参考复制审计：fc child 与参考程序变化比 <0.05 的近复制 OP 11.8%、CVRP 5.9%。剔除后 fc−fp 仍成立（CVRP +3.849 [1.643, 6.267]、OP +0.433 [0.030, 0.823]）。分层显示 OP/TSP 增益集中于低质量层（headroom 依赖），CVRP 全层为正。
- 与同夜完成的 V9.13 Stage P（真实决策快照）交叉核对：方向性结论一致（Explore 受益、Refine 不受益、OP 复制最重、CVRP 有效率同向下降）；矛盾点是 CVRP 的 FC−FP 增量（本探针 +3.77s 对 Stage P −0.28s），快照协议差异（决策时刻 vs 创建时刻、锚点人群、冻结标签版本）未解决，两组结论均按各自口径保留，已写入 L3。

## 2026-08-20：V9.12 四任务 held-out 与闭批

- 批次 `v9_12_20260819_130358` 四任务三重复全部完成 1000 eval，统一 held-out（`experiments/evaluate_best.py`，timeout 1000、workers 16，OBP 六档、无 `max_sample_order`），工件在各任务 `traceaad_v9_12/eval_best_20260820_v912_complete/results.json`。TSP/OBP train sanity 与训练分逐位一致，45/45 规模×重复全部有效。
- 均值：TSP50/100/200 = 5.841171 ± 0.055826 / 8.239322 ± 0.255400 / 11.933763 ± 0.157727；CVRP50/100/200 = 9.286476 ± 0.052247 / 15.760954 ± 0.442275 / 28.314204 ± 1.233234；OP50/100/200 = 14.953837 ± 0.222796 / 29.738431 ± 0.911451 / 52.401146 ± 2.478727；OBP 六档 = 415.267 ± 4.300 / 2027.467 ± 10.100 / 4044.000 ± 19.780 / 80.867 ± 0.115 / 402.333 ± 0.611 / 804.267 ± 1.222。
- 相对 V9.11：TSP 三档低 1.3%–3.6%、OBP 四档更低（10k_100 高 0.2 箱、1k_500 持平），CVRP 三档高 1.4%–1.9%、OP 三档低 1.0%–1.9%（方向一致）。相对 V9.7：TSP 三档低 0.8%–2.5%，CVRP 三档高 2.9%–4.0%，OP 三档低 0.1%–0.6%，OBP 六档 4 高 2 低。TSP50 5.841 与 TSP100 8.239 均低于主表与归档全部行。
- `recompute_rankings.py` 补 V9.12 工件后重算：单版本上场 2.833（6 方法第 2，次于 MCTS-AHD），15 规模中 5 个第一（TSP 三档 + OBP 5k_500/10k_500）、CVRP 三档均第 3、OP 为 5/6/6。四行写入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。

## 2026-08-20：V9.13 r2 修订与 Stage P 第二轮(判停)

- 依第一轮证据修订 V9.13(r2):前沿表改为地板表——本区域完整展示(标签+质量)置顶,其它区域只给质量水平不带机制标签;删除 FC 候选(参考复制证据);四条件 Stage P(864 trials,seed 913201)。实现、测试(全仓 517 项)、审计(其它区域行严格格式校验,修正了标签/家族同名导致的误报)同步修订。
- 第二轮 Stage P 完成于 18 个 shard(server3×9 + server3b×9,约 1 小时):864/864,valid 802(92.8%)。§8 判定仍为 none,但失败项换了:**有效率修复**(最大降幅 3.7pp,门槛内;v1 的 CVRP −7.4pp 消失),失败在均值口径的单步质量——仅 2/4 任务提高(TSP +2.50、OP +0.31;CVRP −0.52、OBP −3.06),max drop 3.06 超 0.5。
- 诊断:OBP −3.06 由重尾离群驱动(单点 −109.6s 贡献约 2.2s 均值差;中位数 pp −0.40 vs fp −0.35);CVRP 中位数 fp 略优(−4.10 vs −4.50)。结论:v1 的跨区域机制标签同时承载质量收益与有效率风险,r2 的内容拆分把两者一起削掉,且 r2 捆绑修改了措辞/排序/标签三个变量。
- r3 方向(解混):保留全部区域的机制标签 + r2 的地板措辞与本区域置顶。在新一轮数据生成前冻结同样六项门槛。

## 2026-08-20：V9.12 机制诊断按终局数据修订

- 用全部 12 路完成工件刷新 `analyze_v912_progress.py` 快照（`docs/01-主线版本/V9.12/机制诊断/traceaad_v912_progress/summary.json`），V9.12 机制诊断按终局口径修订：§7 中期 matched-eval 表（642/706 时点）替换为 1000 eval 终局表 + held-out 结果；§1/§3/§9 的"held-out 尚未产生/仍待完整结果"改为任务分化结论（TSP/OBP 兑现、CVRP/OP 不足，整体 2.833 未追回 V9.7）。
- 完整数据修正一处定性判断：CVRP 的 $p_E$ 后段（21.0%）低于中段（21.7%），与其最高 Refine 改善率一致，"四任务均随过程上升"弱化为"前段最低 + 任务分化"；各过程统计数值随完整运行微调（结论不变，如 Explore 即时改善 1.2%–3.5%、后续超父 2.3%–12.1%）。
- §4.5 快照纪律补第三实例：V9.12 TSP 中期对 V9.11/V9.7 均 1 好/2 差、均值最低，终局搜索均值反超且测试三档居首（TSP50/100 全行最佳）；TSP 收益主要来自泛化侧（train 差距 +0.4% 对测试差距 −2.5%），来源识别列入 §10。

## 2026-08-20：V9.13 r3 与三轮识别终止

- r3(全区域标签 + 地板框架 + 本区域置顶)Stage P 完成:18 shard 全算力,864/864,valid 805(93.2%),目录 `experiments/generation_probe/20260820_114557_v913_stage_p`。§8 判定仍为 none:质量 3/4 任务提高(TSP +0.25、OBP +2.47、OP +0.64),但 CVRP −0.93 超 0.5、TSP valid −5.6pp 超 5pp,两项压线失败;next_selection 3/4、亚前沿 3/4、重复 ≤1.9pp 通过。
- 修订设计里预注册的终止条件生效:**V9.13 以三轮识别证据终止**(r1 全表 CVRP valid −7.4pp;r2 去标签 valid 修好但均值质量 2/4;r3 恢复标签后失败项换位)。跨轮可复现:next_selection 三轮 3/4 改善、亚前沿重入三轮大部下降、重复无上升;不可复现:CVRP 的均值质量与 valid 符号逐轮翻转(快照集抽样方差与效应同量级,r1↔r3 处理仅差措辞而 CVRP 均值差 2.2s)。
- V9.13 机制诊断重写为三轮完整记录;L3 同步更新。不启动 Stage A、无 held-out。后续重启条件:识别分辨力提到效应量之上(每任务快照 ×3 或稳健统计主终点)。

## 2026-08-20：来时路完整搜索消融设计与启动

- 确认无重复:轨迹存在性消融只有单步层(固定锚点三轮含 `code_only` 臂、V9.8 Stage P History×Intent),完整搜索层从未做过(V9.5/V9.6↔V9.7 为联合差异)。协议见[父代来时路完整搜索消融](../../03-机制验证/01-提示与上下文/2026-08-21-父代来时路搜索/README.md):主张 = V9.7 协议下 code-only → parent-path 提高 1000-eval search best 且四任务方向一致;反假设(单步收益不累积/修改收缩损害多样性)同等可证;验证性实验、无门槛终止、按三重复实际均值差报告。
- 实现:独立方法包 `llm4ad/method/traceaad_v9_7_co/`(V9.7 逐位复制、仅生成提示去历史区,意图日程复用 V9.7 映射保证配对),runner 注册 `--version v9_7_co`,启动器 `experiments/runners/traceaad/launch_pp_ablation.py`(4 任务 × 3 seed × pp/co 两臂 = 24 个 1000-eval 运行,watch 补位)。测试 `tests/test_traceaad_v97co_ablation.py` 5 项(提示 = V9.7 去历史块、根提示字节级一致、意图日程等价、smoke、runner),全仓 526 项通过。
- 附:V9.13 分析文档补第四个独立样本(pilot 1080/1080 完成:CVRP 全表 valid −11.1pp 与 r1 同向,OP/TSP 质量四样本全正)。

## 2026-08-20：server1 服务源恢复与吞吐实测

- server1（`222.201.145.8:8080`，vLLM qwen3.6-27b-awq）恢复可用并扩容，`_common.py` 与 AGENTS.md 并行容量记为 9（与 server3/server3b 等容量）；正式调度 `PRIMARY_BACKENDS` 未变，纳入分配时另行加入。
- 8 并发长请求实测（2589 token 提示 + 1024 token 生成，先热身再单轮）：全部成功，聚合生成吞吐 213.1 tok/s、单流 26.6 tok/s。6/8/9 并发同口径对照：聚合吞吐 174.2 → 213.1 → 217.7 tok/s（8→9 饱和，仅 +2%），单流 29.1 → 26.6 → 24.2 tok/s 随并发单调下降；超容量 1 个并发无失败与排队惩罚。

## 2026-08-20：V9.14 单根树实现收口与机制规范

- 完成 `llm4ad/method/traceaad_v9_14/` 单根 Algorithm Tree 实现，`experiments.runners.traceaad.run` 接入 `--version v9_14`。主循环按根生成、算法选择、轨迹条件生成、真实评价和立即更新运行。
- 初始化只生成 8 个根节点。预算分配在所有算法节点上使用 `q + 1/sqrt(count+1)`；`count` 在该算法真实获得一次 evaluator 调用后增加，评价失败也计入。
- 生成提示词要求一句 500 字符以内的 Idea 和完整 Python 程序。程序文本按代码围栏、`Code:` 后文本、完整响应的顺序提取，每个模型响应都进入 evaluator 并消耗一次评价预算。V9.14 只使用 evaluator 调用次数作为运行时钟。每条历史仅向模型展示 Idea 和合并后的 `Result: outcome (Fitness: parent -> child)`。checkpoint 仅保存树、待评价响应和 evaluator 调用次数。
- 完成 [V9.14 规范](../../01-主线版本/TraceAAD-V9.14-机制设计.md)与核心实现收缩，节点不再保存可由基础事实计算的派生状态；后续完善预算分配的研究任务记入 V9.14 规范。尚未启动正式实验。

## 2026-08-21：父代来时路完整搜索消融完成

- 批次 `ppab_20260820_131000` 全部 24 个 1000-eval run（4 任务 × 3 seed × PP/CO 两臂）完成并逐一完成 held-out 评估，结果与四任务汇总见[父代来时路完整搜索消融](../../03-机制验证/01-提示与上下文/2026-08-21-父代来时路搜索/README.md)。方向分裂：OP 偏 PP、TSP 偏 CO、CVRP/OBP 在噪声量级内；不支持来时路跨任务一致的完整搜索净贡献。
- held-out 口径事实：所有 run 恰好 1000 次评价收尾，最优候选必在预算内，不设 `--max-sample-order`（该参数按生成序截断，会把无效响应之后的有效候选误判为超预算）。
- 运行事故与处置：应要求迁移 server3 负载到 server1 时，6 个 PP run 被杀后因当晚代码重构（`search_configuration()` 删除 `protocol_id`/`checkpoint_schema_version`）无法用工作区代码续跑；以 `git worktree` 挂 60e2bcc 快照在 `/tmp/llm4ad_pre_refactor` 续跑完成，主仓库代码未被改动。重构后代码无法 resume 重构前断点的问题仍待处理。

## 2026-08-21：来时路消融判读回写与文档整理

- 消融结果按任务画像判读并回写：净贡献与“锁定区域是否低于可达上限”对齐（OP 全向为正、OBP 两臂终点重合、CVRP 仅最大规模偏 PP、TSP 偏 CO 由单 seed 拖动）；存档保护下“防改废”收益的两条兑现路径（可达终点 / 收敛速度）记为待验证，检验入口 = ppab 24 run 中途预算 best-so-far 曲线 + 短预算对照。回写位置：V9.7 机制诊断 新增 §6.4（§1.1/§4/§6.1/§14.1 同步）、[研究认识](../../04-研究认识与构想/2026-09-18-核心研究认识.md) §1.1。
- [父代来时路完整搜索消融](../../03-机制验证/01-提示与上下文/2026-08-21-父代来时路搜索/README.md)精简：删除“与已有消融的关系”“边界”两个防御性章节，统计口径并入设计节，机制解释移入 V9.7 机制诊断。

## 2026-08-21：运行工件清理（无复现需求）

- 按不再复现的口径清理 `experiments/`：删除全部运行 checkpoints（含 ppab 24 run）、events.jsonl（logs/）、llm_calls（探针目录与 artifacts/llm_calls.jsonl）、tmux/eval 运行日志、`_logs/` 与全部 smoke/placeholder 目录，8.97 GB → 1.79 GB。
- 保留证据级工件：evaluations.csv、best_curve.csv、best_program.py、run_config.json、artifacts/{candidates,decisions,states_snapshots}、eval_best_* held-out results、generation_probe 的 analysis/results/snapshots/schedule/prompt_audit/anchors。
- 影响记录：ppab 批次无 candidates.jsonl，checkpoints 删除后来时路消融的过程指标（修改幅度、improve 率分布）不可再分析；中途预算 best-so-far 曲线分析不受影响（best_curve.csv 保留）。

## 2026-08-21：V9.14 批次与已完成重复的 held-out

- 正式批次 `v9_14_20260821_001824`（四任务三重复、1000 次真实评价、Qwen3.6-27B）00:18 启动。OBP 三路 06:37–09:27 完成，OP rep3、TSP rep3 于 10:27/10:28 完成；其余七路仍在搜索（预计当日 16 时前后收口）。
- `experiments/eval_artifacts.py` 适配 V9.14 工件布局：`best_program.py + logs/summary.json` 兜底分支增加按 `best_algorithm_id` 读取最优样本。
- OBP 三路统一 held-out（timeout 1000、workers 8，工件 `experiments/online_bin_packing/traceaad_v9_14/eval_best_20260821_v914_complete/results.json`）：六档 417.933 ± 1.724 / 2030.200 ± 8.697 / 4046.267 ± 14.883 / 81.267 ± 0.231 / 403.067 ± 0.702 / 805.267 ± 1.405，18/18 有效。六档全部高于（差于）V9.12（+2.3 至 +2.7 箱；capacity=500 档 +0.4 至 +1.0 箱），也全部高于 V9.7。OBP 行写入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。
- OP rep3 与 TSP rep3 部分 held-out（n=1，工件在各任务 `traceaad_v9_14/eval_best_20260821_v914_partial/results.json`）：OP50/100/200 = 15.217498 / 30.844940 / 54.660937；TSP50/100/200 = 5.895706 / 8.290585 / 11.717023。单重复不形成版本结论。
- 收尾接续机制：定时自动化两个实例均未触发（nextRunAt 过期而 runCount=0），改用 tmux 守护 `experiments/runners/traceaad/run_final_eval_v914.sh`（会话 `v914_eval_waiter`）：轮询 tsp rep2 与 cvrp rep2/rep3 的 finished summary，齐一组跑一组完整 held-out（TSP workers 8、CVRP 16），进程消失而无 summary 记日志跳过。
- OP 三路统一 held-out（timeout 1000、workers 8，工件 `experiments/op_aco/traceaad_v9_14/eval_best_20260821_v914_complete/results.json`）：OP50/100/200 = 15.061859 ± 0.137989 / 30.040432 ± 0.788832 / 52.642135 ± 1.754525，9/9 有效。三档全部高于（好于）V9.12（+0.7%/+1.0%/+0.5%）与 V9.7（+0.4%/+0.5%/+0.4%），方向一致；rep3 单路最强（15.217/30.845/54.661）。OP 行写入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。
- TSP 三路统一 held-out（同协议 workers 8，工件 `experiments/tsp_construct/traceaad_v9_14/eval_best_20260821_v914_complete/results.json`）：TSP50/100/200 = 5.749804 ± 0.126348 / 8.094207 ± 0.175829 / 11.494031 ± 0.308433，9/9 有效；三档均低于主表与归档全部行（此前最低为 V9.12 的 5.841/8.239 与 V8.2 的 11.829）。相对 V9.12 三档低 1.6%/1.8%/3.7%，相对 V9.7 低 4.0%/3.5%/4.4%，方向一致；rep1 的 TSP200 单规模用时 909.8 秒，未触 1000 秒超时。TSP 行写入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。
- CVRP rep2 于 16:05 收口（12 路搜索全部完成，全程无崩溃）；守护脚本随即完成 CVRP 三路 held-out（workers 16，工件 `experiments/cvrp_aco/traceaad_v9_14/eval_best_20260821_v914_complete/results.json`）：CVRP50/100/200 = 9.141101 ± 0.027146 / 15.788042 ± 0.038298 / 28.422103 ± 0.035542，9/9 有效。相对 V9.12：50 档低 1.6%，100/200 档高 0.2%/0.4%；相对 V9.7 三档高 1.3%/4.2%/4.2%。CVRP 行写入[归档实验结果](../../02-实验结果/各版本实验记录/历史版本-实验结果.md)。
- V9.14 批次闭包：TSP 与 OP 三档全部好于 V9.12 与 V9.7（方向一致，TSP 三档为两页全部行最低），OBP 六档全部差于两者，CVRP 相对 V9.12 一好两差、相对 V9.7 三档全差。任务分化，不构成跨任务统一升级；机制层归因待过程诊断。

## 2026-08-22：V9.15 四任务首批实验与统一 held-out

- 实现测试：`uv run pytest -q` 704 全通过（覆盖 V9.15 集成、launcher 与修复记账）。
- 批次 `v9_15_20260821_200536`（四任务三重复、1000 次真实评价、Qwen3.6-27B）12 路 run 全部 `finished/evaluator_budget_exhausted`、无 pending。统一 held-out 完成 45/45 个规模×重复；正式结果保留该批次的 CVRP、OP 与 OBP 三个任务，TSP 使用下述错误处理协议批次。
- 保留均值：CVRP50/100/200 = `9.406878 ± 0.369315 / 16.102769 ± 0.761063 / 29.023189 ± 1.300109`；OP50/100/200 = `15.137907 ± 0.027297 / 30.625471 ± 0.196485 / 54.843958 ± 0.543628`；OBP 六档 = `413.200 ± 2.433 / 2022.867 ± 4.438 / 4033.000 ± 7.000 / 81.133 ± 0.416 / 402.333 ± 0.757 / 803.867 ± 1.026`。
- 结果页分流规则修正：归档实验结果从"未进入主表对比的版本"改为"已被后续版本取代或淘汰的版本"，现行版本一律进主表；`recompute_search_rankings.py` 的 `MAIN_TRACEAAD` 相应纳入 V9.15。
- 相对 V9.14（均值方向）：正式 TSP 三档差 1.1%–2.5%、CVRP 三档差 2.0%–2.9%；OP 三档好 0.5%–4.2%、OBP 六档全部好 0.2%–1.1%。相对 V9.7：TSP 三档好 2.0%–3.0%、OP 三档好 0.9%–4.6%、CVRP 三档差 4.3%–6.4%；OBP 六档差 −0.3 至 +5.5 箱，方向不一致。
- 判读：V9.15 的初生保护与 ESS 广度在 OBP 上把 V9.14 的退化全部收回（回到 V9.7 水平），OP 进一步改善；CVRP 未兑现保护预期（比 V9.14 更差），TSP 让出部分精炼收益但好于 V9.7。任务画像再次分化，不构成跨任务统一升级。同规模口径单版本平均名次 2.143（1/6，相对 MCTS-AHD −0.429，全版本第 3，仅次于 V9/V9.7）；搜索 best 口径 2.750（与 V9.12 持平）。机制层归因待过程诊断。
- 口径事实：OBP train sanity 三重复逐位一致；CVRP/OP 无 sanity 项。

## 2026-08-22：V9.15 正式错误处理协议（TSP）

- 正式 TSP 批次 `v9_15_20260821_231453`（三重复、1000 次真实评价）held-out 工件为 `traceaad_v9_15/eval_best_20260822_v915_complete/results.json`，9/9 有效：TSP50/100/200 = `5.810646 ± 0.154429 / 8.199291 ± 0.405792 / 11.786740 ± 0.474832`。
- 修复统计（`evaluations.csv`）：三次重复初始尝试有效率 79%/85%/80%；触发修复 176/130/164 次，修复成功率 75%/69%/69%，修复后总有效率 78%/83%/78.5%。预检命中 3–7 次/千次。
- V9.15 的正式结果工件按任务读取：TSP 使用本批次，CVRP/OP/OBP 使用 `v9_15_20260821_200536` 批次。

## 2026-08-22：V9.14 升入主表与三口径单版本排名重构

- V9.14 四行从归档移入[实验结果](../../02-实验结果/00-主实验结果.md)主表；TSP 三列主表最优改判 V9.14（5.750/8.094/11.494，V9.15 次之）。主表 TraceAAD 名单（`MAIN_TRACEAAD`）现为 V4/V5/V8/V9/V9.7/V9.9/V9.14/V9.15。
- 结果页重构为三口径、各以单版本上场排名为首：训练成绩 → 搜索结果页（历史文件已不在当前仓库），同规模 → 同规模测试结果页（历史文件已不在当前仓库），held-out 15 规模 → [实验结果](../../02-实验结果/00-主实验结果.md) 新增"TraceAAD 单版本排名"节，由 `recompute_rankings.py --markdown` 生成（该脚本新增 markdown 输出）；两个生成页同步把单版本排名节移到主表之前。
- held-out 单版本排名要点：V9 2.067、V5 2.133、V9.7 2.200 居前三，V9.15 2.400（1/6，相对 MCTS-AHD −0.033），V9.14 3.333（3/6）——V9.14 持 TSP 全档最优但被 CVRP/OBP 拖累；V9.15 为当前版本中 held-out 最高。

## 2026-08-22：V9.15 协议修订——全量错误反馈、两次修复、槽位预算

- 决策（用户拍板）：修复提示携带最完整且有价值的错误信息（类型名 + 完整消息不截断 + 只含候选栈帧的 traceback + 超时降复杂度指引）；每个失败候选给两次修复；预算按正常评估计——1000 个槽位 = 1000 个初始候选，修复是额外 evaluator 调用，不动搜索预算；summary 双记录 `budget_slots` / `evaluator_call_count` 保证可审计。
- 依据：修复成功率按错误类型拆解（470 次）——超时 87%、Syntax/Name ~85–90%，弱项 ValueError 43%（101 次，消息为 broadcast shape off-by-one 70 次、argpartition kth 越界 39+ 次，均为巡游末段边界 bug）；直接原因是修复提示只有单行 360 字符消息、无类型名无 traceback，模型拿不到出错代码位置。
- 实现：`EvaluationOutcome` 增加 `traceback` 字段并从安全子进程透传；`prompt.format_failure_feedback` 策展反馈（过滤 site-packages/llm4ad 内部帧，保留 `<string>` 候选帧，无则取最内两帧，至多 8 帧，消息本身不截断，evaluator 侧消息上限 1000→20000）；V9.15 使用 `error_retries=2` + 槽位记账（`_charge_evaluation` 仅初始尝试推进 `_n_eval`，`_n_calls` 计真实调用）；checkpoint 持久化 `n_calls` 与 attempt 状态；runner `method_params` 记 `retry_policy=two_bounded_repairs`、`retry_budget=initial_candidates`。
- 已完成的正式 TSP 批次 `20260821_231453` 采用一次修复协议并将修复调用计入真实评价预算；后续正式 V9.15 批次采用本次修订后的两次修复与槽位预算协议，规范见 [V9.15 规范 §6](../../01-主线版本/TraceAAD-V9.15-机制设计.md)。
- 测试：`uv run pytest -q` 703 通过（`test_traceaad_v915_protocol.py` 覆盖槽位记账、修复穷尽占一槽、修复提示含类型/候选帧/无内部路径、evaluator traceback 透传）。

## 2026-08-23：V9.16 五任务正式结果与 VRPTW 七方法对照

- V9.16 五任务批次 `v9_16_20260822_151700`（三重复、1000 次真实评价）与 VRPTW 对照批次 `20260822_142500`（五外部对照 + V9.14）全部完成 held-out 评估，3/3 有效；工件为各任务 `eval_best_20260823_v916_complete` / `eval_best_20260823_vrptw_complete` / `eval_best_20260823_v914_complete`。
- V9.16 held-out：TSP 5.850/8.344/11.764，CVRP 9.178/15.552/28.065，OP 15.171/30.616/55.028，OBP 420.47/2027.47/4044.53/80.80/402.33/804.27，VRPTW 18.822 ± 0.227。
- 三口径单版本排名（现存工件）：held-out 15 规模 1.900（1/6）、同规模 7 设置 2.214（1/6）、搜索 1.625（1/6，历史 V4 1.000 之上）。
- VRPTW 七方法名次（test 均值）：V9.16 18.822、V9.14 19.389、EoH 19.521、MCTS-AHD 20.836、CALM 20.852、ReEvo 20.926、PathWise 21.009；参考最优 16.282（全部证明最优）。
- [实验结果](../../02-实验结果/00-主实验结果.md) 升为五任务并新增 VRPTW 节；OBP 5k_500 最佳改判 V9.15/V9.16 的 402.333。旧版本（V4–V9.12）评估工件已删，三个排名脚本改为跳过缺失工件，同规模/搜索两页保留历史行手工合并；搜索曲线加入 V9.16 与 VRPTW 面板。
