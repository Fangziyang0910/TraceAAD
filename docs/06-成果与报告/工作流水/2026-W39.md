# 2026-W39 工作流水

## 2026-09-21：V10.11 四组训练终点复核

- **问题与证据**：汇总默认短轨迹、无轨迹、全代码轨迹、档案参考四组的五任务训练表现。读取 `experiments/traceaad_v10_11/results/batch_*.json`、60 路 `tree_state.json` 与 `nodes.jsonl`；四批 manifest 均为 15/15 finished，全部检查点 budget_used=1000，未发现 V10.11 搜索/调度进程。
- **口径**：每路取 evaluation_id≤1000 的最大 fitness，OP 保留正号，其余转回正向成本；按三重复报告均值与样本标准差。重算与既有结果文档训练数值一致。
- **结论**：默认组在 TSP/VRPTW 的训练均值最好，档案参考在 CVRP/OBP 最好，全代码轨迹在 OP 最好；三重复描述性结果不证明普遍优势或泛化。全代码组包含 TSP rep2、VRPTW rep1 提示溢出截断后续跑，保留该口径差异。
- **复核位置**：[V10.11 实验结果](../../02-实验结果/各版本实验记录/V10.11-实验结果.md)。未新增评价或修改任何实验。后续 V10.12 按用户指定的单项改动对照上述基线，训练与 held-out 分开报告。

## 2026-09-21：V11 与 V11.1 未继承上下文互补优势的复核

- **问题**：V10.11 generic 与 rand_ctx 已显示任务互补，为何算子条件化的 V11/V11.1 未继承优势？读取用户引用讨论、两个机制定义、selection.py、正式批 manifest、检查点与节点记录；未改变或停止任何运行。
- **运行边界**：V11 25 路均 E1000；V11.1 manual 批仍 partial，读取时 15/25 检查点达到 E1000，其余 539–875，存在运行进程。排除废弃的非 manual 批；不引用扩大规模测试。
- **共同 E500 训练 best 均值**（generic/rand_ctx/V11/V11.1；前两组各三重复，后两组各五重复）：TSP 5.8509/6.0497/6.1682/6.4226；CVRP 9.2014/8.8107/8.7952/8.9512；OP 14.7933/14.6527/14.6856/14.7720；OBP 727.8333/726.5000/727.8000/728.2000；VRPTW 18.8200/20.4748/20.1168/19.9195。按 evaluation_id<=500 的节点最大 fitness 计算，OP 正号，其余反号；这是共同预算描述性截面，不是 V11.1 最终排名。
- **新增分配核查**：按 manual 各路 tree_state 中 nodes 的 fitness/attempts 和实际 selection.py 公式重建下一次父代概率，ESS=1/sum(p^2)。任务内五路 ESS 均值依次为 TSP 282.1、CVRP 220.0、OP 310.1、OBP 371.5、VRPTW 329.2；质量前八节点概率质量约 6.0%、7.2%、4.2%、4.0%、4.8%。这些是不同进度当前检查点的分配诊断，不是共同预算质量比较。
- **判断与纠偏**：V11 的 argmax 精英锁定与 V11.1 的全库固定温度预算摊薄是不同结构，不能沿用“二者都过度集中”的解释。V11.1 有相对质量偏好，但绝对有效开发节点数随库增长显著增加；质量分布不等于行为多样性。任务级上下文赢家也不能直接识别算子级最优匹配，尤其 rand_ctx 对所有算子提供档案，而 V11.1 只对 Pivot/Fuse 提供。
- **证据缺口与下一步**：没有同分配下的算子上下文单因素对照，不能断言上下文混合无效或分配是唯一原因。优先固定 V10.11 父代分配验证算子条件化上下文，再固定上下文比较分配；以 TSP/CVRP 为相反任务，三重复、每路 E1000，记录 E500/E1000 前沿和有效评价成本。前沿/父代改进脱钩是共有现象，不能单独作为失败归因。原始证据为 experiments/traceaad_v10_11/results、traceaad_v11_0/results、traceaad_v11_1/results 下对应批次的 manifest、nodes.jsonl 与 tree_state.json。

## 2026-09-21：V10.12 最小融合方案建议

- **建议版本**：以 V10.11 generic 为唯一基线，只改生成上下文；保留 ESS-8 质量分配、Pivot 1:1 均匀混合、Fuse donor、四算子比例、一次修复、任务契约、模型与每路 1000 次真实评价。
- **混合上下文**：所有算子继续看到当前程序与最近 8 条短形成轨迹；每次额外加入 2 张档案 profile card；每张卡只包含一个算法节点的 Idea 与实测 fitness，按现有 rand_ctx 的 rank-softmax tau=8 无放回抽样，排除当前父代。档案卡只作为补充证据，不替换形成顺序；Fuse 保留原 donor 完整代码。
- **机制假设**：短轨迹保留 TSP/VRPTW 所需的连续关系，档案卡提供 CVRP/OBP 所需的跨分支机制；低数量档案卡限制提示负担和随机噪声。该假设不预设 task router，也不假设算子标签已经证明了最优历史匹配。
- **检验**：V10.12 与 V10.11 generic、V10.11 rand_ctx 同时比较，五任务×三重复×E1000；训练 best、E500/E1000 frontier、parent-to-frontier 转化、有效率、提示 token 与运行错误分别记录，并做同规模测试。
- **停止门**：若 TSP/VRPTW 任一任务相对 generic 的同规模测试恶化超过 1%，或提示超限/有效率明显恶化，停止扩大档案卡数量；若 CVRP/OBP 没有改善且构造型任务受损，回退为 V10.11 generic。只有在至少一个档案受益任务改善且 TSP/VRPTW 不受实质损害后，才考虑算子条件化或动态路由。

## 2026-09-21：V10.12 最小实现落地与单测验证

- **动作**：按 `TraceAAD-V10.12-机制设计.md` 完成代码修改与验证，保持 V10.11 generic 调度与算子配置完全不变。
- **改动位置**：
  1. `llm4ad/method/traceaad_v10_12/selection.py`：加入 `rank_softmax_sample(nodes, k, rng, tau=8.0)`，无放回抽样并优雅处理节点数不足的情况；
  2. `llm4ad/method/traceaad_v10_12/prompts.py`：`TrajectoryBuilder` 增加 `_cards_text`，在当前代码与形成历史之后渲染 `# Archive Profile Cards`（仅含 Idea 与 measured fitness），为空时自动省略；
  3. `llm4ad/method/traceaad_v10_12/traceaad.py`：`_schedule` 抽样 2 张档案卡并记录 `reference_ids`，保留 ESS-8 质量分配、Pivot 1:1 均匀混合和 Fuse donor 完整代码；
  4. `experiments/traceaad_v10_12/run.py` 与 `launch.py`：支持 `--n-profile-cards`（默认 2）与 `--profile-card-tau`（默认 8.0）；
  5. `tests/method/test_traceaad_v1012.py`：新增抽样性质、提示组装、空卡兜底及端到端参考追踪测试。
- **验证结果**：
  - `tests/method/test_traceaad_v1012.py` 4/4 项单测 PASSED；
  - `tests/task/test_frozen_task_contract.py` 17/17 项冻结契约断言 PASSED，任务契约零改动。

## 2026-09-21：V11.1 实验安全暂停与状态留存

- **动作**：按用户指令暂停 V11.1 manual 批次运行。
- **运行核查与状态**：
  - 检查 25 路运行状态：17 路已达 E1000 完成评测（Finished）；8 路处于运行中（cvrp r1~r5 进度 E823~E941，op r2~r3 进度 E887/E681，tsp r4 进度 E793）。
  - 逐路核验 8 路运行目录中 `tree_state.json`，JSON 结构完整无损，保留后续从断点恢复能力；未清理或修改任何实验结果目录。
- **进程操作**：
  - 仅安全终止 8 个 `v111_manual_*` tmux 会话与对应 Python 进程，释放模型服务端点槽位。
  - 本地模型服务 `qwen3_8-27b` 与后台监控服务 `traceaad_monitor` 保持运行，未受任何影响。

## 2026-09-21：TraceAAD V10.12 批次正式启动与监控适配

- **目标与批次定义**：按用户指定配置启动 TraceAAD V10.12 验证批次 `20260921_v1012`；覆盖 5 个优化任务（TSP、CVRP、OP、OBP、VRPTW），每任务 4 次重复（rep 1~4，对应种子 0~3），全批共 20 路独立进化实验，真实评价预算各 1000。
- **算力与槽位映射**（严格匹配用户指定的 20 路端点分布）：
  - `local`（3 路）：`tsp_rep2`, `obp_rep2`, `vrptw_rep2`；
  - `server1`（5 路）：五任务各 1 路（`tsp_rep1`, `cvrp_rep1`, `op_rep1`, `obp_rep1`, `vrptw_rep1`）；
  - `server3`（6 路）：`tsp_rep3`, `cvrp_rep2`, `cvrp_rep3`, `op_rep2`, `obp_rep3`, `vrptw_rep3`；
  - `server3b`（6 路）：`tsp_rep4`, `cvrp_rep4`, `op_rep3`, `op_rep4`, `obp_rep4`, `vrptw_rep4`；
  - 各端点并发均在硬件上限内（local 3/3, server1 5/6, server3 6/9, server3b 6/9），启动前全端点连通性测试通过。
- **代码冻结与启动**：
  - 通过 `experiments/traceaad_v10_12/freeze.py` 完成运行时冻结（`results/runtime_20260921_v1012/`），记录 `runtime_manifest.json`；
  - 创建并执行 `experiments/traceaad_v10_12/manual_launch.py`，生成 `results/batch_20260921_v1012.json` 并建立 20 个独立 tmux 会话（`v1012_<task>_r<rep>`），全部正常拉起且处于 `running` 状态。
- **训练监控适配**：
  - 调整 `experiments/traceaad_v10_12/monitor.html` 与 `monitor.py`：扩展 CSS/SVG 渐变与配色至支持 4~5 重复，添加多根目录兼容；
  - 重启 `traceaad_monitor` 监控服务（端口 8765），默认绑定并实时呈现 V10.12 批次运行进展。

## 2026-09-22：V10.12 完赛槽位复用与 V11.1 断点恢复
## 2026-09-22：V11.1 全量 8 路暂停任务安全恢复

- **触发与核查**：
  - V10.12 批次中 `online_bin_packing` 的 rep3（运行于 `server3`）与 rep4（运行于 `server3b`）达到 1000 预算顺利完成（finished，最佳箱数分别为 726.0 与 726.8）；
  - `server3` 与 `server3b` 各释放出 1 个 LLM 请求槽位（当前并发：server3 6/9，server3b 6/9，集群处于健康安全水位）。
- **挑选与恢复**：
  - 检查 V11.1 manual 批次 8 路暂停状态，优先恢复在当前释放端点上最接近终点的 2 路：
    1. `tsp_construct` rep4（种子 3，端点 `server3`）：检查点进度 941/1000（仅剩 59 次评价即可完赛，且使 TSP 任务达到 5/5 全完赛）；
    2. `cvrp_aco` rep2（种子 1，端点 `server3b`）：检查点进度 934/1000（仅剩 66 次评价即可完赛，为 CVRP 任务取得首个完成重复）；
  - 启动 tmux 会话 `v111_manual_tsp_r4` 与 `v111_manual_cvrp_r2`，确认 `resumed=True` 成功加载断点树并继续生成评价；
  - 更新 `experiments/traceaad_v11_1/results/batch_20260920_v111_manual.json` 中的运行状态与恢复时间戳。
- **触发与算力容量校验**：
  - V10.12 批次中 `online_bin_packing` 的 rep3 与 rep4 完赛，释放部分槽位；
  - 检查集群全局并发容量（上限：server1 6, server3 9, server3b 9, local 3，总 27 槽位）；
  - V10.12 现有 18 路正在运行（local 3, server1 5, server3 5, server3b 5），全集群剩余 9 个可用槽位（server1 1, server3 4, server3b 4, local 0），完全具备容纳 V11.1 全部 8 路未完赛任务的物理算力。
- **8 路恢复与容量配比**：
  1. `tsp_construct` rep4（种子 3，端点 `server3`，断点 941/1000）；
  2. `cvrp_aco` rep2（种子 1，端点 `server3b`，断点 934/1000）；
  3. `op_aco` rep3（种子 2，端点 `server1`，断点 933/1000）；
  4. `op_aco` rep2（种子 1，端点 `server3b`，断点 885/1000）；
  5. `cvrp_aco` rep5（种子 4，端点 `server3`，断点 872/1000）；
  6. `cvrp_aco` rep1（种子 0，端点 `server3`，断点 817/1000）；
  7. `cvrp_aco` rep4（种子 3，断点 784/1000）：因 `server1` 槽位已满（6/6），经 `prepare_resume` 安全重路由至 `server3b`，写入 `routing_history.jsonl`；
  8. `cvrp_aco` rep3（种子 2，端点 `server3`，断点 681/1000）。
- **运行与监控状态**：
  - 8 个 tmux 会话（`v111_manual_*`）全部正常启动，逐路确认 `resumed=True` 成功载入节点与 RNG 种子继续评价；
  - 集群总并发达到 26 路（V10.12 18 路 + V11.1 8 路），各端点负载（local 3/3, server1 6/6, server3 9/9, server3b 8/9）均在上限内且运行平稳；
  - 同步更新 `batch_20260920_v111_manual.json` 清册，监控面板正常联动展示。

## 2026-09-22：高并发 CPU 竞争超时排查、状态精准回滚与 V10.12 单批续跑

- **问题现象与证据分析**：
  - 监控观察到宿主机 Load Average 飙升至 47.17，且评测日志中伪超时（False Timeouts）激增。
  - 数据实测统计：09:47 前（18~20 路）V10.12 超时率 6.35%，V11.1 超时率 3.07%；09:47~10:11（恢复 8 路达 26 路后）V10.12 超时率升至 13.2%，V11.1 升至 20.3%；10:11~10:26（叠加宿主机 root 外部工具占用 2.7 核）超时率恶化至 31.8% 与 33.9%。
  - 超时时间分布：TSP（限时 20s）密集分布在 `20.11s~20.57s`，CVRP（限时 120s）密集分布在 `120.22s~120.71s`，均为微弱超限；因果定性为并发 26 路进程超出宿主机调度与评测 CPU 承载能力引发的进程争抢误杀。
- **决策与安全备份**：
  - 决定终止 V11.1 实验，仅保留 V10.12 单批运行；放弃 09:47~10:28 期间受 CPU 争抢污染的非正常评测结果。
  - 全量安全备份：建立 `experiments/traceaad_v10_12/backup_before_rollback_20260922_1028` (194MB) 与 `experiments/traceaad_v11_1/backup_before_rollback_20260922_1028` (331MB)。
- **截断基线与状态数学对齐**：
  - 统一截断时间点：`CUTOFF = 2026-09-22T09:47:00`（即早前恢复 V11.1 动作之前）；
  - V10.12 的 `online_bin_packing` rep3 与 rep4 早在 09:45 前已达 1000 预算完赛（Finished），保留最终成果无需回滚；
  - 其余 18 路 V10.12 回滚至 09:47 前状态：`events.jsonl`、`nodes.jsonl`、`llm_calls.jsonl` 按 cutoff 截断，重构 `tree_state.json`；数学校验 `events` 中的 `parent_count_before + 1` 与 `parent_selection_counts` 误差为 0，`step_counter`、`budget_used`、`completed_attempts`、`invalid_streak` 完全对齐；回滚点预算依次为：TSP 698/548/796/748，CVRP 433/438/413/444，OP 550/633/725/662，OBP 926/857，VRPTW 623/660/600/680；
  - V11.1 的 8 路未完赛任务同样截断至 09:47 之前（预算点：TSP r4 941，CVRP r1~r5 817/934/681/784/872，OP r2~r3 885/933），更新 `batch_20260920_v111_manual.json` 清册为 `paused`；
  - 任务冻结契约检验：运行 `tests/task/test_frozen_task_contract.py`，17/17 项测试全部 PASSED，契约零修改。
- **V10.12 重启与当前运行状态**：
  - 启动 18 个 tmux 会话（`v1012_<task>_r<rep>`），逐路确认 `resumed=True` 且成功从 09:47 断点预算续跑；
  - 系统 Load Average 迅速自峰值 47+ 回落至 11~12 的健康水位，CPU 竞争彻底消除；
  - 后台监控服务实时刷新：当前全批 20 路中 18 路运行正常（Running）、2 路完赛（Finished）、0 路故障或停滞。

## 2026-09-22：V10.12 rand_ctx 消融对照组解耦与清理

- **背景与决策**：用户确认无需进行 V10.12 的 rand_ctx 消融实验，清理冗余的对照组实现与启动入口，保持主线代码树干净单一。
- **清理与解耦动作**：
  1. 移除方法目录 `llm4ad/method/traceaad_v10_12_rand_ctx/`；
  2. 移除消融启动脚本 `experiments/traceaad_v10_12/schedule_rand_ctx.py`；
  3. 解耦 `experiments/traceaad_v10_12/run.py`：移除 `TraceAADV1012RandCtx` 导入与 `--rand-context`/`--n-references` 参数及分支；
  4. 解耦 `experiments/traceaad_v10_12/freeze.py`：移除源目录列表中的 `traceaad_v10_12_rand_ctx` 项；
  5. 解耦 `experiments/traceaad_v10_12/launch.py`：移除 `--rand-context` 参数与计划构建中的分支逻辑。
- **验证**：
  - `python -m experiments.traceaad_v10_12.run --help` 与 `launch --help` CLI 输出正常；
  - `tests/method/test_traceaad_v1012.py`（4 项）及 `tests/task/test_frozen_task_contract.py`（17 项）全部 PASSED；
  - 线上正在运行的 `runtime_20260921_v1012` 沙箱与 tmux 进程未受任何影响。

## 2026-09-22：V10 系列旧版本（V10.3~V10.9）解耦与彻底清理

- **背景与决策**：用户确认执行“方案 B：彻底清理”，移除 V10 系列早期探索代码与历史实验产物，保留里程碑与活跃版本（V10.1、V10.2、V10.10、V10.11、V10.12）。
- **解耦与重构动作**：
  1. 解耦 `traceaad_v10_10`：此前其深层依赖 `v10_8 -> v10_7 -> v10_6 -> v10_5 -> v10_3` 继承链。在 `errors.py` 中内联 `normalize_code` 与 `THINK_BLOCK_RE`；在 `trajectory.py` 中内联 `BaseBuilder` 与 `digest`；在 `traceaad.py` 中重构为继承自包含的 `TraceAADV1011` 并映射兼容参数与导出 policy 常量，完全斩断历史继承；
  2. 解耦 `experiments/traceaad_v10_10/launch.py`：内联 `allocate`, `healthy_slots`, `atomic_json`，解除对 `v10_8` 实验工具的依赖；
  3. 解耦 `experiments/traceaad_v10_10/freeze.py`：从复制源中剔除 `traceaad_v10_8`；
  4. 验证 CLI 正常启动：`python -m experiments.traceaad_v10_10.run --help` 正常退出且输出完整参数树。
- **清理与磁盘释放**：
  1. 移除 7 个旧方法目录：`llm4ad/method/traceaad_v10_{3,4,5,6,7,8,9}/`；
  2. 移除 7 个旧实验与结果目录：`experiments/traceaad_v10_{3,4,5,6,7,8,9}/`，释放宿主机约 4.6GB 磁盘空间；
  3. 更新 `experiments/README.md` 导航指引。
- **安全与验证**：
  - 检查正在运行的 V10.12 批次（18 个 tmux 会话及 monitor）运行平稳，完全独立未受波及；
  - 运行并通过方法单测与任务冻结契约全量测试（`tests/task/test_frozen_task_contract.py` 17/17 PASSED）。

## 2026-09-22：V10.12 同预算进展、重复方差与 ACO 停滞复核

- **问题与证据**：核对 V10.12 是否比 V10.11/V11 系列更均衡，以及弱重复是否陷入局部最优。读取五批 manifest、冻结运行时记录、100 路配置/检查点/节点/事件和实时进程；V11.1 仅使用 manual 批，排除回滚段。逐路核验真实评价 ID 连续无重复；同任务契约哈希与评价参数跨组一致。
- **快照**：21:20 +08:00，V10.12 为 15/20 完赛，CVRP 四路 E780/788/731/795、OP rep1 E889；五路搜索进程存活。V11.1 为 17 完赛、8 暂停。manifest 的 running/finished 状态滞后，按实际检查点报告。
- **判断**：共同 E600 下，V10.12 相对 generic 的 TSP/VRPTW 退步约 2.93%/3.44%，CVRP 改善约 0.96%，OP/OBP 小幅改善；相对 V11 系列四任务观察均值较好、CVRP 较差。支持跨任务折中，不支持同时继承各上下文的最强表现，也不构成最终排名或泛化结论。
- **重复差异**：TSP/VRPTW E1000 的样本标准差相对 generic 增大，OBP 缩小。VRPTW rep1 E339 后 566 次有效评价仍无前沿提升；最后 E200 有 15 次父代改进、62 次直接开发 best，支持搜索停滞但不证明数学局部最优。OP 强重复也有长平台，不能仅按平台长度判断质量。
- **新增运行异常**：八路 ACO 最后有效评价集中在 15:55 左右，此后合计 895 次失败、其中 776 次 timeout、0 次有效。宿主机 32 逻辑 CPU，采样 load 约 93–106，多进程 worker 存在各 32 线程的情况。共同运行异常需要优先核查；根因未确认，不能把全部 timeout 直接定为伪超时，也不能将最近 ACO 停滞归因于局部最优。
- **产物与边界**：更新 [V10.12 实验结果](../../02-实验结果/2026-09-23-V10.12与V11.1-heldout测试.md) 为权威分析位置，纠正 README 的“尚未运行”；原始统计快照保存在 `experiments/traceaad_v10_12/analysis_20260922/progress_snapshot.json`。未新增评价、停止/重启进程、改动冻结机制或覆盖 journal。
- **下一步选择**：先定位共同评价中断、在独立记录下复核固定候选的评价有效性，再判断剩余预算和同规模 held-out。搜索机制诊断优先比较 VRPTW 弱/强重复的父代改进到前沿转化；未证明档案卡或父代集中是原因，不在正式批中途改机制。

## 2026-09-22：ACO 后段异常根因确认——工作区导入破坏与冻结启动失效

- **问题**：核查代码清理是否导致 ACO 后段评价失败。保留前次 manifest/运行状态/结果快照，继续检查五路活跃进程、tmux stderr、提交 `4718b68a`、源码时间戳与冻结哈希。
- **直接证据**：五路 tmux 均反复出现 spawn worker 的 `IndentationError`，来自 V10.10 文件连续两行 class 声明（39/40 行）。V10.12 run.py 顶部也仍导入已删除的 rand_ctx 包；此前清理仅追加 generic 赋值，未实际移除该 import。
- **机制与隔离检验**：manual launcher 虽创建冻结副本，但使用 cwd=ROOT 启动工作区模块；现存进程也未设置 runtime PYTHONPATH。已加载的父进程继续生成，新 spawn worker 重新导入被破坏的工作区并退出，Pool 反复补建，外层最终 timeout。完整冻结副本的 75 文件 SHA256 一致；隔离启动对照为完整冻结成功、仅删除 rand_ctx 失败（ModuleNotFoundError）、当前工作区失败（IndentationError）。未调用 LLM 或真实评价器。
- **结论强度**：当前语法错误、残留依赖和未隔离启动均为直接证据；15:55 首次中断由 rand_ctx 清理触发是与时间及受控重现一致的解释，最初 stderr 已不在 pane 历史中，不能伪称直接观察。16:13 的后续语法错误有文件和实时 traceback。上一轮高负载嫌疑应更新为明确的 worker 导入故障；负载中各因素占比未量化。
- **纠正旧记录**：本周此前“清理不影响在线运行”“CLI 验证正常”的判断与当前代码/提交不符；创建冻结目录不等于实验从冻结目录运行。历史流水保留，以上述直接核查为准。
- **产物与下一步**：ACO worker 导入失败诊断（独立诊断已清理） 为权威根因文档；源路径证据、五路 pane 和可复现启动对照位于 `experiments/traceaad_v10_12/analysis_20260922/aco_incident/`。恢复应先获准安全暂停、保留原始异常尾段，再绑定冻结运行时做固定候选评价复核；不能仅改 budget 或静默覆盖失败。本次未停止、重启、修改实验代码或原始结果。

## 2026-09-22：ACO 导入故障修复、精确状态恢复与八路续跑

- **授权与范围**：用户要求修复实验代码，并剔除错误尾段后从正确状态续跑。仅停止五路活跃 ACO，备份全部八路 ACO（含已耗尽预算的三路 OP）；完整备份为 `experiments/traceaad_v10_12/backup_before_aco_recovery_20260922_214100/`，保留 manifest、journal、原 checkpoint、summary 与终端证据。
- **修复**：V10.12 run/launch/freeze 移除 rand_ctx 残留和启动脚本语法错误；回退 V10.10 不完整继承改写并恢复必需的 V10.3–V10.8 方法依赖，避免改变旧算法语义，未恢复旧实验数据。manual launcher 现在验证哈希/语法与 spawn 引导，并显式设置 tmux cwd/PYTHONPATH；run 拒绝从可变工作区直接启动。最终使用 `runtime_20260922_v1012_recovery`，40 个搜索/任务/评价器源文件与原冻结版哈希一致。
- **恢复校验**：以 15:54 前最后已结算候选为边界，CVRP 四路恢复 E650/661/607/657，OP 四路 E744/866/974/917。中间 checkpoint 未逐次保留，使用最终已存 RNG 与尾段抽样次数重建边界状态，再用原 `_schedule` 逐条回放 985 条尾段候选；所有 parent/donor/reference/operator/repair 选择、计数和最终 RNG/节点均匹配后才应用前缀。未简单修改预算或沿用尾端 RNG。
- **预算与留痕**：活跃记录剔除 955 次真实调用，原件保留；每路仍以 E1000 为逻辑评价预算，事故导致的额外物理支出单独保留。八路 routing history 与 manifest 记录恢复点和新 runtime；其余 12 路已完成结果保持。
- **验证**：方法、启动隔离回归和冻结任务契约共 25 passed。固定候选复核 CVRP=-8.6427893553783、OP=14.703999999999999，与原训练分数一致；诊断调用独立保存。八路实际 cwd/PYTHONPATH 都指向最终冻结目录，全部记录 resumed=True，且全部产生新有效分数；检查窗口无 timeout 或导入异常。OP rep3 一次普通候选运行错误由既有修复流程正常恢复。
- **权威记录与下一步**：ACO 故障诊断与恢复（独立诊断已清理）；`analysis_20260922/aco_incident/` 保存恢复脚本、前缀哈希、RNG 回放收据、固定候选结果和运行健康证据。后续依据恢复后的 journal 完成剩余预算，再作同规模 held-out；先前异常快照保留作诊断，不混入最终质量统计。

## 2026-09-23：V11.1 剩余八路续跑

- **检查**：V10.12 的 20 路均已达到 E1000。V11.1 manual 批次仍有 TSP r4、CVRP r1–r5、OP r2–r3 共八路未完成；八路 checkpoint 的 `budget_used` 与此前暂停记录一致，原冻结运行时的搜索、任务和评价器文件可通过哈希校验。
- **修复**：发现 V11.1 manual launcher 沿用可变工作区作为 tmux cwd，未设置冻结运行时 `PYTHONPATH`，存在 spawn worker 导入漂移风险。加入冻结文件哈希、语法和 spawn 预检，显式绑定 tmux cwd/PYTHONPATH；新增仅对暂停路由生效的 checkpoint resume 入口。修复过程中还兼容了 TSP 旧配置没有 `n_workers` 字段的问题。
- **验证与状态**：V11.1 方法测试及冻结任务契约共 25 passed；冻结运行时预检通过。八路已全部创建 tmux 会话并从原 checkpoint 继续，30 秒后预算分别前进至 TSP r4 E945、CVRP r1–r5 E826/945/692/790/878、OP r2–r3 E894/942；日志尾部未发现 Traceback 或 IndentationError。当前仍为 partial，不能据此排名。
- **留痕**：启动前备份位于 `experiments/traceaad_v11_1/backup_before_resume_20260923_v111_resume/`；批次 manifest 保留每路 runtime、source identity、resume budget 和 routing history。后续待八路自然达到 E1000 后再汇总，不改动 V11.1 机制或任务契约。

## 2026-09-23：仓库去平台化重构（三分顶层结构）

- **决策**：用户确认按「三分 + llm4ad 核心」重组：`traceaad/`（15 个版本子包）、`baselines/`（eoh/reevo/pathwise/mcts_ahd/calm/shinka_evo）、`benchmarks/`（五任务 + generated_data_config，冻结契约内容零改动）；`llm4ad/` 只保留 base+tools 共享核心与署名。历史版本删 v9_*、留 v10_1/v10_2；hsevo、llamea 及其测试删除。
- **提交序列**：`c19a439c` 先固化 09-22 事故修复（此前未提交）；`66e7e7e3` 删平台死代码（14 方法包、13 任务族、3 个 LLM 后端，-60.6 万行）并把 4 个递归扫描+静默吞错的 `__init__` 改为显式导入（事故感染路径不复存在）；随后提交顶层重组与全量 import 迁移。
- **依赖与运行方式**：pyproject 依赖 33→13 项（逐项 grep 保留代码使用者后裁剪）；uv 改虚拟项目 `package=false`——setuptools editable 构建对 `experiments/**/results` 巨树的 packages.find 扫描曾卡死 15 分钟（10GB RSS），移除 editable 安装后统一从仓库根 `python -m` 运行。
- **防回归验证（对照事故清单）**：全量 299 passed（含冻结任务契约哈希）；全新解释器 32 模块导入冒烟；六个活版本与六个基线 `python -m experiments.<v>.run --help` 全通过；spawn 子进程重导入探针通过；运行中 V11.1 八路 cwd/PYTHONPATH 仍指向原冻结 runtime 未受影响；`llm4ad.method`/`llm4ad.task` 残留 grep 清零（历史 results 冻结副本与事故证据工具除外）。
- **边界**：未改任何机制代码（类名/METHOD/journal 字段不动）、基线仅改 import 行、任务文件仅搬移；六个活版本 freeze.py 源清单同步迁移，v10_10 freeze 顺带清除 09-22 修复遗留的死赋值行；新增根 `README.md` 结构导航。

## 2026-09-23：共享核心精简与更名 core

- **决策**：用户确认共享代码全面简化并改中性名。两个提交：`5e317978`（精简）、`fe7016bd`（更名）。
- **删除的死开关**（全库 grep 证实零使用者后才删）：Evaluation 的 numba/保护除法×2/random_seed/exec_code 五参；shinka_evo 的 `random_seed` 经查进入方法自身 RNG 与 bandit，从未到达 Evaluation——modify_code.py(418行) 整删，`_modify_program_code` 随之消失，行为零变化。五任务构造器的 `use_numba_accelerate=False` 死参同步删除（冻结哈希测试通过）。
- **方法专用设施下沉**：SampleTrimmer(190行)→`baselines/sampling.py`；profiler 三件(784行)→`baselines/profiler/`；env.py→`experiments/infra/env.py`。LLM ABC 与 OpenAIAPI 合并为单文件。tools/ 目录消失。
- **结果**：共享核心 2610 行→1055 行 3 文件，更名 `core/`；`from core import SecureEvaluator` 等 77 个活文件迁移，六个 freeze.py 源清单同步。
- **验证**：299 passed、导入冒烟、6 个 `-m` 入口、spawn 重导入探针、运行中 V11.1 八路 cwd/PYTHONPATH 仍指向原冻结 runtime。
- **边界**：SecureEvaluator 的多进程击杀机器（事故加固）逐字未动；fork_proc/daemon_eval_process/safe_evaluate 保留（cleanup 测试显式使用）；基线方法体与任务内容零行为变化。

## 2026-09-23：训练可视化独立化与展示逻辑收敛

- **研究问题**：训练面板当前能显示运行状态，但实现仍挂在 V10.11 机制目录，且每次刷新会重复扫描实时工件；需要确认面板是否把真正影响科研判断的证据放在主视图，并降低监控自身的资源开销。
- **实现**：将通用 monitor 数据引擎和页面集中到 [`core/training_monitor.py`](../../../core/training_monitor.py) 与 [`core/training_monitor.html`](../../../core/training_monitor.html)；V10.11/V11.0/V11.1 入口改为兼容转发，旧重复页面删除。总览缓存从 3 秒调整为 5 秒，页面刷新从 10 秒调整为 15 秒，旧版本启动脚本显式传入对应结果目录。
- **展示判断**：主层级保留评价预算、运行状态、Best 改进里程碑和收敛轨迹；单路检视保留尝试散点、父代/全局前沿改善、算子路由、候选代码和血统。LLM/Eval 耗时与 prompt token 降为诊断指标，避免把效率或结构量直接当作质量结论。
- **证据与验证**：当前 V11.1 manual 批次 manifest 与结果目录已检查，未清理或改写运行工件；共享引擎读取 25 路结果并得到 24,868/25,000 评价，三路 stalled，未将此快照当作最终排名。`tests/experiments/test_traceaad_v1011_monitor.py` 3/3 通过，四个 monitor 入口通过 `py_compile`，共享 CLI 帮助正常。
- **边界与下一步**：面板仍是训练期证据视图，不提供 held-out 泛化结论；后续若要做质量判断，应把训练 Best 与同预算 held-out 结果接入独立比较视图，而不是继续增加实时卡片。

## 2026-09-23：benchmarks 低风险路径优化复核

- **问题与证据**：检查五个冻结任务的评测入口、数据生成器、公共配置和测试；发现 OBP 评测模块在普通数值评测导入阶段加载 `matplotlib`，TSP/VRPTW 数据生成器修改进程级 NumPy RNG，TSP 每次候选评测重复计算固定实例的邻域排序。
- **实现**：将 OBP 绘图依赖改为 `plot_solution` 内延迟导入；TSP/VRPTW 改用同序列的局部 `RandomState`；TSP 按实例缓存邻域矩阵，并对外部替换的测试数据保留首次计算回退。未修改任务文本、模板、种子、数据规格或 ACO 参数。
- **验证**：五任务契约、冻结契约、TSP/CVRP/OP/OBP/VRPTW 相关测试共 73 项通过；额外确认 TSP/VRPTW 局部 RNG 与原全局 RNG 生成序列一致，普通导入 OBP evaluator 不再加载 `matplotlib`。
- **保留项**：`generated_data_config.py` 中历史任务配置虽然当前主线不直接使用，但仍被冻结副本和分析/评测设施纳入运行时来源，暂不删除；VRPTW 每次候选调用的 `deepcopy` 是防止候选污染评测数据的安全边界，需要先做等价性和耗时对照后再动。

## 2026-09-23：V10.10 之后版本综合机制诊断

- **研究问题**：核对 V10.10 之后预算分配、算子提示和上下文机制为何长期不能稳定同时改善，并检验“多样性 + 合理预算 + 算子适配上下文 + 优秀提示”这一主张目前得到哪一层证据支持。
- **证据范围**：读取 V10.10 首版/复核批、V10.11 generic 及四组上下文对照、V10.12 完整 held-out、V11.0 完整批和 V11.1 manual 完整 held-out；汇总各路 `events.jsonl` 的 parent/frontier 改进、选择概率、提示规模，并核对运行 manifest 与 ACO 恢复记录。
- **核心判断**：V10.10 的 ESS-8、Pivot 均匀混合、短形成路径和较清楚的算子意图构成目前最均衡的基线，但仍缺少路线价值和代码行为核验。V10.11 证明上下文需求异质：TSP/VRPTW 偏短形成轨迹，CVRP 偏档案，OBP 偏轻上下文，OP 具有规模/阶段边界；由于当时上下文按批次作用于所有算子，不能据此直接证明算子级最优映射。
- **版本诊断**：V10.12 将两张 `Idea+fitness` 卡无差别追加给四个算子，held-out 中 TSP/CVRP/OP/VRPTW 没有稳定收益，只有 OBP 多数规模改善，证明增加参考卡不等于增加可执行证据。V11.0 的 code 聚合和最高分选择造成精英锁定，同时把参考扩展成长尾 Idea，形成窄父代/宽弱参考的耦合。V11.1 修正为节点级 softmax 和算子条件化上下文，取得 CVRP、OBP、VRPTW 局部折中，但全批事件显示 Fuse parent improvement 约 55.1% 而 frontier improvement 约 3.0%，说明它提高了局部改进而未解决前沿形成；TSP 和 OP 仍退步。
- **统一瓶颈**：预算仍主要按当前 fitness 分配，未表示分支的继续开发价值；上下文混淆 Idea、代码和形成关系的用途；算子提示仍是语言意图，未要求改变哪个实际决策、采用哪条证据并核验返回行为；四算子均分也不等于行为多样性。长期问题因此是“信息进入提示”没有稳定转化为“决策改变—路线继续开发—held-out 迁移”。
- **下一步选择**：先固定 V10.11 的 ESS-8 和全部提示，只比较 generic 短轨迹与 V11.1 的算子条件化上下文；若上下文配置通过，再在同一预算/上下文下替换带四个可核验字段的算子提示，最后才独立比较 ESS-8 与 V11.1 softmax。指标统一报告 parent/frontier 改进、行为排序变化、路线覆盖、继续开发比例和 held-out，停止条件沿用相同真实评价预算与既有 1% 泛化门槛。
- **权威文档**：[V10.10 之后版本综合机制诊断](../../02-实验结果/2026-09-23-V10.10之后版本综合机制诊断.md)。本次没有修改实验机制、任务契约或运行中的批次。

## 2026-09-23：V10.13 完整机制设计与实现

- **研究问题**：在不继续堆叠参考卡片、历史长度和局部控制器的前提下，把 V10.10 的预算纪律、V10.11 的上下文异质性证据和 V11.1 的算子条件化组织成一个可运行机制，直接修复路线机会、上下文用途和 Idea–Code 脱节。
- **设计判断**：下一次评价机会应先分配到设计路线，再分配到路线内节点。路线分数同时使用最好 fitness 百分位、平滑的父代/前沿生产率、未充分开发程度和停滞程度；路线 softmax 以目标 ESS 和 35% 均匀底线控制集中度，路线内保留 V10.10 的 ESS-8 质量偏好和 12% 节点探索底线。四算子继续各 25%，不把早期局部成功变成全局算子路由。
- **上下文与提示**：Refine/Tune 使用当前代码、最近最多 3 条形成边和路线证据；Pivot/Fuse 使用最多 3 个跨路线参考、行为摘要和 1 份主要参考完整代码。每次 Idea 强制写 Change、Evidence、Behavior、Preserve，算子提示分别限定单组件修正、最多两个有效参数、连贯主机制替换和一个 donor 计算接入；修复沿用原候选预算身份，不重新选择父代。
- **实现产物**：新增 `traceaad/v10_13/` 完整方法包；新增 `experiments/traceaad_v10_13/` 单路、冻结、批次启动、监控兼容入口和 25 路分配模板；新增 `tests/method/test_traceaad_v1013.py`；权威设计文档为 [`TraceAAD-V10.13-机制设计.md`](../../01-主线版本/V10.13设计演进.md)。行为签名只作为参考上下文证据，不作为质量或预算指标。
- **验证**：V10.13 方法测试 9/9 通过；与 V10.11/V10.12 相邻方法测试合计 41/41 通过；全仓库回归 308/308 通过；`py_compile`、单路运行入口、批次启动帮助、冻结入口帮助、冻结运行时 spawn 预检和分配模板生成均通过。未启动正式 V10.13 批次，未修改任务契约和未触碰正在运行的其他批次。
- **证据边界**：当前已验证的是代码逻辑、提示内容、解析协议、计数语义和 checkpoint 恢复；尚无 V10.13 的训练或 held-out 质量证据，不能把设计目标写成已实现的泛化结论。下一步只有在用户明确启动正式批次后，才按冻结机制和相同真实评价预算记录训练 best、重复均值与 held-out。

## 2026-09-23：按用户反馈重构 V10.13——删除路线语义与强制行为契约

- **纠偏**：上一条 V10.13 实现把 `parent_id` 形成树误用为路线类别，并以路线生产率、行为签名和 Change/Evidence/Behavior/Preserve 约束生成；这些机制已全部从当前 V10.13 删除。上一条记录描述的是已被本次设计替换的草案，不再是当前机制定义。
- **新判断**：树只保存形成关系；archive 的 evolutionary search 只使用真实 fitness 做机会分配；LLM 负责利用内部算法知识提出有随机性的候选。父代选择恢复为 V10.10 的 ESS-8，四算子各 25%，Pivot 保留 50% 均匀探索，Pivot/Fuse 各最多使用一个按质量与均匀成分抽取的可执行参考程序。
- **提示改动**：删除强制行为审计和多卡片协议。Refine 对应单程序改进，Tune 对应主要参数调整，Pivot 对应不同主机制，Fuse 对应 host/donor 协调组合；每个提示只描述动作和结果，模型只返回简短 Idea 与完整 Code。解析、真实评价、一次修复和 checkpoint 仍保留工程边界。
- **来源边界**：参考 EoH 的少数清晰进化操作和“LLM 生成—评价—population survival”结构；仅借鉴 BehaveSim 关于行为证据需要谨慎的启发，不引入行为模拟、行为距离或任务无关的行为簇机制。
- **实现与验证**：重写 `traceaad/v10_13/{selection,prompts,parsing,tree,traceaad}.py`，删除 V10.13 的 `behavior.py`；同步更新运行 CLI、README、设计索引和设计文档；重写方法测试为 7 项，V10.13 与相邻版本测试合计 40/40 通过，全仓库回归 306/306 通过；冻结运行时 spawn 预检和 CLI 检查通过。尚未启动正式 V10.13 批次，质量和 held-out 结论仍为空。
- **权威位置**：当前设计以 [`TraceAAD-V10.13-机制设计.md`](../../01-主线版本/V10.13设计演进.md) 为准；此前同日的路线/行为契约实现只保留在工作区变更历史中，不作为当前机制依据。

## 2026-09-23：V10.13 最终提示打磨、正式批次启动与监控核验

- **最终机制口径**：按用户反馈收敛为“LLM 创造候选 + evolutionary search 分配评价机会”。树只保存形成关系，不承载类别或多样性语义；四个算子各 25%；父代使用 ESS-8 质量分布，Pivot 保留 50% 均匀探索，Pivot/Fuse 各只带一份按质量与均匀成分抽取的可执行参考。上下文按算子用途分为形成历史或单一参考程序，提示只说明算法改进动作并要求返回简短 Idea 与完整代码，不加入行为契约、路线统计或多卡片规则。
- **实现与验证**：当前工作区最后一次全量回归为 307 passed；V10.13 方法、启动、冻结和监控入口均通过语法与 CLI 检查。冻结运行时为 `experiments/traceaad_v10_13/results/runtime_20260923_v1013/`，正式批次 manifest 为 [`batch_20260923_v1013.json`](../../../experiments/traceaad_v10_13/results/batch_20260923_v1013.json)，source identity 为 `e76d13cbc999c61fbabc2d82b56d33812d5d67f9517420c596aa7199d1937c44`。
- **启动设计**：五个任务各运行 4 个重复，共 20 路；固定分配为 server1 5 路、server3 8 路、server3b 7 路，20 个独立 tmux 会话均已创建。按唯一 `run_name` 去重核验后分配正确；进程统计中的额外条目来自 tmux 启动命令和任务 worker，不构成额外搜索路。
- **健康核验**：启动后 20 个 run 目录均生成 `run_config.json` 与 `tree_state.json`；监视器 `/api/versions` 与 `/api/state?version=20260923_v1013` 正常，快照显示 `total_runs=20`、`running_runs=20`、`queued_runs=0`、`stalled_runs=0`，已消耗约 982 次真实评价。20 路日志未发现 Traceback、ImportError、ModuleNotFoundError、TokenizationError 或端口占用错误。监视会话为 `traceaad_v1013_monitor`，地址为 `http://127.0.0.1:8765`。
- **证据边界**：批次刚启动，当前标记为 `partial`；训练 best、重复均值和 held-out 泛化尚未形成，不能据此判断 V10.13 的质量或优于旧版本。旧批次未停止、未修改，后续应待本批次自然完成后按相同真实评价预算汇总训练与 held-out 证据。

## 2026-09-23：V10.13 设计复审、历史归因纠偏与新机制建议

- **研究问题**：当前 V10.13 是否解决旧版本的预算、上下文和提示缺口；结构化输出、edit mode、工具 agent 和多样性机制应解决什么具体问题。先核对 GitHub 分支、批次 manifest、冻结源、运行进程、checkpoint、结果与研究文档；没有改动或停止正式实验。
- **关键纠偏**：V10.10 首批冻结实现已按算子提供形成证据：Refine/Fuse 使用源代码和实际 diff，Tune/Pivot 不带历史。不能将后来统一短轨迹的工作区实现归到首批结果，也不能将算子条件化上下文首次归于 V11.1。首批还含 manifest 明记的 context/capacity 热修段。另据原始 held-out，V10.12 CVRP50 略好于 generic，旧综合诊断“CVRP 三规模均未超过”不准确。
- **完整批次证据**：V11.1 Fuse 超父 3091/5614、超前沿 166/5614；有 donor 分数的超父候选 2981 个，仅 628 个超过双亲，即 78.93% 未超过 donor。应区分已有机制传播与创造性组合，不把 55% 超父率直接解释成创新。293 个有效 Fuse 缺 donor 分数，与修复路径清空字段有关，双亲分析明确限于可观测子集。
- **V10.13 partial 快照**：22:34 +08:00，20 路各 E102–E280。217 次因 Idea 超过 2000 字符拒收，均附有可通过 AST 语法解析的代码；不证明算法质量，只证明说明文字阻断评价。正常 Refine/Tune 的 1439 次调度中，510 次重复同一路同 parent/operator 的完整 prompt hash，支持“固定祖先上下文未纳入该父代的新试验”，不把随机重复采样一概称为浪费。
- **工程复现**：8 项既有方法/启动测试通过。临时目录、假 LLM/假 evaluator 的故障注入在 event 追加后、checkpoint 更新前中断；恢复后配置预算 2、物理假评价 3、评价编号 [1,2,2]。此为已复现的恢复一致性缺陷，不声称正式批次已遭遇该事故。真实服务和 evaluator 未调用。
- **建议与反例**：优先代码接受与 Idea 元数据解耦、阶段化恢复记账、局部试验记忆、Refine/Tune 可选块编辑；之后考虑少量独立 island/无父代探索、LLM 结构生成配合数值校准、按需证据工具。明确保留 E2-B' 的反例：18 锚点未观察到 Pivot 后强制 Refine 的延迟净收益，因此不恢复无条件 continuation。外部结构化生成、编辑、agent、岛模型先例已核对主来源，不作为 TraceAAD 效果证据。
- **历史边界补充**：V10.9 已有直接成败子代上下文、近期候选试用与实码差分；这些是重新审视的旧方向，不能包装成未试过的新机制。V10.1/V10.2 提示配对也未找到跨任务统一获益的文本组件。新建议强调按需证据读取和代码编辑接口，不无条件恢复旧版默认追加材料。
- **产物与下一步**：[V10.13 设计复审与下一步机制](../../01-主线版本/V10.13设计演进.md) 为本次权威结论；[审阅脚本](../../../experiments/traceaad_v10_13/review_20260923/audit.py) 与 [固定快照](../../../experiments/traceaad_v10_13/review_20260923/snapshot.json) 保存逐路边界/前缀哈希和复现。当前批次自然完赛后再做质量结论；机制修改进入独立冻结版本，文档中的提示草案未覆盖在线代码。

## 2026-09-24：V10.13-r2 按需证据、轻量输出与恢复修复

- **研究问题与授权范围**：按用户反馈落实 V10.13 审阅中的工程修复、预算耦合调整、提示/上下文优化、参考思想互补和 full/edit 输出。重点减少无关说明阻断代码、探索父代只拿到 Pivot、随机 donor 缺少用途判断和崩溃重复评价的不确定性；本轮不安排消融或正式实验，不实现数值优化器、语义分类器或 islands。
- **机制判断**：Idea+fitness 是低负担材料，但不是组件因果证据。历史默认不带原始 diff。Refine/Tune 保留短形成摘要，可读一次、最多两条同父有效子代试验；Pivot/Fuse 从质量、均匀、低曝光来源各抽一张不同实现的思想卡，随机顺序展示，LLM 判断互补性并可选读一份完整参考。完整代码、读到的实现与声明采用的 donor 分开记录，不用 AST 距离或 lineage 冒充算法类别。
- **实现**：父代统一 `0.875q+0.125U`，保留原版四算子均分下的边际探索总量；Idea 可缺失、可超长，新增可选 mechanism/change/transfer 字段。JSON full/edit 与旧代码输出兼容，edit 验证精确父代 SHA256 和唯一顺序替换，保留完整模块次序、不重新插回模板。修复只要求 full，不重复分配父代，修复后的 donor 声明不无条件继承；调用时间汇总上下文两轮，并区分预约调用数与已落盘调用数。
- **恢复修复与失败边界**：新增阶段化 pending、评价前预算预约、持久回执、节点/事件幂等投影和 writer lock。已有回执不重评，缺回执标记 uncertain_evaluation 并由启动器阻断；不能宣称外部调用无条件 exactly-once。截断尾部先保存 `.torn-*`，旧机制指纹在 journal 修改前拒绝。最后一次预算中的基础设施故障也会阻断，不能被误标 finished。
- **验证证据**：最终 `.venv/bin/python -m pytest -q` 为 **342 passed / 13.02s**，含 frozen task contract；覆盖 10 类落盘/阶段中断、第二轮读取恢复、缺回执阻断、writer 冲突、journal 缺失、精确 edit、执行失败修复、donor 暴露计数与归因。隔离临时源码副本通过 spawn 预检、run/launch/freeze/monitor 四入口帮助和 20 路 dry-run；没有调用真实 LLM 服务、没有启动新正式评价。设计文档相对链接与 `git diff --check` 通过。
- **运行边界**：2026-09-24 00:02:54 +08:00，只读核验原 `20260923_v1013` 批次 20 路均 partial，20 个唯一 run_name 仍有运行进程，各路 E170–E435；55 个冻结文件无变化，source identity 仍为 `e76d13cbc999c61fbabc2d82b56d33812d5d67f9517420c596aa7199d1937c44`。该截面只用于完整性审计，不作排名。运行时、manifest、原快照与后台任务均未改写或停止。
- **产物**：[V10.13-r2 机制设计](../../01-主线版本/V10.13设计演进.md) 为权威定义；源码在 `traceaad/v10_13/`，测试在 `tests/method/test_traceaad_v1013.py` 与 `tests/experiments/test_traceaad_v1013_launch.py`；[启动说明](../../../experiments/traceaad_v10_13/README.md) 使用新批次/会话名，明确不能用 r2 恢复旧版。
- **结论强度与下一步**：已证实接口、计数与恢复协议的测试行为，尚无 r2 算法质量/held-out 证据。后续若启动完整批次，保持五任务、四重复、E1000 和既有 held-out，另报 LLM 调用/token 成本；检查超父是否同时超 donor/双亲和前沿，以及读取后的候选是否得到有效继续开发。独立搜索不必先分类，但不能保证行为多样性；LLM 结构发现＋数值校准已登记为后续方向，本轮不实现。

## 2026-09-24：提交 r2、授权停止旧批次并启动同配置 20 路

- **用户要求与顺序**：先提交当前改动，测试通过后停止原 V10.13 并启动同样 20 路 r2，检查实际机制执行后再次提交。初次提交为 `697f1c7167193bdb08cebeb043d4d90130c15605`。提交后全量测试 **342 passed / 21.96s**；真实冻结运行时的哈希、语法、spawn 和四入口预检通过，三端点可达。
- **旧批次停止**：00:15:10 起对旧 manifest 精确指定的 20 个搜索主进程发送 SIGINT，全部生成 interrupted 摘要，00:16:11 确认无遗留搜索/评价子进程。旧批次 checkpoint 范围 E180–E451，标记 stopped_by_user/partial；保留原 manifest、停止前 checkpoint 和停止后工件哈希，未停止模型服务、未删除结果。证据位于 `experiments/traceaad_v10_13/results/transition_20260924_v1013_to_r2/`。
- **新批次配置**：`20260924_v1013r2` 于 00:16:31 启动五任务×四重复，seed 0–3，每路 E1000、8 个有效 root、8192 输出 tokens；任务配置和 LLM 配置逐路与旧版一致。后端仍为 5/8/7 路，独立 tmux 搜索会话与 `v1013r2_batch_watch` watcher 正常。冻结源身份 `37b7c52fb7f7954073e1626c726e7a0c424f3c0672ef352352a030653da93f0f`，56 个文件，未在批次运行中修改机制。
- **实际执行证据**：00:27:49 有界快照含 423 个结算事件、338 次结算评价、324 有效节点，逐一从原响应重建的代码与节点一致；评价 ID/回执/代码/edit/context/parent 机会检查无违规。成功 edit 41 个；54 轮读取中参考实现 9、非空局部试验 9、空局部试验 36；83 次已结算修复中 73 成功。新增只读 `experiments/traceaad_v10_13/audit_r2.py`，快照保留 checkpoint 与 journal 前缀哈希。
- **研究判断**：VRPTW rep1 candidate 10 完成选 reference 2、读取完整实现、edit 接入加性评分，训练 fitness 超 host 与 donor，但未超前沿；OBP rep4 candidate 22 读到局部试验后反而退步。TSP/OP 已见成功节点得到再次开发。上述证据证明执行链发生，不证明读取或融合的因果收益，也不构成版本排名。
- **发现的代价**：85/423（20.1%）事件 invalid_output，涉及 JSON 包装/转义、多代码块和代码语法；一次 parent hash 抄写失败被拒绝并修复。空读取产生额外模型成本。未把修复率或解析率当作质量；未因此中途热修冻结机制。后续若需修订，优先按真实失败原因处理输出负担和空读取。
- **最终运行核验**：00:30:43 的独立确认显示 20 路全部完成 8 个有效 root 并继续开发，进程 cwd 均为新冻结源，面板 running=20、queued=0、stalled=0；旧搜索会话为 0。旧工件停止后哈希未变化，新旧冻结源均无漂移。
- **权威记录与下一步**：[V10.13-r2 启动与机制执行检查](../../01-主线版本/V10.13设计演进.md)；[启动快照](../../../experiments/traceaad_v10_13/review_r2_20260924/startup_snapshot.json) 与 [运行确认](../../../experiments/traceaad_v10_13/review_r2_20260924/live_confirmation.json)。保持完整 E1000 批次，完赛后用既有 held-out 设施判断质量与泛化，同时报告额外模型成本；第二次提交仅包含审计与运行记录，不改变正在执行的方法。

## 2026-09-24：V10.13-r2 早期效果与运行诊断

- **研究问题**：用户观察 r2 不及预期；区分运行故障、输出接口损耗、同预算质量变化及实际算法决策停滞。读取 manifest、冻结源、20 路进程/日志/检查点和既有研究记录；没有停止、重启、热修或新增评价。
- **完整性**：01:22:11 +08:00 固定快照 20 路 E53–189，1,833 次结算评价，候选/评价/回执及代码重建无协议违规，冻结源无漂移。01:28:49 再次审计为 20 路 E58–205；01:32:40 面板 running=20、stalled=0，各路最近活动不超过 41 秒，无所查基础设施异常。
- **同预算证据**：相同 E50 的四重复训练 best 均值相对 r1，TSP -2.19%、CVRP -1.16%、VRPTW -0.61%，OP +2.62%、OBP +0.32%。OBP E150 只剩 +0.076%，两路仍停在首次评价。全部为 partial 前缀，不是最终排名或泛化结论，也不能归因某个单项改动。
- **接口缺陷**：638 次评价前拒收中，322 次内部 JSON 提案取出后通过现有语法/接口检查；72 次合法阶段的上下文请求因外围文字/围栏未被识别，转入修复。重放仅解析，不评价、不回填。E50 总模型调用 1,115→1,662，修复占实际评价 13.4%→35.6%；拒收本身不扣 E，不能直接解释同 E 质量下降。
- **机制缺口与正例**：217/544 局部读取为空；748/840 正常 Pivot/Fuse 没有显式展开参考实现。OBP rep2 node182 的 fragment bonus 上界 0.125，不足以改变整数 slack 上的 best-fit 排序，Idea 声称的作用未发生；大量同分节点令 ESS 目标提高且试验记忆分散。rep3 则出现 modular reuse/dead-space 路径并继续开发至 -728，edit 有成功实例，不能整体否定。
- **产物与下一步**：[早期运行诊断](../../01-主线版本/V10.13设计演进.md) 为权威结论，`experiments/traceaad_v10_13/review_r2_20260924/progress_*` 保留边界、哈希、同预算数据、响应重放和实码证据。下一修订优先唯一 payload 识别、正确路由 context、消除空读、减少 hash 抄写负担；提示关注实际决策而非增加字段/规则。当前批继续冻结运行，后续同预算复查与完赛 held-out 决定质量判断。

## 2026-09-24：按奥卡姆剃刀简化 r3 并准备授权切换

- **研究问题与授权**：用户要求针对已发现问题从简修正，测试后重启实验。r3 删除双轮 context 请求、三卡片挑选、低曝光加权和模型抄写 SHA256；避免用更多规则补救模型处理负担。修订前 r2 保持冻结运行。
- **机制改动**：默认输出完整 Python 加可选一句 Idea；保留可选 JSON exact edit，parent 绑定交给宿主，主动提供的旧 hash 仍验证。Refine/Tune 直接带已有至多两条 trial 摘要和短形成路径；Pivot 只看当前程序；Fuse 直接带一份参考实现，50% 质量/50% 均匀选择。父代预算和四算子概率不变，不加语义分类、多样性门控或 agent。提示精简为实际算法动作与可能改变选择的尺度。
- **解析兼容与证据**：唯一明确 JSON 可从外围说明/围栏识别；单个完整 Python 块保持原有解释，多份冲突 JSON 仍拒绝。真实 r2 固定前缀离线重放，1,726 个原有效程序代码全部一致，323 次旧拒收可解析；未发起模型调用/真实评价，未回填。一次重放曾发现旧单 Python 块与尾部 JSON 两份实现的优先级差异，已保留原代码优先解释并补测试。
- **验证**：最终全量回归 **350 passed / 22.08s**，含任务冻结契约、edit 与回执恢复；文档链接和 diff 空白检查通过。权威机制为 [V10.13-r3](../../01-主线版本/TraceAAD-V10.13-r3-机制设计.md)，[离线重放](../../../experiments/traceaad_v10_13/review_r3_20260924/r2_response_replay.json)保存证据。
- **下一步**：新批次冻结后按用户授权停止 r2 的指定搜索及 watcher，保留运行和路由身份，以相同20路配置启动 r3。启动后核查真实单轮生成、代码接受、预算回执与后台健康；算法效果仍需同 E 和 held-out 证据。

## 2026-09-24：r3 同配置20路已启动与初始执行核验

- **授权切换**：r3提交 `036b055a` 后冻结60文件，source identity `3dbf7069340b30a5a9f7b84262583dec27225b90d6cf3b65e0f38d9e385571b0`，入口/spawn/20路预检通过。01:43:49确认r2指定20路及watcher停止，全部interrupted，预约E73–246；原manifest、checkpoint、日志、路由和文件哈希完整保留，无旧搜索/评价进程残留。
- **未结算尾部**：r2有5次评价处于evaluating且无完整回执（CVRP四重复、OBP rep2各1）。保留预算预约与pending，不伪造结果、不自动重评；只能使用已结算前缀分析。其余15条停在scheduled。
- **新实验**：`20260924_v1013r3` 于01:44:06启动watcher，01:44:08为20路running；五任务×四重复、seed0–3、E1000、8roots、token及LLM/task_eval配置逐路与r2一致，后端仍5/8/7。旧、新冻结源均无漂移，原监控可显示新批。
- **实际检查**：01:46:43固定快照20路E2–14，88次模型调用对应88次成功评价，无拒收/失败/修复/额外读取；full86/edit2，已见Fuse3、Pivot2、Refine2，参考代码和回执链路检查无违规。edit与Fuse早期仍有同分/无AST变化实例，不把执行成功当作算法质量提升。较慢任务仍初始化，局部trial尚未在该快照出现。
- **权威记录**：[r3简化与重启](../../01-主线版本/V10.13设计演进.md)；原始迁移证据 `experiments/traceaad_v10_13/results/transition_20260924_r2_to_r3/`，启动和协议快照 `experiments/traceaad_v10_13/review_r3_20260924/`。后续保持冻结至E1000，按同E前缀和held-out判断，失败/缺失单列。

- **结束前补充**：01:47:53协议快照扩展到130次结算评价，全部有效，full127/edit3，四个开发算子均出现，1次inline trial实际进入提示；无读取调用、修复或协议违规。01:48:26再次确认20路进程及冻结cwd，面板running=20/queued=0/stalled=0，无所查基础设施异常，旧工件哈希不变。仍不据此推断算法质量。

## 2026-09-24：评价预算口径成文、术语清理与未完成批次删除

- **研究问题与授权**：用户确认预算口径（解析拒收不计、凡是调用 evaluator 即计一次评价）并要求成文统一，同时报告评价调用次数、有效程序数、模型调用数与 token 三类成本；授权删除 V10.13 前两个未完成批次，r3 为标准版本。
- **口径核对**：以现行代码核对失败分类与计费（解析拒收/评价失败/基础设施异常/uncertain evaluation），并核对基线实现：EoH 只计拿到合法分数的样本，ReEvo 按模型采样尝试计费，ShinkaEvolve 与 MCTS-AHD 和 TraceAAD 同为"评价执行即计费"。修复不是额外亏损：失败那次评价由失败本身消耗，修复的边际成本与放弃修复、调度新候选相同，区别只在父代机会已消耗与错误信息可见。V9.16 及更早的宽松记账正是 V10.9/V10.10 统一口径的原因。
- **新文档**：[评价预算与成本口径](../../02-实验结果/00-评价预算与成本口径.md) 为权威定义：标准术语表、记账规则、成本报告三数、基线口径对照。docs/README.md 证据口径节挂链。
- **术语清理**：主线版本文档（V10.13-r3、V10.13-r2、V10.12、V11.0）中的 E1000/E500/E100~E900 等记号替换为"每路 N 次评价预算/第 N 次评价"；并按用户要求删除机制文档中的档案存放叙述与证据等级说教（"保留在档案中""不声称 X""代码是权威实现"等 12 个文件、28 处），保留仍改变读者行为的规则句与 r2 的版本边界声明。
- **批次删除**：确认无 r1/r2 残留进程后执行——停止 r1 监控会话，删除 `20260923_v1013` 与 `20260924_v1013r2` 两个未完成批次的全部运行数据；默认 tmux 服务器承载 r3 会话，未动。
- **r3 状态**：删除前后确认 `20260924_v1013r3` 正常运行（逐路 journals 持续更新、watcher 会话存活），未受清理影响。
- **下一步**：r3 完赛后的版本比较按口径文档并列报告三数；跨方法比较表需注明各基线预算单位差异，不得直接用名义"每路 1000 次"对齐。

## 2026-09-24：科研文档表达诊断

- **研究问题**：检查主线版本、实验结果和研究认识文档中“冻结、可证伪、预期、边界、暂不、不做”等表达是否遮蔽了机制、结果和结论。
- **诊断证据**：只读审阅 V10.10–V11.1 主线与综合诊断；`docs/01-主线版本`至`docs/04-研究认识与构想`中“冻结”出现 182 次、“边界”227 次、“尚未”95 次、“假设”188 次；V11.1 机制文档中选定的负向设计表达至少 18 处，并单列“明确不加入的机制”。
- **判断**：问题不是个别词，而是文档体裁混杂。机制定义反复被“不做什么、保持什么、当前还不能声称什么、后续如何检验”打断；实验结果、证据强度和项目运行管理被写进同一叙事层，导致读者需要自行重建“机制→结果→发现/结论”。V11.1 设计文档还没有吸收已完成的 held-out 结果，设计结论主要重复机制规格。
- **保留项**：运行副本、固定配置、任务契约和算法中的实际选择阈值仍是复现或机制事实，不能机械删除；后续应集中到实验设置/复现附录，主叙事改用直接事实、测量结果和结论。
- **权威证据**：[V10.10 之后版本综合机制诊断](../../02-实验结果/2026-09-23-V10.10之后版本综合机制诊断.md)、[V11.1 机制设计](../../01-主线版本/生成协议与上下文演进.md#v11-1)、[V10.12 与 V11.1 held-out 测试](../../02-实验结果/2026-09-23-V10.12与V11.1-heldout测试.md)。本次未修改主线文档、实验机制或运行批次。

## 2026-09-24：主叙事文档改写

- **改动范围**：更新 V10.10 之后综合机制诊断、V11.0 机制与实验分析、V11.1 机制设计，以及两份当前研究定位文档；历史版本快照、事故诊断和运行流水保持原样。
- **改写结构**：综合诊断按每个版本的“机制—实验结果—发现与结论”重排，补入 V11.1 held-out 与 parent/frontier 结果；V11.0 和 V11.1 删除研究边界、证据边界、可证伪预期、明确不加入等防御性叙事。
- **表达处理**：把“不能据此声称、尚未证明、暂时最好”等元话语改成直接的观测、测量缺口和下一步实验；保留模型端点、计数规则、运行状态等复现事实。
- **核验**：`git diff --check` 通过；三个目标文件存在；目标防御性标题和短语未残留。未修改任务契约、实验机制或后台运行批次。

## 2026-09-24：r3 运行中现象观察文档（partial 快照）

- **研究问题**：用户要求脱离机制词汇，单从现象层面记录 r3 批次表现（解析失败集中在什么、修复成功率、比上一批好坏等），并指出此前汇报混入了机制耦合的因果措辞（如"拒收"）。
- **产物**：[r3 实验观察](../../02-实验结果/2026-09-24-V10.13实验观察.md)，快照固定 11:32 +08:00（9/20 完赛）。六个现象：解析失败 2.9%（62% 为多个/不明确代码块，r2 为 25.8%）；运行失败 7.4%（TSP 超时为主）；修复成功率 78%、占评价 8.2%（r2 35.6%）；22.7% 评价花在最后改进之后，OP-2/OBP-1/OP-3 严重早停；OP-2 停滞段 99% 代码不同、67% 落在最优 1.0 内、最优分被并列未超越；请求:评价 1:1（r2 1.66:1）。
- **边界**：批次仍在运行，数字随进度变化；全部为训练侧观察，无 held-out，不构成质量结论。机制归因不在本文范围。

- **机制清单**：应用户要求建立 [V10.13 机制清单](../../04-研究认识与构想/2026-09-24-V10.13机制清单.md)（M1–M53），按搜索循环/预算分配/初始化/算子/参考与试验/上下文/输出/解析/错误处理/持久化/经验组织/不存在机制十二组，粒度到单个算子与单条规则，供逐项重审引用。

## 2026-09-24：V10.10 主线文档全量改写

- **研究问题**：修复 V10.10 文档中首版、修订版和第三版混写，机制规则、实验结果与研究结论没有分层，以及“边界/不做什么/不能声称什么”类表达过多的问题。
- **改动**：重写 [V10.10 机制设计](../../01-主线版本/TraceAAD-V10.10-机制设计.md)，按版本身份、共同搜索流程、各版机制、实验结果、研究发现和复核路径组织；保留父代与 donor 分配、形成路径、一次错误修复、代码优先解析、目标函数生成等实际机制。同步纠正复核批结果页中 OP 训练 best 与 held-out 方向的表述，使其与数值一致。
- **结论**：首版和修订版分别对应独立的 15 路完整批次；首版平均名次 2.133，修订版 2.867。修订版改善 TSP、VRPTW 和 OBP 多数测试规模，同时使 OP、CVRP 回落；第三版已有机制记录，但本页没有独立质量批次结果。
- **核验**：目标文档 119 行、8 个主体章节；内部结果页、实现和实验说明链接均存在；目标防御性词组未残留；`git diff --check` 通过。未修改任务契约、实验代码或运行中的批次。

## 2026-09-24：M1 评价次数口径逐项复核

- **研究问题**：M1“解析失败不计、实际送评即计一次”是否为其他方法通用做法，是否需要调整定义。
- **证据与判断**：复读[评价预算与成本口径](../../02-实验结果/00-评价预算与成本口径.md)，核查本地实现：`baselines/mcts_ahd/mcts_ahd.py:238–285` 解析失败提前返回、评价返回后计数；`baselines/shinka_evo/shinka_evo.py:350–399` 补丁失败不计、评价返回后计数，但评测调用直接抛异常时不计；`baselines/eoh/eoh.py:270–288` 无效分数在计数前返回；`baselines/reevo/reevo.py:150–250` 解析失败也计数。因此只能支持“有其他方法采用相近口径”，不能支持“所有方法统一采用”。这些是本地实现事实，本次未核查各方法官方仓库版本。
- **改动与核验**：[机制清单](../../04-研究认识与构想/2026-09-24-V10.13机制清单.md) M1 改名为“评价次数统计”，明确重复与修复只有再次送评才增加评价次数，并说明模型成本单独记录；规则含义不变。仅修改文档，未运行实验或操作后台进程；检查目标文本及文档差异格式。
- **后续选择**：保留 M1 作为本项目预算定义；跨方法比较继续按实际评测调用核对，不能仅凭相同的名义 sample 上限认定同预算。若需声称领域普遍做法，需补查官方实现及其版本。

## 2026-09-24：M1 升格为项目评价预算规范

- **研究问题与决定**：用户将 M1 确认为 TraceAAD 的基础约定，并要求规范化记录；沿用现有[评价预算与成本文档](../../02-实验结果/00-评价预算与成本口径.md)作为唯一权威位置。
- **规范改动**：区分实际调用与搜索评价预算；候选失败计数，解析失败未送评不计，确认的基础设施故障不扣搜索预算但保留证据，未决异常保留预约并阻断。补充故障核定、重评关联和唯一评价号要求，同步机制清单 M1 与文档索引。
- **实现证据与缺口**：读取 `traceaad/v10_13/traceaad.py:293–323` 及阻断分支，当前实现提前增加 `evaluations_used`，基础设施异常后不释放计数。规范已确认，代码尚未对齐；保留历史及运行中冻结批次的原口径。本次仅改文档，未运行实验、修改任务契约或操作后台进程。
- **核验与下一步**：检查文档格式与相对链接；下一次采用新规范的实现工作须分离唯一评价号和预算结算，验证候选失败计数、故障不扣预算与未决预约阻断，再决定新批次是否采用。无需为本次规范成文启动算法质量实验。

## 2026-09-24：V10.13 实现对齐 M1 基础设施故障预算规范

- **研究问题与授权**：用户要求将刚确认的 M1 规范落实到 V10.13。需要消除“故障不扣预算”与原实现计数的差异，并防止释放预算后复用评价号或恢复时重复结算。本次是研究仪器记账修正，不检验算法质量。
- **实现**：`traceaad/v10_13/traceaad.py` 将单调递增的评价预约/ID 与搜索预算分离，从回执和核定记录推导已结算评价、未决预约和基础设施故障数。`prepare_error` 和显式 `infrastructure_error` 不扣搜索预算；候选失败照计；未分类 `evaluation_error` 和缺回执保留预约并阻断。增加 `confirm_infrastructure_failure(evaluation_id, evidence=...)`，只允许核定最新已提交的失败候选，保留原始证据，核定后仍阻断。`storage.py` 增加独立 `evaluation_resolutions.jsonl`。修复预算上限处的故障在再次恢复时可能绕过阻断的问题。
- **持久化与范围**：新增预算策略指纹，持久化协议升级到 v3；旧 checkpoint 拒绝由新代码恢复。核定记录及回执可幂等重放。未修改任务、基线、冻结批次或服务进程。新字段和核定入口统一记在[预算规范](../../02-实验结果/00-评价预算与成本口径.md)，机制清单与实验 README 同步实施状态。
- **核验**：`.venv/bin/python -m pytest -q tests/method/test_traceaad_v1013.py tests/experiments/test_traceaad_v1013_launch.py`：73 passed（9.91s）。覆盖候选各类失败、真实 SecureEvaluator 准备异常、故障回执/检查点/提交边界崩溃恢复、核定落盘后崩溃、未决预约、唯一评价号、成功评价禁止免责及重复恢复阻断。文档链接与目标文件 `git diff --check` 通过。
- **运行状态与证据边界**：只读核查 `results/batch_20260924_v1013r3.json`（13 finished、7 running，仍为 partial）及 `results/runtime_20260924_v1013r3/runtime_manifest.json`；60 个冻结文件哈希全部匹配，当前 r3 工作进程使用该冻结目录。未停止或迁移实验。测试使用假模型和小评价器，不构成训练或泛化质量证据。
- **后续选择**：下一批使用新的批次名冻结工作区代码；当前批次继续原口径。出现未分类异常时，先诊断再核定；缺回执仍需恢复事实证据。核定接口仅结算预算，故障修复后的重评/恢复须另行确定安全检查点，不自动继续搜索。

## 2026-09-24：精简 M1 文档状态说明

- 按用户要求，删除机制清单、预算规范开头和实验 README 中重复的“工作区已对齐、冻结批次不变”等状态说明；版本适用信息集中保留在预算规范第 3 节。规则及实现不变，既有工作流水保留历史证据。
- 核验目标文档差异格式；后续逐项讨论直接描述机制，涉及具体恢复或跨版本比较时再引用适用规则。

## 2026-09-24：M2 归入初始化配置

- 按用户逐项审查意见，将机制清单 M2 从“搜索循环与预算”移至“初始化”，改为“初始个体数量：初始化 8 个评价有效的个体，加入档案”。保留编号便于引用，代码与数量不变；后续在初始化部分讨论数量选择。文档差异格式检查通过。

## 2026-09-24：合并 M1 与 M3

- 按用户意见，将固定每路 1000 次评价、包含初始化与开发、预算耗尽结束及评价计数合并为 M1“搜索评价预算”，删除独立 M3，其余编号保留。故障阻断的具体处理引用统一预算规范。仅整理机制清单，未改代码；文档差异格式检查通过，后续继续逐项审查。

## 2026-09-24：明确 A 节的预算主题

- 按用户意见，将 A 节改为“进化搜索评价预算”，M1 改为“预算额度与计数规则”。用“每次独立搜索”明确预算归属，用“初始化和后续进化共同使用”明确覆盖范围，并将“送评/重评”展开为评价动作。规则与实现不变，文档差异格式检查通过；后续审查继续沿用该术语。

## 2026-09-24：机制清单按六个研究主题重组

- **问题与调整**：用户要求按预算→初始化→预算分配→算子设计→输出与解析→错误处理组织，合并零散重复项。重写[机制清单](../../04-研究认识与构想/2026-09-24-V10.13机制清单.md)，保留原编号便于持续审查。
- **合并对应**：M6/M8→M5，M9→M7，M11/M39→M38，M26→M17，M23→M22，M21→M20，M27→M24，M29→M28，M34/M37→M30，M32/M33→M31，M36→M35，M41→M40，M43→M42，M48/M49/M50→M47。M51–M53 改为算子设计部分的范围说明。参考与上下文归入各算子，持久化/日志/恢复归入第六部分的运行保障，明确不作为搜索策略。
- **证据与边界**：核读 `traceaad/v10_13/prompts.py`：公共证据说明也用于初始化和 Pivot，Tune 子代优先仅在 Tune 中生效；据此消除旧清单措辞歧义。本次不改变机制参数、代码或实验，不对算法质量作判断。
- **核验与下一步**：检查六部分顺序、编号唯一性、合并项覆盖与相对链接，运行文档差异格式检查；下一步先审查初始化数量、输入和生成指令，再进入预算分配。

## 2026-09-24：删除偏离主线及重复的分析文档

- **问题与授权范围**：用户明确要求删除无用的 BehaveSim 分析，并确认清理已被后续报告替代的临时诊断、讨论稿和提示词快照；保留版本机制定义、正式结果、文献与工作流水。
- **执行结果**：删除 52 份 Markdown，文档总数由 275 降为 223。包括算法行为几何目录中的 7 份 BehaveSim 分析、研究认识目录中的 3 份 BehaveSim 分析、38 份历史提示词快照及索引、4 份重复讨论/设计复述。四份为 `2026-09-06-V10.6-机制讨论稿.md`、`2026-09-08-V10.7-采样轨迹讨论.md`、`V9.19-新版机制审视.md`、`V9.22-机制设计与实现分析.md`；对应机制定义保留在 `01-主线版本/`。提示词以对应版本实现和实际请求为准。
- **引用整理**：删除机制验证总览中的 BehaveSim 分析入口；移除 V10.7、V9.21 机制文档及 BehaveSim 文献页对已删讨论/复审的指向。历史流水的四处链接改为带清理标注的文字，原研究结论与工作记录保留。
- **保留与证据边界**：仍有独立证据的旧诊断未仅凭日期删除。文献原文阅读、版本定义、正式结果及原始 JSON 数据保留；未操作实验目录、代码或后台进程。待删文件含已有未提交修改，删除前的实际内容保存于 `/tmp/traceaad-doc-cleanup-20260924-before.tar.gz`，清单为 `/tmp/traceaad-doc-cleanup-20260924-plan.json`，便于本次审阅恢复。
- **核验与后续选择**：扫描仓库可检索的 Markdown，未残留指向本次删除文件的链接；删除清单与文档差异格式检查通过。下一步旧诊断若要继续缩减，应先确认其具体证据已有权威承接位置，避免只因版本老而删除仍有用的机制证据。

## 2026-09-24：第二轮文档精简候选审查

- **问题**：用户认为剩余 223 份文档仍多，要求识别过时内容及可合并文档。本次盘点与抽读，未新增删除。
- **数量与证据**：主线版本 31、实验结果 53、机制验证 11、研究认识 13、文献与基线 98、成果与报告 16、总索引 1；文献与基线中单篇阅读及索引 72 份。抽读研究认识的主张和结构、基线方法页与汇总、V10.10 批次对照及机制页、V11.0 两篇分析，核查重复与建议时效。
- **可直接精简项**：EoH/ReEvo/MCTS-AHD/PathWise/CALM 五份方法页均只有重复的最终表格，每页 46 个小数值全部已存在于基线汇总及主实验表，可保留汇总后移除方法页。研究定位五页可合为一页，上下文与五任务收益两页可合并，两篇训练方向可合并；Idea 长度的“超长即修复”建议已不适用于当前协议，应只保留文献事实并归入输出协议调研。
- **合并候选与边界**：V10.13 原版复审、r2 启动检查、r2 早期诊断和 r3 简化重启可整合为演进记录；V11.0 跨任务边界与机制分析可并为一页，纠正将联合改动收益单独归因参考上下文的措辞；V10.10 版本对照可并入机制定义的批次表。旧版本定义与诊断可进一步按阶段整理，但独立证据、精确参数及批次身份需逐项承接，不能仅按日期删除。
- **下一步选择**：优先合并重复认识与表格，随后按阶段整理历史机制；单篇文献与工作流水不作为压缩数量的首要对象。预计第二轮可先减少约 15–20 个独立页面；更大幅度的阶段归并尚需全文核查，不宣称已完成。

## 2026-09-24：按研究主题凝练文档

- **研究问题与授权**：用户要求继续压缩 223 篇文档，允许将单篇文献按主题合并、减少索引，并让正文重点讨论观点、分析与推论。本轮将同一问题的重复论述收敛到一个长期位置。
- **产物**：文档由 223 篇减为 76 篇，替换 165 篇原文、新增 18 篇归并文档，净减少 147 篇。追加本条前字符总量由 1,223,329 降为 489,186，约减少 60%。文献与基线 98→9，研究认识 13→4，主线版本 31→9，实验结果 53→26；机制验证 11 篇、成果与报告 16 篇保留。
- **组织与判断**：71 篇精读归入六个主题，另设反馈与实现协议，共七篇主题文献；专题调研并入相应论述，保留实验设置与基线两份事实对照。原论文总表的延伸阅读分散到对应主题，重复条目的原文入口合并到精读摘要。研究认识形成研究主线、任务收益与上下文、模型学习三篇长期文档，加当前机制清单。旧版本与诊断按四个阶段归纳，保留主要变化、独立观察、紧凑参数摘录和工件路径；当前与主要参照版本继续单列。
- **分析修订**：V11.0 两篇分析合并，围绕父代集中与参考分散讨论联合变化；V10.13 历史设计、r2 诊断与 r3 衔接合并，保留 partial 时间、离线重放范围及五次未结算预约。重复的 V10.10 版本对照回归机制定义。文献正文删除重复的防御性限定，以方法联系和可研究的推论为主。
- **核验**：71 个论文锚点逐一对应；原总表的 129 个不同原文链接均有主题承接（arXiv abs/html 按同一论文识别）；删除路径都有迁移目标，未新增失效本地链接。正式版本结果、已有工作流水及当前机制清单的正文通过前后比对，变化仅为引用迁移；机制验证总览补上直接实验入口。仓库外层两份实验 README 同步修正链接。未修改代码、任务契约、原始实验工件或后台进程，本轮不运行算法测试。
- **复核与恢复**：本轮前实际文档快照为 `/tmp/traceaad-doc-condense-20260924-before.tar.gz`，原文内容 `/tmp/traceaad-doc-condense-source.json`，迁移表 `/tmp/traceaad-doc-condense-map.json`，校验摘要 `/tmp/traceaad-doc-condense-validation.json`。阶段回顾承接研究判断与关键配置，完整旧文可以从快照复取；这批临时备份用于本次审阅恢复。
- **下一步**：从当前机制清单继续讨论初始化数量、初始化输入和生成指令，再讨论预算分配；后续认识补入长期主题，具体新实验仍记录到对应结果或机制验证文档。

## 2026-09-24：合并版本结果，删除工程诊断

- **问题与授权**：用户要求 V10.10 不再按正式批/复核批分文档，次要版本并入历史，并删除耗时、失败等 bug 类型诊断。
- **完成内容**：76 篇降至 60 篇。V10.10 两份结果改为一份，按任务并列 V10.10/V10.10R 的逐重复数据、均值与标准差，统一设置与结果认识。V10.1–V10.9（含 V10.7R）十份结果并入历史版本，保留批次、原始路径、预算例外和逐路数值。V10.12、V11.1 机制并入生成协议与上下文演进；V10.12 旧故障期快照由已完成的 V10.12/V11.1 测试报告承接。
- **删除**：移除 ACO worker 导入失败诊断、MEoH/CVRP 耗时诊断两份独立文件。已有工作流水保留历史故障与恢复记录，实验 README 直接指向原始恢复资料。方法诊断中的机会分配、上下文研究与当前 V10.13 观察继续保留。
- **判断修订**：V10.10 旧结果正文曾把 OP prize 下降写成改善；合并文按数值统一为训练与 held-out 同向回落，并删除重复排名评论。V10.1 CVRP rep2 的 E815 截断仍明确标注；V10.12 的计划三重复与实际四重复分别说明，测试中候选自身导致的失败及 n=3 标注保留。
- **验证**：276 行合并结果逐行比对，保持原有精度；历史表原有行、主实验表和当前机制清单保持。删除与合并路径均已处理引用，无新增失效本地链接；文档差异格式检查通过。仅修改 Markdown，未重新评价、修改任务契约或操作实验进程与原始工件。
- **复核路径**：本轮前快照 `/tmp/traceaad-doc-round3-before.tar.gz`，原文 `/tmp/traceaad-doc-round3-before.json`，迁移与删除记录 `/tmp/traceaad-doc-round3-report.json`。下一步从当前机制清单的初始化讨论继续；次要版本后续直接补入历史，工程故障记录在工作流水。

## 2026-09-24：建立 AAD/AHD 初始化专题文档

- **研究问题**：初始化是否是 AAD/AHD 的共同起点，各代表方法如何产生首批候选，以及首批生成数、初始有效数和后续种群容量是否相同。
- **产物**：新增 [`AAD 搜索初始化`](../../05-文献与基线/AAD代表方法与机制.md)（现已改写为代表方法总览），覆盖 FunSearch、EoH、ReEvo、MEoH、HSEvo、MCTS-AHD、PathWise、RoCo、CDEoH、CALM、AlphaEvolve、ShinkaEvolve、TurboEvolve 和 DeltaEvolve；`docs/README.md` 增加文献入口。
- **核心判断**：代表方法都需要某种可评价起点，但初始化不只有“先生成 N 个个体”一种形式。批量生成后筛选、顺序参考生成、单种子/任务骨架启动分别对应不同的搜索偏置；文献中的种群容量不能直接当作首批评价次数。TraceAAD 当前“顺序参考、8 个评价有效根、全部进入档案”属于具体设计选择，不是领域统一标准。
- **证据路径**：使用本地基线实现 `baselines/eoh/eoh.py`、`reevo/reevo.py`、`mcts_ahd/mcts_ahd.py`、`pathwise/pathwise.py`、`calm/calm.py`、`shinka_evo/shinka_evo.py`，并核对 `AAD实验设置对照.md` 的论文配置。没有把缺少明确首批数量的方法补成确定数字。
- **后续选择**：初始化实验应固定总评价预算和后续搜索机制，比较独立生成 8 个有效根、顺序参考 8 个有效根及超量生成后筛选 8 个的差异；记录初始化消耗、算法差异、前沿曲线和 held-out。

## 2026-09-24：删除 BehaveSim 实验目录

- **授权与范围**：按用户要求删除 `experiments/behavesim_population_calibration/` 和 `experiments/behavesim_archive_validation/`，包括实验脚本、配置、缓存、行为 profiles、距离矩阵和汇总结果，共 928 个文件、936,278,543 字节（约 936 MB）。
- **删除前检查**：读取两目录 README、配置、profile_summary 和 summary；未发现独立 manifest/status 文件。进程命令扫描未发现对应任务，可访问的进程 cwd 与打开文件未指向两目录；其他用户的部分进程详情无权读取。受版本管理文件在两目录外没有引用其模块或路径。
- **结果与后续**：目录删除完成，未停止任何进程，未修改主线方法与任务契约。相关研究分析此前已清理；后续围绕当前机制清单继续研究，不保留这两组行为几何实验入口。

## 2026-09-24：将初始化专题改为 AHD/AAD 代表方法总览

- **研究问题**：用户需要先理解代表性方法及其机制，再讨论 TraceAAD 的具体初始化设计；原专题只比较初始化，覆盖范围过窄。
- **产物与依据**：将原专题改写为[代表方法与机制](../../05-文献与基线/AAD代表方法与机制.md)，按程序进化、机会分配、历史利用、多样性、设计对象、执行反馈与模型学习七条路线介绍代表工作，链接现有主题文献的论文条目；初始化缩为一节。更新 README 入口和旧流水链接。
- **结论与边界**：方法主要区别在设计对象、生成动作、信用分配和经验使用；种群大小只是其中一个配置。总览说明机制与研究取舍，不作为同预算性能排名；当前仍缺跨方法统一预算、任务和 held-out 的直接比较。
- **下一步选择**：以此总览为讨论 M2、M12–M14 的共同背景；若检验初始化方案，固定总评价预算和后续开发规则，比较顺序参考、独立生成与超量筛选对后续改进及泛化的影响。

## 2026-09-24：删除 AAD 实验设置对照表

- **取舍**：按用户要求删除 `05-文献与基线/AAD实验设置对照.md`。它按论文逐项罗列任务、模型、预算及对比方法，现阶段机制讨论不需要独立维护这份配置表。
- **承接**：代表方法与机制留在[方法总览](../../05-文献与基线/AAD代表方法与机制.md)及主题文献，论文配置从其原文核对；本仓库正式结果见[对比方法基准](../../05-文献与基线/对比方法基准.md)和主实验结果。README 与方法总览的失效链接已移除；旧流水中的引用作为历史留痕保留。
- **后续选择**：需要比较具体初始化数量或论文预算时，按所讨论的方法逐篇核对原文单位，再决定是否整理到相应机制条目；不预建大表。

## 2026-09-25：移除源码冻结运行时与哈希门禁

- **问题与决定**：源码复制、`runtime_manifest.json` 哈希核验和冻结目录启动增加了启动与恢复复杂度，并把运行入口绑定到副本目录。按用户决定，后续实验直接从仓库工作树运行，由 `uv` 管理环境和执行 Python 模块。
- **实现**：删除 V10.10、V10.11、V10.12、V10.13、V11.0、V11.1 和 B/C 的 7 个 `freeze.py`；启动器、手工分配、排队与恢复路径不再创建或校验运行副本，不再写入或比较 `runtime`、`source_identity`，也不再设置副本 `PYTHONPATH`。共享 `LaunchItem` 及独立启动脚本统一使用 `uv run python -m ...`，tmux 从仓库工作树启动。批次 manifest、任务/seed/预算配置、检查点、评价回执和 routing history 继续保留。
- **兼容清理**：V10.12、V10.13 和 V11.1 的单路入口取消冻结目录门禁；`equivalent_backends.py` 保留服务路由恢复，删除冻结 manifest 适配入口；优先队列改为等待后执行显式 `uv run python ...` 命令。V10.13 只读审计不再加载或核验冻结源码，仍对事件、调用、回执和检查点内容生成证据摘要。版本 README 与测试同步更新。
- **证据与边界**：新批次不再通过 manifest 证明精确源码身份，代码变更期间的可复现性依赖版本控制和研究记录；这是移除源码冻结后的明确取舍。任务冻结契约、不可变 dataclass、实验数据快照和用于内容寻址/证据摘要的哈希用途未改，它们不参与运行时源码门禁。本次未修改任务、搜索机制、评价预算或已有结果目录。
- **核验**：受影响的 10 个模块入口均可通过 `uv run python -m <module> --help` 加载；`uv run python -m compileall` 通过；`uv run python -m pytest -q tests/experiments` 为 57 passed；`git diff --check` 通过。V10.10 启动器中指向不存在 V10.8 模块的冗余导入同时移除，工作树入口可正常加载。
- **运行状态**：`20260924_v1013r3` manifest 为 19 finished、1 blocked，当前无对应搜索 tmux 会话。PID 957961 是保留的 tmux server，当前只承载监控会话；未停止、迁移或修改任何后台实验。已有 `results/runtime_*` 作为历史证据保留，不再由新代码创建。
- **后续选择**：新实验直接使用新批次名从工作树启动；若研究结论需要精确源码复现，应在启动记录中明确对应提交或差异，而不是恢复运行时复制与哈希门禁。

## 2026-09-25：移除任务冻结契约与内容摘要校验

- **研究问题与决定**：用户进一步确认任务文本、模板、数据、种子和对比方法不应由不可修改契约约束，日志、提示、代码、检查点及实验快照也不应依赖内容摘要来验真、判重或恢复。后续通过显式配置、完整内容、运行/候选/节点 ID、普通状态和版本控制记录实际实验口径。
- **实现**：删除 `tests/task/test_frozen_task_contract.py` 及 AGENTS.md 的对应强制条款；主实验配置文档改名并删除不可修改声明。V10.5–V10.13、V11.0/V11.1 移除任务、源码、模板、提示、代码和编辑基准摘要字段；token 缓存、代码聚合和重复检查分别改用完整文本或 AST 表示。V10.13 edit 直接对当前父代应用唯一精确替换，不再接收或核对摘要字段。
- **实验工具**：提示探针、E1/E2 快照与缓存、V10.10/V10.13/V11.0 诊断和恢复脚本不再生成或比较内容摘要。快照保留截点、路径、ID、状态与完整记录；缓存使用运行名、节点 ID 或显式文本 ID。删除两份只服务于旧运行副本核验/恢复的 V10.12 事故脚本。
- **证据边界**：已有 `results/`、`raw/`、历史诊断 JSON 和早期工作流水不改写，避免伪造过去的运行记录。包管理器锁文件中的上游制品校验值、Python 不可变数据类和对象映射所需的语言级散列不属于实验冻结或证据校验机制。
- **核验**：活动源码、测试和当前机制文档中未检出摘要算法、相关字段或任务冻结契约引用；`uv run python -m compileall -q traceaad experiments tests docs/03-机制验证`、`git diff --check` 通过，全量测试为 **354 passed / 14.58s**。V10.13-r3 manifest 仍为 19 finished、1 blocked；只发现既有 monitor 进程，未停止或修改后台任务。
