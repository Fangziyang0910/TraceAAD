# V10.13-r3 简化与同配置20路重启

按用户“从简修正，修好后重启”的要求，r3 已提交、通过350项测试并启动同配置20路实验。双轮读取、三张参考卡和模型抄写哈希被删除；模型默认直接写 Python，可选 edit。最早进入开发阶段的固定快照中88次评价均有效、没有额外读取和修复，证明简化接口可执行；尚不能据此判断算法质量提升，早期 OBP 仍可出现同分修改。

## 修订与验证

- 代码提交：`036b055afb8c6ae67ba98ed69fa490f5c73b5e6e`。
- [r3 机制定义](../01-主线版本/TraceAAD-V10.13-r3-机制设计.md)：Refine/Tune 直接提供已有局部试验摘要，Pivot 只看当前程序，Fuse 一份完整参考（50%质量/50%均匀）；四算子概率、父代预算分布和评价器不变。
- 默认单 Python 块加可选一句 Idea；edit 不需 base_hash，由宿主绑定父代，唯一精确替换仍校验。保留单 Python 块原解释，并兼容外围文字中的唯一 JSON；冲突对象不猜选。
- 最终测试：`.venv/bin/python -m pytest -q`，**350 passed / 22.08s**。包含任务冻结契约、实际 parser 场景、inline 参考及恢复故障注入。
- [旧响应离线重放](../../experiments/traceaad_v10_13/review_r3_20260924/r2_response_replay.json)：1,726份原有效程序代码全部一致；323次旧拒收可解析，315次仍拒收（包含72次已删除的 context 请求）。没有调用 evaluator 或回填 r2 结果。
- [冻结预检](../../experiments/traceaad_v10_13/review_r3_20260924/preflight.json)：冻结60文件、语法及 spawn、run/launch/freeze/monitor 入口和20路配置 dry-run 通过。三后端可达。

## 旧批次停止与证据边界

01:43:49 +08:00 确认 `20260924_v1013r2` 的20个指定搜索进程和 watcher 已停止，全部摘要 interrupted，预约预算范围 E73–246。manifest 标记 stopped_by_user/partial；未删除结果、未停止模型服务或监控。

停止前后保存 manifest、checkpoint、进程身份/后代、文件哈希和 routing 身份。复查没有遗留旧搜索或评价子进程，启动新批前旧文件哈希稳定。完整证据在 `experiments/traceaad_v10_13/results/transition_20260924_r2_to_r3/transition.json`。

**5条旧运行停止时处于 evaluating，已预约但没有完整回执**：CVRP rep1/2/3/4 各1次，OBP rep2 1次。保留原 pending 和预算预约，不补造结果、不释放预算、不自动重评；质量分析只用完整回执/事件前缀。这是授权中断的未结算尾部，不把它算成已完成有效评价。其他15条停在 scheduled，没有未投影的完整评价回执。

## 新批次

- 批次：`20260924_v1013r3`，watcher于 **2026-09-24 01:44:06 +08:00** 启动，20路于01:44:08全部进入 running。
- 五任务 × 四重复；seed 0–3；每路E1000，8个有效root；输入/输出token上限24320/8192。
- 后端分配 server1=5、server3=8、server3b=7；逐路 task_eval、LLM、seed、repeat、backend及所有预算参数与r2一致。
- 冻结源：`experiments/traceaad_v10_13/results/runtime_20260924_v1013r3`，身份 `3dbf7069340b30a5a9f7b84262583dec27225b90d6cf3b65e0f38d9e385571b0`。r2冻结源也复核无漂移。
- manifest：`experiments/traceaad_v10_13/results/batch_20260924_v1013r3.json`；会话前缀 `v1013r3`，watcher `v1013r3_batch_watch`。既有8765监控可显示新批，无需重启。

## 首批实际机制证据（partial）

[01:45:31初始快照](../../experiments/traceaad_v10_13/review_r3_20260924/first_protocol_snapshot.json)含46次成功初始化评价。随后[01:46:43开发快照](../../experiments/traceaad_v10_13/review_r3_20260924/development_protocol_snapshot.json)显示：

- 20路存活，E2–14，88次结算评价/88次模型调用，88有效，0解析拒收、0评价失败、0修复、0额外上下文读取。
- 已执行 Init81、Fuse3、Pivot2、Refine2；full86、edit2。Fuse所展示的完整参考都真实出现在提示中，候选和评价回执、父代机会、代码重建没有协议违规。
- OBP rep1 candidate9 的 Fuse 实际拿到 node2 完整参考，但结果仍为 -744.5；rep2 candidate12 的 edit 被正确执行，但最终 AST 没变化、fitness同分。不能把“edit成功”称为有价值改进。
- 此时较慢任务仍在初始化，尚未观察所有算子/任务的开发，inline trials 在该固定快照仍未出现。对应行为已有测试，实际长期收益未知。
- [配置与运行核验](../../experiments/traceaad_v10_13/review_r3_20260924/configuration_confirmation.json)：20/20配置一致，冻结无漂移，所查运行日志无基础设施异常，面板running=20、queued=0、stalled=0。

结束前补充：[01:47:53协议快照](../../experiments/traceaad_v10_13/review_r3_20260924/final_protocol_snapshot.json)已覆盖四个开发算子：20路E3–17，130次模型调用/130次有效评价，full127/edit3，Init112、Fuse7、Pivot4、Tune3、Refine4，仍无拒收、失败、修复和协议违规。出现1次inline trial；OBP rep4 candidate10 Tune实际收到node8同父试验摘要，结果仍为-744.5，读取材料不等于改进。

[01:48:26最后运行确认](../../experiments/traceaad_v10_13/review_r3_20260924/final_live_confirmation.json)：20路进程cwd均为r3冻结源，运行日志无所查基础设施异常，面板running=20、queued=0、stalled=0；旧r2工件哈希仍保持停止后状态。该时点面板156次预算预约与前述130次固定结算快照边界不同，不混用分母。

## 后续判断

保持新批机制冻结，按相同完成评价预算比较重复训练前缀和模型调用成本；完赛继续使用原 held-out 口径与 `experiments/infra/evaluate.py`。当前只证实接口负担显著减少的早期执行事实，不能宣称质量问题已解决。如果同分现象继续普遍存在，应回到具体返回决策和可开发结构分析，不能用更多描述字段或复杂规则掩盖它。
