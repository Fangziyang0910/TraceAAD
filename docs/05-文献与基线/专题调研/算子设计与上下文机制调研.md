# AAD 与 AHD 的算子设计调研

从**算子**视角纵读论文库中的 59 个方法（44 个直接 AAD/AHD，15 个广义程序与算法发现），回答四个问题：各家设计了什么算子、为什么这样设计、他们把算子理解成什么角色、算子周围有哪些上下文机制。

**算子的口径**：凡是把"已有算法程序 + 若干信息"变为"新候选程序"的生成机制都算算子——提示式变异/交叉/改写、RL policy 的动作、latent 空间的梯度步、多智能体的生成动作均在内。选择、接受、预算分配机制在算子之外，但它们决定"算子在哪里、以什么条件被调用"，作为上下文机制一并讨论。**上下文的口径**：算子调用时条件到的全部信息，包括 prompt 里的父代与文本、以及决定谁进 prompt 的选择结构。

一条贯穿全文的观察先放在这里：**算子角色经历了四次重定位**——(1) 语义变异/交叉（把人类修改先验装进 EA 算子）；(2) 带上下文的条件生成（反思、记忆、规划进入 prompt，算子本体不变）；(3) 采样管线中的 proposal（proposal—acceptance—resampling—schedule 显式分离，算子成为被调度的采样单位）；(4) 可学习的策略/智能体动作（算子内化进权重或升格为自主决策）。下文十节按此谱系展开。

## 一、奠基：把 LLM 安装进变异与交叉（2022–2023）

| 方法 | 生成算子（原名） | 输入与上下文要点 | 角色定位（论文原话） |
| --- | --- | --- | --- |
| ELM | Diff mutation（单算子，无交叉） | 1 父代全码 + commit message（如 "Changed parameters…" 按概率抽样）；prompt 不含适应度与历史 | "intelligent perturbation operator"，在代码流形上模仿人类程序员 |
| LMX | LMX = ψ(LLM(φ(x₁,…,x_k))) | k 个父代文本按序拼接，无指令、无分数；k=1 退化为变异 | "intelligent evolutionary crossover"；也被证明与选择算子合成一个 EDA |
| LLM_GP | 8 个 LLM 化算子：i/e/φ/s/v_xo/v_mu/r/b | 每算子一个 prompt-function，文法 `<EXAMPLES><QUERY><PRIMITIVES><RESPONSE_FORMAT>`；few-shot 例取自当前种群 | "prompt 驱动的 EA 算子角色"，把选择/替换/排序也 LLM 化 |
| FunSearch | Best-shot prompting（变异+交叉合一） | 同岛采样 k=2 个程序按分数排序改名 v0/v1，抽掉待演化函数、末尾留 v2 空头让 LLM 补全；分数不进 prompt 文本 | "an instance of genetic programming with the LLM acting as a mutation and crossover operator"；LLM 是 "the creative core" |

设计动机的共同起点是对随机算子的批评：GP 的离散随机扰动"常导致功能剧变"（ELM §2.1）、"designing effective and general code mutation operators remains difficult"（FunSearch Related Work）。LLM 预训练于人类代码与代码修改，天然带有"像人会做的修改"的先验，能一次协调多处相关修改。四篇的贡献分别是：ELM 用在 GitHub diff 上训练的 300M 模型证明该先验存在（4-Parity 上 diff 变异一次修复 5 个耦合 bug，随机变异 10 万次无一成功），并率先演示算子自改进（用被接受 diff 微调 diff 模型）；LMX 把算子写成 few-shot 拼接的闭式公式，证明多父代上下文产生可测的特征继承，且父代按适应度排序本身就是一种软选择压力；LLM_GP 给出形式化词汇表——哪个 EA 环节可以被 LLM 化、每个算子的 prompt 是新超参数，并首次把 token 成本与错误率算作算子设计维度（实测 selection/replacement 的 prompt 最贵最易错）；FunSearch 把这条线工程化：skeleton 固定已知结构只开放关键函数、岛模型 + signature 聚类的 Boltzmann 分数加权采样 + 短程序偏好把评价反馈**只**写进"谁被放进 prompt"，evaluator 负责挡住幻觉。

这一阶段确立的两个默认约定被后来几乎所有方法继承：**分数不进 prompt 文本，选择压力经由父代挑选与排序间接进入**（FunSearch 采纳了 LMX 的排序发现）；**算子只提议，evaluator 只裁决**。

## 二、EoH 模板：prompt 策略谱系与它的后代（2024–2026）

EoH 把"prompt 措辞"本身做成算子谱系：个体为 (思想描述, 代码) 双表示，五个进化 prompt——探索组 **E1**（5 父代，要求新启发式"as much different as possible"）、**E2**（5 父代，先识别共同 "backbone idea" 再据此生成不同实现）；修改组 **M1**（改结构）、**M2**（只改参数）、**M3**（分析并删除冗余组件），外加初始化 prompt。动机是"模仿人类专家的启发式开发过程"（演化 thoughts 与 codes），对照 FunSearch "evolves codes only and does not use prompt strategies explicitly"。上下文机制极简：父代按 rank 概率 p∝1/(r+N) 采样，prompt 含任务 + 策略指令 + 父代（描述+代码），无分数无反馈；消融显示思想通道贡献显著（仅代码的 EoC 最差）。

| 方法 | 对 EoH 算子谱系的改动 | 创新落点 |
| --- | --- | --- |
| MEoH | 五算子原样继承（M3 改写为"识别过拟合组件、简化以增强 OOD 泛化"） | 选择端：dominance-dissimilarity（Pareto 支配掩码 × AST 距离）采样父代与管理种群 |
| HSEvo | 换成 crossover（2 父代显式标注 better/worse + flash reflection 指导）+ elitist mutation + 和声搜索参数优化（LLM 抽参数、经典 HS 调参，M2 被**非 LLM** 数值搜索取代） | 代级 flash reflection 批量压缩（批评 ReEvo 逐对反思浪费资源）；批评 EoH "does not focus enough on exploitation" |
| QUBE | 零改动——FunSearch 的双父代补全 prompt 逐字沿用 | 选择端：UIQ = 簇的子代平均质量 + UCB 访问计数不确定性，用于父代簇选择与岛重置；逐级消融直接支持三层设计 |
| CDEoH | 压缩为 innovation/improvement 两个单父代 prompt，删除交叉；新增 reflection 错误修复算子与 category induction（生成评估**之后**打标签，只进选择不进 prompt） | 选择端：类别两阶段精英 + 稀有类加成 $\lambda/\vert C(i)\vert$ |
| EoH-S | 收敛为 CS（互补感知搜索：实例级性能向量 Manhattan 距离最大的父对）+ LS（即 M1） | 上下文的语义化：prompt 告诉 LLM 两父代"effective for solving different instance distributions"；集合端 CPM 超模贪心 |
| Controlling Mutation | LLaMEA 的 (1+1) 变异 prompt 加"只能改 x% 代码"指令族（手工 1–5、自动 6–11） | 变异率控制：x 从 fast-mutation 幂律采样；GPT-4o 可执行速率指令，GPT-3.5 基本失败；不指定时两模型都倾向极端变异 |

这组的主演进而用一句话概括：**创新重心从"prompt 里写什么"移到"谁被放进 prompt"**。MEoH、QUBE、CDEoH、EoH-S 的算子文本几乎照抄前人，全部改动在父代选择与种群管理；HSEvo 与 Controlling Mutation 是仅有的两个继续在算子文本上下功夫的（前者换算子族并给反思留槽位，后者把 GA 变异率理论移植到 prompt 层）。EoH-S 值得单独记：它把"上下文选择"变成一个显式优化问题（argmax 互补父对），并用一句自然语言把互补性语义传给 LLM——这是"选择信号进 prompt"的最早干净示例。

## 三、反思与元认知：把评价信息写进生成条件

ReEvo 首次把评价反馈转译为算子上下文：generator 与 reflector 两个 LLM 角色；短期反思对 (worse, better) 父代对做比较分析进 crossover prompt；长期反思递归压缩为 ≤50 词的单一滚动文本，条件化 elitist mutation。角色理解是该文最值得继承的部分——用适应度景观把反思理论化：邻域定义为 N(h_c)={h | LLM(h|h_c,x)>ξ}，反思改变 prompt x 即改变邻域结构，故称 "verbal gradient"；实测带反思的 correlation length 1.28 vs 无反思 0.28（邻域更平滑）。此后四篇沿"反思产物的位置"递进：

| 方法 | 反思产物的位置 | 关键机制 |
| --- | --- | --- |
| ReEvo | prompt 槽位：短期反思进 crossover，长期反思进 elitist mutation | 字数上限即压缩；无检索无打分 |
| HiFo-Prompt | 被管理的知识资产：Insight Pool（准入 Jaccard<0.7、按 utility 检索 top-3、分段信用 EMA、eviction 淘汰）+ foresight 三态指令（种群停滞计数 + 表型多样性 → Explore/Exploit/Balance 指令池） | 生成算子完全照搬 EoH 五模板，只加两个上下文插槽；关键消融：把相同 seed insights 静态注入 EoH/ReEvo/HSEvo/MCTS-AHD 增益可忽略——**表明知识文本的生效高度依赖动态检索与淘汰机制，但在该设定下无增益不能直接推导为“知识内容不重要”** |
| EvoPH | 被进化的对象：meta-prompt 用父子指标差 + 完整对话史重写演化 prompt 本身；另按经验采样 5 档探索强度的策略（参数修改→…→完全重写） | "elevate the evolutionary search from the program level to the prompt level"；prompt 演化同时提高可执行率 |
| MeLA | 唯一搜索对象：放弃种群动力学，metacognition 读全部 thoughts/errors/fitness 输出 ≤80 词反思，条件化唯一的 Metacognitive Generation 算子 | "evolves the problem-solving strategy itself, which is encoded in the LLM's guiding prompt"；批评 ReEvo 交叉要求可执行父代、不可执行则停摆 |
| MeEvo | 与代码并立的基因型：reasoning trace 是可遗传基因型、代码是表型；自然进化（LLMCrossover/LLMMutate，强制生成 reasoning trace）与元认知进化（结构化 Meta Insight：收敛诊断/局限分析/路径建议）按 N:M=2:1 循环交替 | 交替而非并行的操作性理由：交叉需要稳定父代池、反思需要跨代历史；算子比率 p_c 从 0.7 线性降至 0.5；消融 (2,1) 最优、纯 NE/纯 ME 均差 |

演进逻辑：反思产物的载体从 prompt 槽位 → 被打分检索的知识池 → 被进化的 prompt → 唯一搜索对象 → 与代码并立的遗传实体。算子相应从"变异 + 反思上下文"演化为"表型变异算子 + 基因型反思算子"的双进程体系。

## 四、树与规划：算子作为被调度的扩展动作

MCTS-AHD 把 EC 算子搬进树并发明树原生算子：六个动作 **i1/e1/e2/m1/m2/s1**——e1 在根节点做跨子树交叉，e2 用"父代 + elite set 采样参考"做交叉，m1/m2 是机制/参数变异，**s1 是树路径推理**（把根到叶路径上所有 unique 启发式给 LLM，先列 helpful ideas 再综合）。动机直接批评种群法"directly discards low-performance heuristics… falling into local optima"——树允许 worse-before-better。分工：UCT + 渐进扩展（⌊N^0.5⌋）+ max 回传决定算子在哪里调用；生成后二次调用做 thought-alignment（先码后述，避免先描述后码的幻觉）。s1 的消融正面支持"树结构本身催生新算子"。

| 后续 | 改了哪一侧 | 要点 |
| --- | --- | --- |
| PoH | 生成侧 | 唯一算子族 = "建议→执行"两段式：自反思产出 improvement suggestion（动作），optimizer LLM 按建议改码（转移）；state_transit prompt 同时放示例、建议与**祖先链**；受控消融固定算子只换 MC/Greedy/Beam/MCTS，证明规划层本身的贡献 |
| PathWise | 两侧 | 动作 = (父集 S, 自由文本 directive κ)——"the policy can dynamically invent new operator types through κ"；policy agent 提议、world model 执行 N_w 个 rollout、双 critic 反思路由；**取消 UCT 与访问数**，预算交给 argmax + leaf-first 种群管理；谱系变为可多父的 entailment graph + 压缩父代元数据 |
| Clade-AHD | 只改选择，生成冻结 | prompt 与六动作**逐字沿用** MCTS-AHD；节点点估计换 clade 级 Beta 信念（深度衰减聚合 + Thompson 采样 + 冻结 + 预算退火）；理由是 stepping-stone 节点孤立看差但后代优，稀疏评价下 UCT 点估计失真 |
| CogMCTS | 生成侧的上下文供给 | 骨架照搬 MCTS-AHD，交叉替换为 em1（CCS 候选集配对组合）与 em2（全局最优引导）；两阶段认知（rapid 两两对比 → complex 整合 K⁺/K⁻ 知识库）注入两个 em 算子；CKV 用前后 global-best 变化验证知识正负归属 |

组内分工演化：生成算子从固定模板（MCTS-AHD）→ 建议-执行两段式（PoH）→ 策略自由发明的 directive（PathWise）→ 认知知识库注入（CogMCTS），越来越依赖上下文而非模板；选择机制分岔出贝叶斯谱系信念（Clade-AHD）与反思路由 + 种群管理（PathWise）。一个反复出现的模式：**每篇都保留"祖先谱系 + 全局精英"两类跨分支上下文**，承载形式分别为 elite set / trajectory / PM+reflection / CCS+K⁺K⁻ / clade 信念。

## 五、调用条件：调度、时机与在线共生

这组方法不改算子内容，改算子**何时、何地、以什么版本**被调用。

| 方法 | 改变的调用条件 | 角色定位 |
| --- | --- | --- |
| RefineEvo | Planner 按种群状态（多样性、收敛率）与算子统计（成功率、无效率）选算子并触发算子改写；算子库 op1–op5 改编自 EoH，其**自然语言描述本身**被在线改写（incremental/radical 两模式）；Reflector 产出经验条目入 E⁺/E⁻ 双银行（utility 信用 + 矛盾消解 + top-3 检索） | "operator selection adapts *which* operators to apply, operator refinement adapts *how* each operator guides generation"；算子是被调度的 prompt 资产；选择性调度把 token 开销从约 965K 压到 511K |
| BaSE | 算子与 prompt 全冻结，研究 C=T·N 的深广划分与 K 条轨迹间的 bandit 路由（improving bandits，UCB/EXP3.P/Thompson） | "not a model improvement, nor a prompt improvement, but an allocation improvement"；发现双线性 production law 与平台半宽；机制解释：轨迹在前几代锚定算法族，广度给更多找到可行族的并行机会、深度在族内精炼 |
| EvoStage | 设计对象按时段拆分：Stagewise-Design 由 coordinator 分解 K 个阶段、coder 逐段生成，输入**该阶段执行后的中间反馈**（如当前 wirelength/overflow 及变化）；Global-Explore/Enhance 保留 one-shot 整体生成作"全局视角"；三算子 1:1:1 固定频率 | 针对 black-box modeling：只有终态分数使 LLM 无法利用中间反馈纠正设计方向；还关闭了 EoH 式 thought-of-code（稀缺数据场景加剧幻觉） |
| DyACE | 算子调用主体移入求解过程：每 5 代解进化触发 1 代算法进化；三模式 Combine/Mutate/Explore 全部两阶段 think-then-code，Diagnosis Agent 把 solver 的搜索轨迹特征（landscape kinematics + operator telemetry）翻译成 verbal gradients 再驱动 Coding Agent | grounded meta-controller（receding horizon）；关键反面证据：DyACE-Blind（保留在线循环、去掉特征提取）劣于静态版本——**无感知的频繁算子改写是随机游走** |

## 六、历史结构与知识态：从种群副产品到一等输入

| 方法 | 历史的表示 | 算子与历史的关系 |
| --- | --- | --- |
| DeltaEvolve | 三层 delta 金字塔：L1 FROM/TO 策略摘要（20–40 token）、L2 组件级 plan（COMPONENT/OLD_LOGIC/NEW_LOGIC/HYPOTHESIS）、L3 全码（存库不进上下文） | 唯一 mutation 算子在同一次响应中生成新码 + 自文档化 delta；上下文 = 父码 + 3 个 top delta + 2 个 diverse delta（MAP-Elites 嵌入网格取远端）+ 定性 ΔR；动量类比 SGD；EM 形式化把上下文构造定为唯一被优化的潜变量。**核心受控发现**：在已通过 fitness 筛选出 Top-3 精英的前提下，Blind-Elite（Prompt 文本中省去分数数值）几乎无损，而 Random-Context（随机抽选上下文）性能崩溃——**这证明起作用的是“由评价驱动的上下文选择”而非文本中的具体分数展示；评价信息已通过选择端强力生效，不能误解为“评价信息无价值”** |
| PhyloEvolve | 谱系森林，S-表达式边 (mod, delta_reward)；Elite Pool 两形态：完整 elite trajectories + 统计化 elite modifications（带 μ/σ/n 与 value(m) 公式） | 四 agent：NextStepper（in-context policy，warmup/explore/exploit 三模式）、ModifyAgent（最小 diff + 失败调试路径上树作负例）、Designer（跨范式分叉造新树根）、Summarizer（周期压缩）；理论外衣是 ICRL（Algorithm Distillation + prompt-Decision Transformer）："EA learns at population level through selection; ICRL learns at trajectory level through contextual conditioning" |
| BttB | 知识态 K（design principle/motif/rule 文本）成为一等搜索对象，代码只是 K 的实例化与检验 | CX_K/MT_K 在知识层做交叉/变异；DS/GR 两个算子专职代码↔知识互译；把 ReEvo 与 MCTS-AHD 改造成 knowledge-first 版本做同预算对照；distortion–compression 权衡论证换轨的代价与收益；稀疏评估下未评估候选退化为 structure-only evidence |
| MEMOIR | 双时间尺度记忆：branch-local memory 存修复/改进记录（**代码本身不入记忆**，存独立 artifact 库）；global memory 存分支终止时 REFLECT 压缩的三字段条目（设计、失败原因、负约束，约 272 token） | 五算子 PROPOSE/REPAIR/IMPROVE/CRITIC/REFLECT 各条件于相应记忆；PROPOSE 只见全局记忆并被指令要求 DIFFERS from failed attempts；消融证明**分层本身**（flat memory 变体显著差）与失败记录的价值；只升级 Critic/Reflect 模型即有 +4.17 提升——记忆条目质量本身重要 |
| AutoSND | 执行证据档案 → LLM-Struct 投影到**固定结构词汇**（degree/two-hop/boundary…）→ 对比证据（正例 Pareto 层 + 负例按差质量/慢/失效分类）+ 结构统计 | Policy induction（B₂=10 次）+ 确定性 Compile 合并为 Π=(A⁺, A⁻, B) 三段结构政策，硬性进入 Stage III 每次生成调用并拒绝违反 forbidden_patterns 的响应；知识从建议变成可执行约束 |
| GeoEvolve | 历史主要在**外部**：141 份文献的向量检索库；内部只有 OpenEvolve 默认种群 + 全局精英 | Analyzer 诊断代码知识缺口并提出检索查询 → GeoKnowRAG（多角度查询扩展 + RRF 重排）→ Geo-informed Prompt 四要素注入；消融显示静态知识 prompt 有时比纯 OpenEvolve 更差——**外部知识必须绑定当前失败证据** |

一条成形的设计语法：历史 →（差分/谱系/记忆条目/政策）→ 条件化生成。分歧只在四点：知识态由谁产生（搜索自产 vs 外部检索）、压缩发生在哪（生成时自文档化/分支终止反思/阶段间编译）、对算子是软上下文还是硬约束（AutoSND 的 forbidden patterns、MEMOIR 的负约束措辞）、代码与知识谁是搜索坐标系（BU vs TD，BttB 的核心轴）。

## 七、大规模系统：从单个提示到采样—接受—调度管线

AlphaEvolve 论文正文没有给生成算子命名（"evolve/recombine/SPIRE" 等名称出自开源复现，引用时须注明）；其生成端只有一个 creative generation（SEARCH/REPLACE diff，短程序可整写），交叉隐含在 prompt 含多个先前解中。真正的贡献在环绕机制：prompt sampler 四组件（显式上下文、随机格式化、渲染评估结果、**meta prompt evolution**——指令本身在平行数据库中共同演化）、Flash+Pro 模型 ensemble 的静态吞吐分工、评估级联与并行化、MAP-Elites + islands 程序库。消融中 "No context in the prompt" 单项显著退化，是"上下文是算子有效性的一部分"的大规模证据。

后续工作把隐式机制显式参数化，最终形成四层管线：

| 层 | 代表工作 | 机制 |
| --- | --- | --- |
| proposal | ShinkaEvolve：变异族 diff/full/cross 概率 0.45/0.45/0.1；RL-Algorithms：macro mutation（重写恰好一个组件、"justified by the failure patterns"）+ diversity-aware crossover（第二父代按 Levenshtein 距离混合采样，α=0.5），生成器写成显式条件分布 q_φ(f₁,f₂,op,R,E)；SMCEvolve：2×2 kernel mixture（edit/rewrite × 有无 peer inspiration）+ Thompson 选 kernel | 生成分布的参数化与混合 |
| acceptance | ShinkaEvolve 的 novelty rejection sampling（嵌入阈值 0.95 + LLM judge）；SMCEvolve 的 reward-only Metropolis 接受（温度化） | 生成与入库之间的显式过滤 |
| resampling/选择 | ShinkaEvolve 父代 power-law/weighted（多子代惩罚）；SMCEvolve 温度化 softmax + systematic resampling | 父代分布的构造 |
| schedule | TurboEvolve：VS 多子代采样 + 岛级自适应 K（按 cell replacement 事件调节）；RelayEvolve：Grow/Deepen bandit + relay gain（超模质量-覆盖集分数）驱动 cheap→strong 的**种群级**交接（变异外包给 ShinkaEvolve 不动） | offspring 数、调度动作、交接种群成为新的算子变量 |

SMCEvolve 的收束性表述值得单列：AlphaEvolve 与 ShinkaEvolve 是"固定 β、K=1、接受概率恒为 1、预固定 T"的特例——"This explains why existing frameworks work"。其 inspiration 取自**包含被拒绝程序在内的完整历史**（拒绝者的信息不丢弃、转为灵感），ESS 自动停止取代固定迭代数；消融显示 adaptive resampling > uniform/greedy、mixture > 单 kernel、同预算下 (N,K) 配置存在宽度-深度权衡。

另一条正交轴：**父代选择与分区信号本身成为算子设计面**。BehaveSim 用执行轨迹（PSTraj + DTW 双重期望）重定义岛归属（新算法放入平均行为最相似的岛）与父对选择，生成 prompt 不动；PartEvo 用特征投影 + K-means 在语言空间构造 niche，其上组合 prompt-centric（RE 反思进化、SE 摘要进化）与 EC-inspired（CN 跨 niche 交叉、LGE 个体+niche 最优+全局最优三元组）四算子，K=1 时显式退化回 EoH 式选择。

## 八、设计对象扩展：算子粒度随对象分层

固定单函数模板被从四个方向突破：

| 方法 | 设计对象 | 算子形态 |
| --- | --- | --- |
| RedAHD | 规约 (f,g) + 其下的启发式模板 | 三段串行 prompt（候选问题描述 → 规约函数 → 启发式模板，刻意不合并防幻觉）；启发式层只复用 EoH 的 E2+M1（E1 被弃："often erroneous"）；跨规约共享种群——任一问题的启发式可为另一问题当父代；规约层停滞触发 refinement |
| Hercules | 仍是 GAF 内单函数，但四类槽位 | 算子沿用 ReEvo；创新在上下文前置（CAP：每迭代从 top-5 精英抽象核心组件作方向先验，后期切换为父代自身组件保多样性）与评估代理（PPP 少样本性能预测 + 置信分层接受）；批评 ReEvo 反思产出 "test and iterate" 式空泛方向 |
| MoH | heuristic-optimizer 本身（`optimize_algorithm(population, utility, LM, prompt, subtask)` 可调用函数） | **没有固定算子集合——算子是被设计对象**；外层 meta-optimizer 进化 optimizer（跨任务规模加权 utility），内层被设计的 optimizer 生成启发式；seed optimizer 跑出的后代包含 GA/ACO/PSO/SA 及混合 |
| InstSpecHH | 按实例子类的启发式组合 + 在线选择 | EoH 算子池全量复用，上下文按子类实例化；Neighbor Search 把邻近子类的既有启发式拿来评估复用；LLM/NN 二选一做在线算法选择 |
| A2DEPT | 完整 solver 程序（mutable/immutable 角色划分） | 分层算子 m1 micro（单函数、接口锁死）/ m2 macro（只写主函数骨架、**允许未定义 helper**）/ e1 语义交叉（双亲 THOUGHT+主函数）；维护算子（角色重分析 + 依赖修复闭环）兜底可执行性；谱系只喂选择与调度（LCA 多样性选交叉伴、节点级算子权重 softmax），**不进生成 prompt** |
| BEAM | 结构层 α（骨架）+ 函数层 w 双层变量 | 外层 GA 在结构层做交叉/变异（ReEvo 式 better/worse 比较，**仅结构不含函数**）；内层 Education 逐函数构造——每填一个函数就临时补全并评估完整个体（函数级中间信用）；AM 自适应记忆把函数沉淀为可 import 的组件库（LLM 只见函数名+用途，"Do not reimplement"）；CMA-ES 校准超参 |
| DGA²D | 有向算子图：节点=功能算子带实现池，边=执行顺序，算法=有界有向 walk | 三类正交动作：实现级编辑（ADD/REPLACE，带失败诊断）、边编辑（GraphDelta，至多 2 加 2 删，事务式提交）、沿 walk 的一阶马尔可夫实现选择；**信用粒度是显式设计变量**：一阶转移信用 Q(c_{i−1}→c_i)，消融 first > zero > second > full-path（full 最差："excessive path dependence introduces noisy signals"） |

系统级四篇（A2DEPT/BEAM/DGA²D 与其自述）共享同一组困难：可执行性、空间爆炸、信用分配。三种解法各有消融支持：A2DEPT 用**粒度调度**回避归因（先在易归因的小粒度改）；BEAM 用**双层中间评估**制造归因（每函数一次完整个体评估）；DGA²D 用**一阶马尔可夫近似**显式建模归因的偏差-方差。DGA²D 的 full-path 变差同时为"上下文不是越长越好"提供了组内最直接的证据。

## 九、算子的学习化与外部信号

三条正交轴：

**（一）角色化（不学习）**：RoCo 在 EoH 之上保留其算子，把生成拆为 explorer/exploiter 两个角色策略（指令分别强调"creative, future-oriented"与"conservative, efficiency-driven"），新增 critic（比较前后代产出 ≤30 词反思 + ≤40 词可执行建议）与 integrator（按分数融合双方产物），另有带角色记忆的 elite mutation 三算子；产物与 EoH 算子产物合并后 TopN 选择——选择机制仍是 EoH 式精英制。

**（二）外部信号条件化（学习信号源，不学生成器）**：

| 方法 | 外部信号 | 进入算子的方式 | 边界证据 |
| --- | --- | --- | --- |
| LLaMEA-SAGE | archive 训练的 XGBoost surrogate + SHAP | 一句结构指令 "try to \<increase/decrease\> the \<feature\>" 扩展 LLaMEA 变异 prompt | Figure 5：random-new prompt 下 LLM **完全忽略**引导，refine prompt 下大多遵循——引导的生效依赖基础算子类型；作者明示相关性非因果 |
| Teacher-Aware | 独立训练的神经求解器（L2D/LEHD/ECO-DQN）在候选 on-policy 状态上的动作偏好 | analyzer LLM 把分歧案例转成 revision briefs，驱动三个修改模式（structural rewrite / parameter calibration / mechanism fusion）；种群按 objective–alignment Pareto 管理 | 最终选择只按任务目标（max-align 消融在 TSP OOD 更差）——**教师信号只应引导搜索，不应成为目标** |

**（三）生成器学习化**：EvoTune 最早把话挑明——"refining the search operator, the LLM, through RL fine-tuning"：FunSearch 式 prompt（2 程序+分数）产 K=8 个输出，off-policy DPO 用同 prompt 输出构造偏好对，程序库类比 replay buffer。Fine-tuning LLM for AAD 转为离线：用 EoH/FunSearch 历史产物 + DAR 分层采样（跳过相邻质量档保证信号、x 用固定模板而非 few-shot——后者导致 DPO 不稳定）训出可插入任意搜索器的专门模型。CALM 合并两线并前进一步：细粒度 verbal 算子（injection/replacement/crossover/simplification，比率 1:2:4:1）被**明确设计成便于 GRPO 做 token 级归因**的形态（论文原文：uniform treatment of all parts is unreliable），配层级化负奖励（缺 idea −1.0 … 含随机性 −0.75）与 collapse 重置；消融中 w/o GRPO 是最大退化。AHD Agent 取消独立算子：生成、评估调用、工具调用（实例结构分析、AST 新颖度检查——免费且只碰设计集）、终止全部是一个 GRPO 策略的动作空间，只有一个 system prompt + 一个 user prompt；预实验发现"在固定工作流里给 LLM 全部工具收益有限甚至有害"——缺的是**状态依赖的信息获取机制**，这正是把上下文获取本身变成学习动作的动机。

## 十、内化、连续化与智能体化

对"推理时反复搜索的边际成本"的三种回答：

| 方法 | 内化到哪里 | 算子形态 | 关键证据 |
| --- | --- | --- | --- |
| LHS | 程序流形的几何：encoder 冻结，训练 flow（高斯化）+ surrogate（RankNet）+ mapper（soft prompt） | 先验空间中对 surrogate 的梯度上升步，frozen LLM 只做 soft-prompt 条件化解码 | No-Flow（原始空间搜）有效率 61% vs 74%；No-Grad（top 对插值）有效率更高但质量更差——梯度换质量、插值换稳定；surrogate 漂移自认为未解决 |
| GAE | mutation policy 权重：OpenEvolve 主干 + 三条解耦学习链（AST→GNN 编码器周期微调、离散 SAC 选 mutation hint、GRPO 在线更新变异 LLM，父代真实分数作组内基线） | LLM 变异算子被在线 GRPO 适配，SAC 决定"改哪怎么改" | 批评对象明确："the LLM for mutation is fixed throughout the entire search"；无逐组件消融数值；论文内部两处不一致（SAC 实际不选父代、GRPO 基线非 GNN 嵌入） |
| Hero | solver 合成策略：一次性 GRPO 把推理成本摊销为可复用 solver（compile-once，逐实例 91× 低于累积 Best-of-64） | 分层奖励：可行性硬门（cliff）+ 结构关键词课程消退 + **anti-lazy-sort 负向罚**；scaffold 被定位为"filter that prevents the policy from staying in the greedy basin"而非教师 | interpreter→compiler 的话语；w/o Structure 退回 greedy 盆地；base 模型 21.9% 输出本就 SA-like 但 28.8% 带全局最优 bug——"presence ≠ competence" |
| Self-Developing | 生成改进算法的工厂：DPO 偏好对（top 3% / bottom 10%）闭环更新 algorithm factory，温度逐迭代衰减 | 产物是 `merge_models` 可执行函数；工厂总作用于原始 M₀（对象与工厂解耦） | 迁移协议（固定 top-15 算法换新候选模型执行）支持产物是可复用算法；缺同预算 DPO-off 对照 |
| AlgoPilot | 程序性先验：随机函数轨迹训练 TLM，其似然作 RL 排序 agent 的塑形奖励 | 裸 RL 轨迹无算法模式；TLM 引导后轨迹可被 GPT-4o-mini 恢复为正确冒泡排序 | 无等预算结构指标消融；Step 5 的 LLM 自认带常用算法先验 |
| CORAL | 智能体决策与共享记忆：模型全程冻结，Retrieve/Propose/Evaluate/Update 四阶段的控制权交给 agent（17 个 CLI 命令、共享 attempts/notes/skills 文件记忆、heartbeat 反思/整合/转向干预） | 算子升格为自治决策："The LLM has no agency over what to explore next. CORAL removes this scaffolding" | 4-agent 协同 > best-of-4 独立单 agent（1103 vs 1180）；知识积累消融在困难任务上关联 55% 的改进；改进率与收敛所需 evals 数优于固定流水线 3–10× |

共同的经验教训：无论内化进权重、几何还是记忆，**选择与验证机制都不能省**——Hero 的硬可行性门、GAE 的四级验证链、LHS 的执行评分、CORAL 的隐藏 grader 都是各自系统中 load-bearing 的部分；且两个独立来源（Hero 的 anti-lazy-sort、GAE 的父代分数基线）都指向**负向/相对信号比正向引导更算数**。

## 十一、跨谱系综合

### 1. 算子角色的四次重定位

1. **语义变异/交叉**（ELM→LMX→FunSearch→EoH）：算子 = 装了人类修改先验的 EA 算子。核心主张是 LLM 能一次协调多处相关修改并"留在功能流形上"；温度≈变异率、父代排序≈软选择、prompt 措辞≈算子定义。
2. **带上下文的条件生成**（ReEvo→HiFo→EvoPH→MeLA→MeEvo，及树搜索组的生成侧）：算子本体稳定（EoH 模板或 MCTS-AHD 六动作），创新全部发生在"算子条件到什么"。ReEvo 的邻域形式化 N(h|h_c,x) 把这一点说透了：改 x 就是改邻域。
3. **采样管线中的 proposal**（AlphaEvolve→ShinkaEvolve→SMCEvolve→TurboEvolve/RelayEvolve）：proposal、acceptance、resampling、schedule 四层显式分离；offspring 数、调度动作、交接种群也成为算子变量。SMCEvolve 的"现有框架是特例"表述是这一定位的成熟形态。
4. **可学习策略/智能体动作**（EvoTune→CALM/Fine-tuning→AHD-Agent→GAE/Hero；CORAL）：算子内化进权重（mutation policy、solver 合成策略）、连续化为 latent 梯度步、或升格为接管信息获取与评价调度的自主动作。

四个定位不互相淘汰而是叠层共存：2026 年的系统（GAE、DGA²D、AHD Agent）同时使用 prompt 算子族、显式接受规则与学习型调度。

#### 2. 上下文机制类型学与完整路径追踪

在考察上下文与算子关系时，最常出现的误区是将机制作用位置概括得过于笼统（例如将“影响选父”误写为“进入 prompt”）。为了严谨分析，必须对每条信息**追踪完整路径**：

$$\boxed{\text{信息从哪里来} \longrightarrow \text{如何组织} \longrightarrow \text{谁读取} \longrightarrow \text{改变哪一个决策} \longrightarrow \text{原文用什么证据检查效果}}$$

遵循该路径，可以清晰区分四类经常被混淆的作用位置：
1. **影响候选生存与淘汰（Survival & Eviction）**：信息用于决定哪些程序保留在档案/种群中，哪些被永久丢弃或移入冷库；
2. **影响父代与上下文选择（Parent & Context Selection）**：信息用于决定当前步骤挑选哪个程序作为基底或参照（如 CDEoH 的类别加成、EoH-S 的互补配对、Boltzmann 质量采样）；
3. **直接作为文本进入生成 Prompt（Prompt Input）**：信息被组织为自然语言、代码或差分，直接暴露给 LLM 改变其采样偏好（如 ReEvo 反思、TraceAAD 形成来时路、DeltaEvolve 的 L1/L2 差分）；
4. **仅作为事后离线诊断或监控标签（Offline Telemetry / Post-hoc Analysis）**：信息不直接参与在线闭环，仅在离线评估中用于度量多样性或诊断停滞（如部分行为距离与谱系特征）。

**信息类型**（什么进条件）：

| 类型 | 代表 |
| :--- | :--- |
| 父代与精英（分数间接进入） | rank 概率（EoH 系）、Boltzmann 分数簇（FunSearch/QUBE）、互补父对（EoH-S）、行为 niche（BehaveSim/PartEvo）、LCA 多样性（A2DEPT）、Levenshtein 第二父代（RL-Algorithms）、dominance-dissimilarity（MEoH） |
| 分数直接进 prompt | EvoTune（2 程序+分数）、DeltaEvolve 的定性 ΔR、DyACE 的轨迹特征、EvoStage 的阶段反馈——少数派，且多经语言化转译 |
| 反思与知识条目 | ReEvo 短/长期、HiFo insight pool（带信用与淘汰）、CogMCTS K⁺/K⁻、RefineEvo E⁺/E⁻、MEMOIR 负约束、AutoSND A⁻ |
| 谱系与历史结构 | 祖先链（MCTS-AHD s1、PoH trajectory）、delta 金字塔（DeltaEvolve）、谱系树 S-expr（PhyloEvolve）、entailment graph + PM（PathWise） |
| 规划与状态信号 | HiFo foresight、RefineEvo Planner、PoH/PathWise 规划器、CogMCTS 认知指导 |
| 外部信号 | SHAP 结构归因（SAGE）、teacher 动作对齐（Teacher-Aware）、文献检索（GeoEvolve）、性能预测（Hercules PPP） |
| 组件与知识库 | BEAM 的 AM（import 复用）、PhyloEvolve elite modifications、AlphaEvolve 程序库、CORAL notes/skills、MoH 被设计的 optimizer 接口 |

**管理机制**（怎么进条件）：字数上限（ReEvo 50 词、MeLA 80 词）是最原始的压缩；分层渲染（DeltaEvolve 按 recency 降粒度）、终止点反思压缩（MEMOIR ~272 token 条目）、固定词汇投影（AutoSND）、周期摘要（ShinkaEvolve meta-scratchpad、PhyloEvolve Summarizer、PartEvo SE）是结构化压缩；准入-检索-信用-淘汰（HiFo、RefineEvo、BEAM AM）是完整的知识资产管理；接受规则与调度（SMCEvolve、TurboEvolve、RelayEvolve）决定算子的调用分布。

### 3. 被继承的算子资产

- **EoH 五算子**是被复用最多的资产：MEoH、InstSpecHH 全量继承；RedAHD 裁为 E2+M1（E1 因错误率高被弃）；RefineEvo 改编为可改写的 op1–op5；HiFo 原样继承并加上下文插槽；RoCo 保留 E1/E2/M1/M2；BEAM 把 ReEvo 交叉/变异升维到结构层；PartEvo 沿用 EoH 双表示。
- **FunSearch 双父代排序补全**：QUBE 逐字沿用；AlphaEvolve 保持"多先前解进 prompt + diff 输出"精神；EvoTune 沿用并首次把分数写进 prompt。
- **MCTS-AHD 六动作**：Clade-AHD 逐字冻结（为隔离选择机制贡献）；CogMCTS 沿用选择/回传、替换交叉类动作为认知驱动版本。
- **ReEvo 的 verbal gradient 概念**被 DyACE（在线控制）、Hercules（批评其空泛）、PartEvo（叠加 niche）、HSEvo（批量化为 flash reflection）多向引用，是反思谱系的标准术语。

### 4. 消融支撑最硬的算子层事实

以下各条都有论文内的受控对照，是本调研中证据强度最高的设计事实：

1. **在已由适应度筛选出高分候选的前提下**，上下文选择支配分数条目（DeltaEvolve：Blind-Elite 几乎无损，证明评价信息已通过选择端充分注入，提示文本无需显式数值；不能误读为评价无用）；
2. 反思使生成邻域更平滑（ReEvo correlation length 1.28 vs 0.28）；
3. **静态知识注入未显增益，知识收益高度依赖管理架构**（HiFo：说明知识文本的生效需要动态检索、信用与淘汰机制支持，在特定静态设定下无增益不能直接推论为知识内容本身不重要）；
4. 记忆分层本身、失败记录本身有价值（MEMOIR flat/w/o-failed 消融）；
5. 求解器内部 pipeline 拓扑中一阶转移信用优于零阶与全路径（DGA²D 信用粒度消融；指算法单次运行内部组件执行的前后调用拓扑，既非搜索祖先路径，亦非 prompt 长度；可启发“组件价值依赖运行上下文”的认识，不能直接证明“TraceAAD 只应使用短历史”）；
6. prompt 级变异率控制依赖模型能力（Controlling Mutation：GPT-4o 可、GPT-3.5 不可）；
7. 结构引导只在 refine 型算子生效（SAGE Figure 5）；
8. 教师信号引导搜索有效、作为最终目标有害（Teacher-Aware max-align 消融）；
9. proposal/acceptance/resampling 分离有独立贡献（SMCEvolve 组件消融）；
10. 无感知的在线算子改写有害（DyACE-Blind 劣于静态）；
11. 固定工作流里堆信息无益，缺的是状态依赖获取（AHD Agent 预实验）;
12. 硬可行性门与负向结构罚是跨越能力墙的关键（Hero 消融组）。

### 5. 与既有文档的链接

- 算子作为研究对象的**诊断证据**（变异偏置、可控性、景观、行为多样性）见LLM 作为进化算子的机制分析；设计侧结论与诊断侧结论互为表里——例如该文"有效算子需要把变化控制在模型能理解、evaluator 能辨别的范围"正对应本文 SAGE/Controlling Mutation/Hero 三条证据。
- 涉及**模型训练**的方法细节另见自动算法设计与模型训练结合的方法；各方法实验设置见AAD 方法实验设置汇总。

## 附：机制事实勘误归档与多层同步状态表

本表为**历史机制事实偏差与迁移推论边界的核验归档与多层传播审计记录**，用于防止旧说法重新回流，**绝非当前尚未修复的错误清单**。表中所列各项偏差已全部在逐篇阅读笔记、专题综述（《自动算法设计与模型训练结合的方法》、《算子设计与上下文机制调研》、《AAD方法探索与利用平衡调研》、《LLM作为进化算子的机制分析论文》）、文献索引（《阅读笔记索引》、《论文库文献汇总》、《LLM自动算法设计方法汇总》）以及总纲性认识中完成全流程同步修正：

| 涉及笔记 / 方法 | 原先偏差 / 容易混淆的旧说法 | 原文核验后的正确机制事实（已全流程同步至各层文档） |
| --- | --- | --- |
| 03-Evolving-Code | 整篇误记为 ELM 的衍生或同一技术谱系 | 该文是 Hemberg 等人（MIT CSAIL）提出的独立 LLM-GP 算子代数形式化框架，与 Lehman 等人的 ELM 无技术继承关系（无 diff 变异模型、Sodaracer 或 MAP-Elites）；按训练型闭环标准独立复核，其全程基于预训练 GPT-3.5 提示，**全程无模型参数更新**，已独立排除于训练方法表。 |
| 07-HSEvo | "HS"误读为层级搜索（Hierarchical Search），描述了不存在的分层演化机制 | HS = Harmony Search（和声搜索）；机制为 GA 负责代码结构探索，和声搜索负责数值参数微调（不经 LLM），两者双通道分工。已在各综述与索引中正名为和声搜索。 |
| 08-QUBE | 混淆两类不确定性，将访问不足导致的未知建议为对父代追加复评 | 严格解耦两种不确定性：(1) 对子代生成潜力的认识不确定性（需向该节点分配生成调用采样新子代以消除，复评父代自身毫无作用）；(2) 对父代代码自身得分的测试方差（需在更多实例上复评父代自身）。严禁统一写成含混的“追加验证”。 |
| 09-CDEoH | 误称类别组织生成语义、进入生成 prompt 上下文结构 | 类别归纳发生在候选生成与评估**之后**，仅用于两阶段小生境种群选择（类别精英保留与稀有度加成），不进入生成 prompt 上下文；生成端采用单父代 innovation（创新）与 improvement（改进）两类独立指令，无交叉；执行报错由 Reflection 反思算子独立修复。 |
| 10-EoH-S | 将逐实例最优 CPI 误当作免额外成本的部署性能，成绩向量距离当作互补事实 | 澄清集合 CPI 是逐实例 Oracle 评价上限（部署需路由器或 $k$ 倍运行开销）；性能向量 Manhattan 距离只是“互补代理”而非“互补事实”（全劣解也可产生极大距离但增加零覆盖）。 |
| 11-Controlling-Mutation | 误称有质量反馈调节修改强度与步长 | 变异率固定来自幂律分布（$\beta=1.5$）采样，与候选质量无关；代码差异率仅为离线遵从度度量，不进入自适应反馈回路。 |
| 14-Evolutionary-RL-Algorithms | 将 Levenshtein 机制概括为控制突变幅度的结构正则惩罚 | 按原文方法公式（§3.3），Levenshtein 距离项用于多样性感知交叉（diversity-aware crossover）中挑选第二父代的相异性加权（$S(f_2 \mid f_1) = \alpha F(f_2) + (1-\alpha) d_{\mathrm{lev}}$），防止与近期变异产生的近似重复个体近亲交叉；它不直接等同于对子代变异幅度施加惩罚，变异算子采用单模块宏观重写。 |
| 12-AlphaEvolve | 误引用第三方实现的 "evolve/recombine/SPIRE" 算子名 | 原文与附录均无这些命名，出自第三方开源实现；官方论文为多 LLM 进化编码智能体。 |
| 17-Experience-Guided-CoEvolution | 消融标注"未验证" | 原文包含 w/o Strategy Sampling、w/o Prompt Evolution、w/o Island-Based Elites 三项受控消融（TGB），已更正为“部分支持”。 |
| 18-MeLA | 消融标注"未验证" | 包含 PA 有/无对照与 Meta-1/2/3 阶段递进实验，已更正为“部分支持”。 |
| 19-MeEvo | 消融标注"未验证" | 包含五种探索-利用配置消融与超参数敏感性分析，已更正为“部分支持”。 |
| 20-DeltaEvolve | 首句"不直接保存历史完整代码"易产生误导 | Level-3 全量代码完整保留于数据库（评估与衍生必需），仅 prompt 上下文中的示例使用 delta 差异压缩。 |
| 21-PhyloEvolve | 遗漏主干架构与学习理论 | 补充记录其 ICRL 理论框架、四智能体分工（NextStepper/Modify/Designer/Summarizer）与 Elite Pool 两种知识形态。 |
| 26-CogMCTS | em1/em2 与 rapid/complex 认知对应关系颠倒 | rapid/complex 是认知指导内部先后两阶段，其产物同时注入扩展动作；em1 与 em2 分别对应局部对比驱动与全局最优引导。 |
| 27-RefineEvo | "细化阶段生成代码"表述错误 | 代码生成始终由 Evolver 负责；Refinement 仅用于改写与优化算子自身的自然语言指令（incremental/radical）。 |
| 33-A2DEPT | 交叉 prompt 描述遗漏关键输入 | 补充记录其交叉算子 $e_1$ 输入除双亲主函数代码外，各包含对应的 Strategy（THOUGHT）字段。 |
| 36-EvoTune | 上层曾误记为"缺少去训练对照"，且同篇表格口径不一 | 原文明确比较了同构的无训练（FunSearch-style）基线，统一结论为：无训练搜索基线支持加入训练阶段这一整包机制的收益；但未必隔离训练阶段内部每个设计（全历史采样、退火调度、基模型重启），且总训练算力需另计。 |
| 39-RoCo | 遗漏生成侧算子与种群合并逻辑 | 补充记录 memory-guided elite mutation 三算子以及“协作产物 ∪ 变异产物 ∪ EoH 产物合并后选 TopN”的混合种群机制。 |
| 46-Latent-Heuristic-Search | 声称联合训练 program encoder | 原文明确使用预训练代码编码器（UniXcoder）与解码 LLM 并全程冻结，仅训练 Flow、Surrogate 与 Mapper；已全面删除“训练 encoder”表述，并明确区分潜空间连续优化步骤（零真实 evaluator 成本）与最终解码候选的真实评价。 |
| 50-GAE | 遗漏论文摘要与正文内部矛盾 | 澄清摘要称 SAC 选择父代与正文事实冲突（正文父代均匀采样，SAC 仅选择编辑元组）；GRPO 训练基线采用父代真实得分而非 GNN 嵌入。 |
| 51-DGA2D 与三种轨迹综合阅读 | 混淆求解器内部 pipeline 拓扑与搜索祖先演化路径，外推信用粒度为短历史结论 | 澄清 DGA²D 一阶信用为单次求解器内功能组件执行拓扑，非外层进化祖先路径亦非 prompt 长度，不可直接证明短历史优越性；明确解耦【形成谱系】、【搜索空间几何】、【求解行为 PSTraj】与【Pipeline 拓扑】四类轨迹；行为距离高未改善不能径判为无效漂移，低亦不能径判为停滞，它们是条件性诊断信号而非自动化分配结论。 |
| 59-GeoEvolve | 记录了不存在的 Code-to-Formula 组件 | 澄清系统仅包含 Code Evolver、Evolved Code Analyzer、GeoKnowRAG 与 Geo-informed Prompt Generator 四组件。 |
| 67-LinTree / 70-Memento-2 / 71-EMG | 将推理规划微调、双时间尺度渐近理论、专家参照轨迹直接当作 TraceAAD 的理论证明 | 明确保留为表示、检索与信用分配的启发，严禁用“同构”略过假设鸿沟：LinTree 基于 SFT+GRPO 微调且推箱子上仍低于 BFS(RL)；Memento 2 为无穷步极限且慢更新非工程保存准则；EMG 依赖具备正确性保证的专家轨迹，AAD 的 best-so-far 无此保证且强行对齐会抹杀替代方向。 |
