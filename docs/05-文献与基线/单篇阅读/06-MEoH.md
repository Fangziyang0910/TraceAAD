# MEoH

- 论文：MEoH；本地来源：`papers/MEoH/MEoH.tex`；设计对象：面向组合优化的启发式代码。

## 1. 核心问题与方法

MEoH 把启发式设计重构为**多目标优化**：目标向量为（最优 gap，运行时间），附录扩展三目标（加可读性）。五个生成算子（E1/E2/M1/M2/M3）明确标注 adapted from EoH，属继承；真正的机制贡献是 **dominance-dissimilarity 选择信号**：对种群两两计算 AST 相似度取负得相异度矩阵，与 Pareto 支配掩码逐元素相乘后按列求和（个体得分 $v_j=\sum_{i:P_i\prec P_j}(-\text{Sim}(P_i,P_j))$，被支配且与支配者代码雷同者受罚最重），该复合分同时用于父代采样（softmax）与种群截断（降序 top-N）两个位置。输出从单点最优变为非支配前沿。

## 2. 论文宣称的机制贡献（逐项）

- 把启发式设计多目标化，单次运行产出一组性能-效率权衡的启发式（相近 gap 下运行时间最多省 10×）。
- dominance-dissimilarity 把目标空间支配关系与代码空间相异度复合进同一评分，用于选择与种群管理两处。
- 收敛分析以 HV/IGD 显示 EoH 的多样性随代数塌缩（单目标进化在多目标度量下退化），MEoH 稳定上升。
- 收敛分析以 HV/IGD 显示 EoH 在双目标空间下的指标随代数下降（因其只优化质量单目标，在多目标度量下显示退化），MEoH 稳定上升。

## 3. 实验究竟支持了什么

|机制主张|论文证据（具体表/图/消融/章节）|证据等级|判断|
|---|---|---|---|
|MEoH 在论文基准上取得主结果|§Experiments 的 `tab: BPP`、`tab: TSP_GLS_random`、`tab: TSP_GLS`|直接支持（整法有效性）|匹配协议下的整法比较直接支持 MEoH 方案在所测基准上优于基线；不能顺带推出每个组件分别有效（需由消融实验分别隔离）。|
|dominance-dissimilarity 优于常规 MOEA|§Comparison to Conventional MOEAs，`fig: BPP_ablation`、`fig: TSP_ablation`|部分支持|胜 NSGA-II 与 MOEA/D，支持"传统支配+拥挤度不适合离散启发式空间"；但支配与相异度两项未单独隔离。|
|EoH 种群多样性塌缩被直接用作反例|收敛分析（HV/IGD 曲线与平均 dominance-dissimilarity 分数，3 runs）|直接支持|EoH 的 HV/IGD 随代数恶化，MEoH 稳定上升——单目标截断选择牺牲多样性的实验证据。|
|dominance-dissimilarity 优于常规 MOEA|§Comparison to Conventional MOEAs，`fig: BPP_ablation`、`fig: TSP_ablation`|部分支持|在所测任务与设置下 HV/IGD 优于 NSGA-II 与 MOEA/D；但这仅是特定参数/实现对比，不能过度外推为传统多目标机制“普遍不适合离散启发式空间”；且支配与相异度两项未做正交隔离。|
|EoH 种群在双目标度量下退化|收敛分析（HV/IGD 曲线与平均 dominance-dissimilarity 分数，3 runs）|部分支持|证据仅表明未优化时间的单目标搜索无法在“质量-时间”空间中维持前沿分布；多目标度量（HV/IGD）的数值退化不直接等于算法行为多样性或求解机制多样性的绝对塌缩。|
|某一算子是增益来源|无逐算子等预算消融|未验证|五算子继承自 EoH，整体优于基线不能分配信用。|

## 4. 机制的底层逻辑

阅读分析：dominance-dissimilarity 把"不被支配"（目标空间）与"和支配者代码相异"（结构空间）乘在一起，是选择信号从纯质量扩展到质量+结构相异度的代表形式。它的风险面在 AST 相似度与行为差异的错位（BehaveSim 后来证明同类度量在部分任务上失效）；三目标与逐算子信用分配未做。

## 5. 对 LLM4AD / TraceAAD 可学习之处

- 可学习点：选择信号复合"质量 + 与支配者的结构相异度"。前提：相似度可计算且与行为相关。风险：AST 相似与行为相似错位。最小验证：分桶报告相异度与下一代真实改进率的关系。
- 可学习点：把效率（运行时间）作为第二目标而非约束。前提：评价时记录耗时。风险：前沿上时间维度退化为噪声。最小验证：报告 Pareto 前沿随代数的展开形态。
- 记录候选运行时间作为基础度量：评测中记录候选求解耗时是关键的执行层观测数据，但**记录耗时绝不意味着必须将运行时间升级为外层搜索的第二优化目标**。
- 选择信号可复合“质量 + 与支配者的结构相异度”：前提是相似度可计算且与行为相关。风险是 AST 相似与行为相似错位。最小验证：分桶报告相异度与下一代真实改进率的关系。
- 备选验证设计（非当前必选）：若后续需要正式检验多目标机制能否迁移并改善单目标搜索，可参考 2×2 受控实验（原上下文/增加时间反馈 × 原选父/质量-时间选父），严格在同 1000 eval 预算下对比单目标最佳 fitness。
