# V10.7 修复、Smoke 与正式启动

批次：`20260907_bounded_formal`。启动于 2026-09-07 23:40（Asia/Shanghai），启动核查完成于 2026-09-07T23:44:50.325716+08:00。正式 15 路均存活并已产生有效评价；V10.6 原 12 路继续运行。

## 修复

- 删除全档案精确容量预筛，先在去重档案上分层，再抽样并精确检查；每个参考位置最多 32 次无放回尝试。
- Idea 输出要求不超过 100 words。原始 Idea 留档；提示视图超过 256 tokens 时整段省略，组合容量不足时也可省略，始终保留完整代码。
- 增加采样/调度时间和 tokenizer 计数接口调用数日志；未改 node ID 级父代分配。
- [机制设计](../../docs/01-主线版本/TraceAAD-V10.7-机制设计.md)已整理为当前规范；历史讨论已移至 ideas。

66 项相关测试通过，包括 1000 节点档案的计数调用上界，以及长 Idea 不剥夺父代资格。未修改运行中的 V10.6 源码。

## 真实服务 Smoke

local/OBP、server1/TSP、server3/CVRP 各 16 次真实评价，共 48 次；每路保留 8 个有效根初始化。全部有效，无生成或传输失败；各有 8 次采样搜索，总计 24 次全部展示 3 份程序，1/2 份比例均为 0%。

下表均为搜索阶段中位数，单位秒（tokens 和请求数除外）。

| 后端 | 采样 | 完整调度 | 计数接口调用/轮 | Prompt tokens | LLM | evaluator |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| server3 | 0.066 | 0.186 | 7 | 3785.5 | 37.606 | 13.564 |
| local | 0.074 | 0.160 | 6.5 | 2128.5 | 12.673 | 0.609 |
| server1 | 0.038 | 0.103 | 6 | 2752 | 42.163 | 0.063 |

采样最大耗时 0.121 秒以内；三路全程计数接口调用总数分别为 server3 79、local 76、server1 74。首次调度检查全部 8 个根的资格，使该轮计数达到 29，随后主要为每轮 6–7 次。计数包含缓存未命中的接口调用，不代表底层 HTTP 请求数（可能有重试或 apply-template）。单次响应、评价收据与源码指纹检查通过。

按节点和按完全相同代码聚合的父代选择 HHI，在这三条短 smoke 中分别相同；完整值保留在对应运行日志中。这仅检查运行与成本，不能判断完整搜索收益或重复代码问题在大预算下是否严重。

## 正式实验

五任务 × 三重复，seed=0/1/2，每路 1000 次真实评价，8 个有效根。模式 `sampled_trajectory_v1`，最多 3 份程序，窗口/输出预留/余量仍为 32768/16384/256。

| 后端 | 新 V10.7 | 原 V10.6 | 总占用/容量 |
| --- | ---: | ---: | ---: |
| local | 3 | 0 | 3/3 |
| server1 | 2 | 4 | 6/6 |
| server3 :8000 | 5 | 4 | 9/9 |
| server3 :8001（server3b） | 5 | 4 | 9/9 |

15 路源码指纹、单次调用协议、正式预算、上下文配置、存活状态、无重复调用 ID/评价收据、输入窗口均通过启动核查。检查时档案快照合计 90 次已完成评价、87 个有效节点；已有 13 个采样生成事件。运行仍在推进，各文件逐路读取，数值不是同一瞬间的事务快照。

已观察到 2 次无效生成和 3 次候选评价失败，均由现有流程正常处理；前者不占评价预算，后者占预算。15 路都已有有效节点，没有服务/调度错误。启动核查不等于实验完成。

- 调度会话：`v107_sched`。
- [批次 manifest](results/batch_20260907_bounded_formal.json)
- [启动核查快照](startup_20260907.json)
- 各路目录：`results/<task>/20260907_bounded_formal_<简称>_v107_rep<重复>/`；查看 tree_state.json、pending_candidate.json 和 tmux_run.log 获取最新状态。

## V10.7R 正式实验（2026-09-08 启动）

- 批次 `20260908_v107r_formal`，会话前缀 `v107r`，代码提交 `07a730cf`（`METHOD=v107r`，检查点 `version=1073`，`task_evidence_v1`，`--max-context-programs 2`）。
- 五任务 × 三重复，seed=0/1/2，每路 1000 次真实评价，8 个有效根；与旧 `v107`/`v106` 批次运行目录与指纹隔离。
- 启动时在跑占用：server3 4 路（1 路 v10.6 + 3 路 v10.7）、server3b 4 路；剩余槽位 server1 6、server3 5、server3b 5、local 3。launcher 按任务分散 + 最低利用率自动分配 15 路：server1 5、server3 5、server3b 3、local 2（每任务横跨 3 个后端）。
- 启动后总占用：server1 5/6、server3 9/9、server3b 7/9、local 2/3。server3 短暂满载，随旧路完成释放。
- manifest：`results/batch_20260908_v107r_formal.json`；launcher 日志：`results/launch_20260908_v107r.log`（`--watch` 常驻补跑）。

## 2026-09-08 23:25 后端迁移（server1/server3b 退役）

server1 :8080 与 server3 :8001（server3b）的 vLLM 已停止释放显存；全部负载迁至 server3 :8000 与 local。迁移 7 路（tsp_r1、cvrp_r1 → local；obp_r1、tsp_r2、op_r2、cvrp_r3、vrptw_r3 → server3），其余 4 路后端未变（op_r1=local；cvrp_r2、tsp_r3、op_r3=server3），v10.6 剩余 tsp_r1 仍在 server3。迁后占用 server3 9/9、local 3/3。

checkpoint 的 `mechanism.llm` 记录后端身份，换后端恢复会被 `_load_state` 拒绝（报错 status=error 重试至 attempts 耗尽）。修复为数据层修补：7 路 `tree_state.json` 的 `mechanism.llm` 改写为目标后端记录（自完成路 checkpoint 原样拷贝，源码哈希等其余字段未动），恢复后 `_save_state` 落盘的 mechanism 与代码放宽方案终态一致。另有 4 路（cvrp_r1、tsp_r2、cvrp_r3、op_r3）被杀时停在已预约未确认的评价上，恢复即 blocked；将 `pending_candidate.json` 的 phase 由 `evaluating` 翻回 `responded`（completion 已持久化、无收据，确定性评价重跑，candidate 序号不变）。被改动文件均留 `.pre_backend_migration` 后缀备份；op_r3 的 blocked summary 归档为 `run_summary.json.pre_backend_migration.blocked`。

调度器现以 `--max-attempts 25` 运行（迁移重试耗尽过默认 15）。迁移不改变采样配置：local 为 llama.cpp Q4_K_XL、server3 为 vLLM AWQ-INT4，同模型不同量化栈，各路后端轨迹可在 tmux_run.log 的 `llm=...` 行追溯。
