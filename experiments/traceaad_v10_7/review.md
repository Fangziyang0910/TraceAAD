# V10.7 采样模式实现 Review

本页记录首次实现审查。后续修订已删除全档案容量预筛，并将 Idea 改为有界可省略视图；当前规范以设计文档与运行说明为准。

审查基线：`9c85876e220a99ba3bcb19a0d40347218247119f`，对象为本轮未提交的采样实现、入口与测试，包括新增文件。规格为[完整设计文档](../../docs/01-主线版本/TraceAAD-V10.7-机制设计.md)及同代码随机完整记录、线性分位数和有界容量重试的补充约定。Standards 与 Spec 由两个独立 reviewer 检查，发现后修正并复查。

## Standards

未发现硬性仓库规范违例。保留一项性能关注：单参考精确容量预筛针对新父代可能需要 O(档案大小) 次同步 tokenizer 请求；请求缓存只对相同完整 Prompt 生效，32 次上限只限制后续组合重试。已修正最大程序数为 1 时仍预筛的冗余操作，并在运行说明中记录复杂度和日志位置。为保持规格规定的“可容纳候选先分层”语义，本轮保留精确预筛；尚无真实服务计时，不宣称该开销可忽略。

## Spec

初审发现：容量失败后重新抽层，不符合“在相应采样层中尝试其它候选”。现已修正为同槽固定层、无放回重试，层耗尽才换层，槽位总尝试不超过 32 次；回归测试通过，独立复查确认关闭。最终未发现未解决的规格缺漏。

确认实现：三种上下文模式；全档案去重及随机完整记录；分数分位分层与同分同层；完整 Idea/Code/fitness 按内部 fitness 排序；Refine 的底座索引不因排序更换；Pivot 无继承底座约束；Fuse 从样本确定 donor 并允许同血缘；容量不足减少程序；参考不增加父代次数；单次生成和原评价入档流程；独立上下文来源日志；请求前持久化与恢复复用；CLI 模式传递和检查点配置隔离。

## 验证

以下共 69 项通过：

```bash
.venv/bin/pytest -q \
  tests/method/test_traceaad_v107.py \
  tests/method/test_traceaad_v107_sampling.py \
  tests/method/test_traceaad_v106.py \
  tests/experiments/test_traceaad_v107_launch.py \
  tests/experiments/test_traceaad_v106_launch.py
```

新测试覆盖从未登顶、祖先和后代参考、同分不同代码、完全重复代码、多记录身份、并列分层、均匀对照、排序与角色、容量退化、有界重试、零参考、单次调用、父代计数、三个中断点恢复与配置漂移。测试使用可控生成响应和小型真实 Python 评价函数，不调用正式模型服务；未启动三组正式搜索或 held-out 比较，因此不能据此判断搜索收益。

最终：Standards 硬性问题 0，性能关注 1；Spec 未解决问题 0（初审 1 项已修正）。
