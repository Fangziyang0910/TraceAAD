# V10.9 实现审查与正式启动

## 交付范围

[V10.8 机制分析](../../docs/02-实验结果/版本机制诊断/2026-09-09-V10.8-思想形成与精炼.md)整理现象、代码机制与归因边界；[V10.9 设计](../../docs/01-主线版本/TraceAAD-V10.9-机制设计.md)以有限预算内形成并开发优秀算法为目标，将质量集中、近期试用、跨程序部件迁移、直接试验证据和数值精炼联动实现。

正式配置：五任务 × seed 0/1/2，每路 1000 次真实评价、8 个有效根；Refine/Tune/Pivot/Fuse 请求比例为 25%/25%/15%/35%。训练数据生成器、评价器与服务配置沿用现有协议。不从历史最好程序开始。整体系统收益与各因素因果收益分开判断。

## Standards 审查

首轮发现 Tune 增加了提示长度，但继承的父代适配检查仅覆盖 Refine/Pivot，边界程序可能在调度后超出容量并使恢复重复失败。已覆盖 Tune 并补边界回归。

复审无遗留必须修复项。版本继承、显式算子字典及调度复用符合仓库最小有效改动原则；共享监控保留旧版本入口并加入 V10.9 与 Tune。源码冻结与凭据读取的真实可用性另经启动验证。

## Spec 审查

首轮发现三项：上述 Tune 容量缺口；`edit_kind` 遗漏合法但评价失败的输出；轨迹提示正文未包含评价号。三项均已修复，分别补充边界/失败分类测试与提示正文。复审通过。

操作核验项是冻结环境读取凭据：采用本地 `.env` 链接复用原私有文件，没有复制密钥到源码或清单。四个后端真实请求均成功后关闭此核验项。另修正文案：最多三次启动尝试包含首次启动，至多两次自动恢复。

## 验证证据

- 方法、队列及 V10.7/V10.8 相关回归：66 项通过。
- 共享 V10.6 与 V10.9 监控补充：6 项通过，与前项重叠 1 项，即本轮共 71 个不同测试通过。
- 包含评价收据、事件、检查点三个崩溃位置的幂等恢复；未知评价阻断；复制拒绝不扣评价预算；真实调度计划15路与恢复检查；证据完整性、容量裁剪和历史 donor 可见性。
- 启动脚本语法、监控 JavaScript 语法、文档本地链接检查通过。监控 API 已验证 15 路计划、排队状态和调度器身份；没有进行浏览器视觉回归，未宣称页面呈现已逐项验证。
- 冻结目录四服务生成冒烟：server1/server3/server3b/local 均返回非空内容、正常结束；精确输入计数与服务 usage 均为 19 tokens。
- 冻结目录五任务完整评价链路各成功 1 次：TSP 6.823969、CVRP 18.688237、OP 12.734、OBP 744.5、VRPTW 33.072963。使用各任务自带模板程序，ACO 单 worker；这是依赖、接口、评价收据的功能验证，不是 V10.9 搜索表现，未计入15路正式预算。

完整本地证据：`results/startup_preflight.json`、`results/startup_templates/`。单元测试用替代模型，真实服务冒烟与真实任务评价分别验证，正式批随后验证真正的生成—评价链路。

## 冻结与启动

批次 `20260909_v109_formal`，源码基点 `6893d167d3065e5b766184b5d348b690530feae1` 加本工作区改动，实际冻结内容以清单哈希为准，未提交或推送。

- 运行副本：`results/runtime_20260909_v109_formal/`。
- 逐文件清单：`results/source_20260909_v109_formal.json`；副本内 `runtime_manifest.json`。
- 冻结清单 SHA-256：`f9b9e883824b5e640a19383ad063a38e956b5c4f2e1f0fe5985c7258e50e9435`。
- Python：`/home/fang/code/LLM4AD/TraceAAD/.venv/bin/python`，版本 3.11.13。Python 环境和模型服务未打包独立镜像。
- 调度会话：`v109_launcher`。启动时校验源码完整性，批次持久化绑定冻结清单身份。
- 调度清单：`results/batch_20260909_v109_formal.json`，15 路、总预算15000。
- 共享监控：[V10.9](http://127.0.0.1:8765/?version=v10_9)，会话 `v106_monitor`。旧 V10.8 正式批与分配实验仍可选择查看。

2026-09-09 22:22（北京时间）开始正式调度，每30秒最多补一路；既有实验保持运行。新任务在空闲服务逻辑槽启动，正在恢复的任务保持后端。实际进度读取上述清单与评价收据，不以 tmux 存活替代产生有效结果。

最终启动核验：2026-09-09T22:26:08.275598+08:00，**7 路运行、8 路排队**；557 个冻结源码文件哈希校验一致。累计 14 次真实评价收据，其中 13 次成功；五个任务均已有成功生成并评价的正式候选。一次生成候选评价失败按约定扣除预算，没有触发重做或退款。server1/server3/server3b/local 的总占用分别为 6/9/9/3，均未超过配置容量，包含原有实验。监控同样报告 15 路、14 次评价、7 路运行与 8 路排队。逐路证据见本地 `results/startup_verified_20260909.json`。这是启动快照，终局效果尚待完整预算结果。
