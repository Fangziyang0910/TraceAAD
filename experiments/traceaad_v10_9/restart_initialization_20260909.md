# V10.9 初始化机制更新与优先调度

2026-09-09。当前批次 `20260909_v109_initaware`，内部状态版本 **1091**。原 `20260909_v109_formal`（1090）已安全停止并标记 superseded，保留全部文件，不能与新版拼接为一条搜索曲线。新版五任务 × 三重复，从零开始，每路 1000 次真实评价。

## 本轮改动

第一个根独立生成；后续根读取此前已评价根的完整代码与成绩，提出另一个有竞争力的决策假设，允许借鉴有效组件。上下文装不下时，依次尝试最新、最早、次新、次早根，程序整块纳入或跳过，记录实际引用及遗漏。提示要求差异体现在实际决策，未实现行为硬去重或思想识别。

仍生成 8 个有效根，不增加每根强制开发、不注入任务专属种子。Init 与已有任一根 AST 完全相同则不评价，同分不同代码仍可进入搜索。后续分配、Refine/Tune/Fuse/Pivot 和评价口径保持原 V10.9 设计。详见[机制文档](../../docs/01-主线版本/V10.9/机制设计.md)。

## 冻结身份

- runtime：`results/runtime_20260909_v109_initaware`
- 源码清单：`results/source_20260909_v109_initaware.json`，558 个源码/配置文件。
- runtime_manifest SHA-256：`9d33de4514d66c76f9fcf89e8ad957bb1f79f0011379c4ddde30a7cabdc70984`
- Git 基点：`6893d167d3065e5b766184b5d348b690530feae1` 加冻结清单中的工作区改动；不是纯提交状态。
- Python：现有 `.venv/bin/python`，3.11.13；模型与评价环境沿用原配置。

启动命令使用冻结清单给出的解释器和 runtime，在 `v109_launcher` 中运行，并添加 `--interval 5`，每次最多启动一路。实际命令及队列命令保存在 `results/restart_commands_20260909.json`。运行清单为 `results/batch_20260909_v109_initaware.json`。

## 优先级与恢复

V10.8 正式批的 8 路运行继续，另 7 路已完成。12 路 TSP 分配消融全部暂停，释放原有逻辑槽给新版 V10.9。各消融保留原冻结源码，后端等价并按空槽分配；没有覆盖检查点或重置启动次数。

暂停时先固定主进程状态；若存在尚未落收据的评价，恢复进程等待其完成。最终 12 路均在 pending selected 边界退出，评价预算逐路与收据相等，无未知评价。核验文件：`results/pause_checkpoint_audit_20260909.json`。

| 消融臂 | rep1 | rep2 | rep3 |
| --- | ---: | ---: | ---: |
| A | 614 | 595 | 405 |
| B | 538 | 425 | 518 |
| C | 747 | 384 | 500 |
| D | 665 | 448 | 480 |

`v108alloc_launcher` 在本次重启时运行冻结的 `experiments.infra.priority_queue`，每 30 秒检查 V10.8 正式批和新版 V10.9 清单。两批全部为 finished 后，它切换到原消融 runtime，执行 原消融启动命令，从检查点恢复。缺失、blocked、stopped 都不会提前释放队列；这类异常需要处理后才能继续。等待日志为 `results/allocation_priority_queue_20260909.log`。

共享监控已重启，V10.9 仅显示新版 15 路，旧批仍保留在磁盘。TSP 消融概览与详情均显示排队并保留评价进度；会话重新出现时，即使结果文件尚未更新，详情也刷新为运行中。

## 验证与审查

本轮相关方法、调度和四版监控共 67 个测试通过；包括完整根引用、容量整块裁剪、同分不同代码接受、复制根不占评价预算、提示恢复、持久化评价幂等、15 路计划、恢复路由与搜索状态保留、依赖完成门槛以及监控暂停/恢复缓存。

Standards 审查：0 项遗留。Spec 审查：发现 1 项 P2（暂停详情错误显示中断），修复并复核后 0 项遗留。已核验新冻结源码及原消融源码哈希。研究效果尚未验证，初始分数或程序差异不能替代同预算终局和泛化结果。

## 实际启动验收

2026-09-09T23:02:49.862885+08:00 快照：新版 15 路全部运行，V10.8 正式批 8 路运行、7 路完成，12 路消融排队。新版已有 16 次评价（15 次成功、1 次评价失败），失败按预算记录后继续生成。TSP 与 OBP/VRPTW 已实际生成多个根，后续 Init 提示包含此前根的完整代码及对应标识；五任务至少一路进入下一根生成。

已通过共享监控 API 核验上述计数，并两次读取每个消融臂的实际暂停详情，均显示 queued 且保留进度。验收快照在 `results/restart_verified_20260909.json`；运行仍在早期，不能据此判断初始化质量或终局收益。

## 2026-09-10 调度调整（历史，TSP 消融现已结项）

后续状态：TSP 分配消融在 9311 次评价处提前结项，不再补位、恢复或监控。下述恢复和槽位验收均是当时的历史记录。

用户要求利用当前空闲槽位，让 TSP 消融逐步恢复。已取消等待两个正式批全部结束的条件，停止原等待器，重新在原冻结 runtime 中启动消融调度器（每 10 秒检查空位）。V10.8/V10.9 正式实验继续运行；消融按任意后端的空闲槽位恢复并持续补位。12 路检查点均为 selected，评价次数与收据一致；未更改算法源码或评价进度。操作和恢复前快照见 `../traceaad_v10_8/results/resume_free_slots_20260910.json`。

### 同日补充：所有后端等价

用户明确所有启用后端等价，哪里有空就用哪里。当前三个调度器已采用任意空槽分配；消融 11 路运行，1 路排队。恢复只同步检查点内的服务地址和模型服务名称，以及运行配置中的路由字段，不修改候选、随机状态、源码哈希或评价收据。第一次更新前保留检查点备份。

冻结批通过独立的 `experiments/infra/equivalent_backends.py` 调度适配器调用原启动器，搜索继续导入原冻结源码。适配器副本及其摘要记录在 `../traceaad_v10_8/results/equivalent_slots_20260910.json`；后端字段只用于运维定位，不作为算法效果差异的解释。相关调度与路由恢复测试 7 项通过。

运行验收：27/27 槽占满（server1 6、server3 9、server3b 9、local 3）；本轮换服务的三路已分别从 665→666、448→450、500→501 继续评价。快照见 `../traceaad_v10_8/results/equivalent_slots_verified_20260910.json`。
